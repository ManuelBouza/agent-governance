# R029 / T067 Materialization Qualification

This directory contains the Orchestrator-owned Stage 5 semantic/conformance inputs for post-materialization qualification of D082.

- `preservation-ledger.json` maps all 79 R029 S1 semantic units to their materialized root/route/reference destinations.
- `activation-corpus.json` freezes production Maintainer/transverse positive, anti-trigger, composition, ambiguity, and workspace-internal-route cases.
- `qualification-profile.json` freezes topology, cold-start, descriptor, representative context-load, and reference-hop measurement inputs.
- `normative-rule-index.json` records stable normative ownership families so references are not mistaken for duplicate normative ownership.
- `descriptor-equivalence.json` records the Stage 5 semantic-equivalence disposition governing reuse of the prior 36/36 Codex transverse evidence.

`tools/r029_materialization_qualify.py` is the deterministic measurement/conformance helper. Stage 6 owns actual execution and may persist its non-Markdown result under this evaluation surface as authorized by T067.

These assets are semantic authority for T067 qualification and must not be weakened by the Executor to obtain a passing result.
