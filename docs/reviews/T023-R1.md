# T023 — Review R1

Status: `BLOCKED / EXPERIMENT CLOSED`

## Identity

- Task: `T023`
- Contract: `docs/tasks/T023-unified-skill-profile-activation-evals.md`
- Canonical execution base: `c7239ffd5b97d225e85499c25a6afd6d9880cdf9`
- Submitted Executor HEAD: `2da17551d42b5a2fc6b37ad952ea835a1a89bcbe`
- Implementation/evidence anchor: `f87b924f1b9481dde68279f24ff4c4c35f51c868`
- Handoff: `handoffs/T023-executor-handoff.json`
- Evidence integration PR: `#229`
- Evidence integration merge: `c070c0bd5924aa283237d3a1995c58579bd0500a`
- Frozen oracle: `MG1-T023-TOPOLOGY-ORACLE-v2`
- Corpus: `MG1-T023-CORPUS-v1`

## Result

The MG1-v2 experiment executed completely: 30 cases x four candidates x three clean trials = 360 live Codex/native-Windows/GPT-5.6-Sol/Medium trials. Raw and structured evidence are retained. The full deterministic suite passed with 417 tests; profile-isolation and source-independence regressions passed; Ruff and diff checks passed; all frozen MG1-v2 semantic assets remained unchanged.

No topology is accepted. The frozen rule returns `BLOCKED` because neither B0 nor B1 qualifies, and all four candidates fail at least one mandatory/qualifying live-routing condition. The evidence branch is therefore canonical experimental evidence, not accepted topology authority.

## Independent convergence findings

Two upstream semantic defects are confirmed for any future experiment revision:

1. `loaded_reference_bytes` is computed only from the case expected-capability set and shared references. It is candidate-invariant (median 960 bytes for B0/B1/F2/G3), so the frozen 80%/85% context-improvement gates cannot detect presentation-topology load differences.
2. The v2 ambiguous-case grader counts any Skill activation as `ambiguous_context_permission_broadening`, even when a neutral dispatcher/router activates only to return the required `clarify-context` outcome. D050/T023 require bounded behavior and no permission/capability broadening; they do not require zero activation in every ambiguous case.

These defects do not justify selecting a v2 candidate. They also do not erase the observed routing failures. In particular, v2 remains blocked because qualification failures exist independently of the context-load defect.

## Anti-post-hoc boundary

MG1-v2 is closed. Its corpus, presentations, metrics and selection result must not be modified and rescored to force a topology outcome.

Any continuation is a new pre-registered experiment with:

- a new oracle identity and presentation revision;
- a fresh holdout corpus not used in v2;
- corrected ambiguity semantics that distinguish activation from permission/capability grant;
- candidate-sensitive observed host load accounting;
- exact candidate surfaces frozen before the new live run;
- unchanged Core/engine/profile semantics and D051/D052 invariants.

## Acceptance criteria disposition

- AC-T023-1: PASS for the executed v2 experiment.
- AC-T023-2: PASS; all 360 trials executed.
- AC-T023-3: PASS as evidence production; context comparison itself is semantically defective for selection.
- AC-T023-4: PASS preservation; oracle defects were reported, not mutated by Executor.
- AC-T023-5: PASS.
- AC-T023-6: FAIL/BLOCKED; no qualifying single-family reference and no selected topology.
- AC-T023-7: PASS.

T023 is not accepted. Re-entry is `Specify` through the separately persisted T042/MG1-v3 restart authority.
