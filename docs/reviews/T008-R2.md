# T008-R2 — Handoff identity correction

Review: T008-R2  
Status: REWORK_REQUIRED  
Task: T008 — EGLL deterministic learning-detector MVP  
Reviewed executor HEAD: `b67d9894e6f979d04f17ed5e512cf92a6601d228`

## Review result

The T008 implementation and verification gates are otherwise acceptable, but the persisted handoff contains an invalid base identity and therefore fails the Task Contract's remote-auditability acceptance criterion.

Observed handoff field:

`base_sha = 2be1ad9329e0b7901b784cff542aca50a8516927`

Authoritative `develop` incorporated for the successful T008 rerun:

`2be1ad97fefb43e7116d534eecfd420a3c4f9923`

GitHub cannot resolve the observed handoff SHA as a repository commit. Independent comparison from the authoritative `develop` SHA to the reviewed executor HEAD confirms the effective T008 diff remains limited to the three detector/test/fixture artifacts plus the handoff.

This is a deterministic instance of the accepted T008 fingerprint `task.handoff.identity_mismatch`.

## Required rework

On the existing `test/egll-deterministic-learning-detectors` branch:

1. preserve detector implementation, fixtures, and tests unchanged;
2. correct `handoffs/T008-executor-handoff.json` so its base identity exactly matches the authoritative `develop` revision actually incorporated for the successful rerun;
3. ensure all branch/base/head/task/handoff identity fields are internally consistent and resolvable from canonical Git/GitHub;
4. rerun the original T008 focused/full/Ruff gates;
5. commit and push the corrected handoff/current branch state and return the normal T008 response.

No detector semantic change, fixture expansion, Governance Core change, dependency/configuration change, or new task scope is authorized.

## Acceptance after rework

T008 may be accepted when the corrected handoff identity resolves and matches the actual incorporated baseline, the detector/test/fixture artifacts remain unchanged, all original gates remain green, and no new unresolved issue or procedural nonconformance appears.

## Learning consequence

This review is evidence that the new EGLL identity-mismatch fingerprint is useful on the source-maintenance workflow. Detection does not self-approve remediation: ChatGPT persists review authority, the executor corrects the non-Markdown handoff, and ChatGPT re-verifies.
