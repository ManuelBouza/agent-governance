# T050 RF1 — Characterization Baseline Acceptance

Status: ACCEPTED

## Scope

This review accepts the remotely persisted RF1 characterization baseline for `docs/tasks/T050-agent-legible-harness-refactor-and-code-health-ratchet.md` and authorizes RF3 structural mutation on the same Executor refactor branch.

## Reviewed identities

- Canonical base: `develop@e7c3be305afd4be34ee87956603aaf115fd7c988`.
- Refactor branch: `refactor/t050-agent-legible-harness`.
- Characterization HEAD: `6d3384916d7b753a72a54fb6d2b77ac15b8d7182`.
- Persisted RF1 checkpoint HEAD: `6a00be1a807081d6e17b20088426ca060cd59055`.
- Baseline handoff: `handoffs/T050-rf1-baseline.json`.

The refactor branch is based exactly on the canonical T050 base. From base to the persisted RF1 checkpoint it adds only:

- `tests/test_t050_harness_cli_characterization.py`;
- `handoffs/T050-rf1-baseline.json`.

No structural mutation of `evals/skill_activation_topology/harness.py` has begun. The baseline records `harness.py` at 3,133 physical LOC and SHA-256 `51f570333bb3c147149d57ecd1e746616c050fc1ce37b9aa5d82576cd91c6759`.

## Characterization acceptance

The accepted RF1 baseline covers the surfaces required by T050 through the existing focused harness suite plus the added CLI characterization:

- CLI subcommands, arguments, defaults, dispatch and exit behavior;
- frozen-input validation;
- candidate/fixture materialization and workspace behavior;
- Codex adapter/profile binding;
- trace/body-read/access-rejection classification, including the corrected V10 regression;
- retry/capacity/scheduling/futility behavior;
- evidence/provenance shapes;
- scoring/selection behavior.

The added CLI test fixes the externally visible facade contract for `validate`, `materialize`, `run`, `score`, and `verify`, including defaults and usage/error exits. It does not redefine semantic outcomes.

## Verification

RF1 reports:

- Ruff check: PASS;
- Ruff format check: PASS;
- focused characterization: 71 passed;
- full pytest: 476 passed;
- isolated pre-existing failures: none;
- synthetic Skill canary calls: 0;
- acceptance prompts: 0;
- scored observations: 0;
- other provider/model calls: 0.

Frozen MG1 semantic asset hashes are recorded and unchanged.

## Baseline freeze

The characterization meaning represented by the RF1 branch state is now frozen for T050.

During RF3/RF4 the Executor MUST NOT weaken, delete, reinterpret, or rewrite the accepted baseline merely to accommodate the extracted architecture. A genuine baseline defect, semantic ambiguity, or need for behavior change requires Orchestrator re-entry before changing the baseline meaning.

Supplementary Executor-owned implementation tests may be added as needed provided they do not weaken the accepted characterization.

## RF3 authorization

RF3 is authorized on the existing `refactor/t050-agent-legible-harness` branch, preserving the accepted RF1 checkpoint as an ancestor.

The Executor should continue T050 without opening a new refactor branch and without integrating the RF1 branch into `develop` before RF5/RF6. The final handoff remains `handoffs/T050-executor-handoff.json` as specified by the refactoring workflow and T050.

All T050 constraints remain controlling, including:

- behavior-preserving decomposition only;
- `harness.py <=500` physical lines;
- extracted modules <=1000, target <=500;
- code-size/complexity ratchets and deterministic symbol map;
- no changes to D052 semantic assets or committed Markdown;
- zero live MG1/provider/model calls.

## Decision

`RF1 ACCEPTED — PROCEED TO RF3`.
