# T034-R1 — Native SDD executable materialization review

Review-ID: `T034-R1`  
Task: `T034`  
Disposition: `REWORK_REQUIRED`  
Re-entry stage: `Design / Plan & Trace`  
Reviewed canonical base: `develop@47410b601c0e03bc128e92c99509f678317aa91c`  
Submitted branch: `feat/t034-native-sdd-executable-materialization`  
Submitted terminal HEAD: `83abcafc60c943040fa4aa70f242950220851981`  
Implementation review anchor: `3c85675394f4764f7436bbbb12e75de64a505f68`  
Frozen oracle: `T034-A2-v1` / `tests/test_t034_native_sdd_conformance.py`

## Convergence result

The Executor correctly stopped instead of expanding scope. The submitted branch is two commits ahead of the reviewed canonical base and contains only the represented T034 implementation plus terminal handoff. No committed Markdown and no frozen-oracle drift were introduced.

The in-authority T034 implementation is directionally accepted for rework preservation:

- `SDD.md` / `SDD-Version` were added to the existing closed Core inventory and deterministic helper inventory;
- the T015 `no_sdd` corpus/grader/self-tests were synchronized to the exact frozen native fallback vocabulary;
- the two already-authorized artifact protocol expectations were advanced to `1.14.0`;
- no external SDD dependency, second SDD runtime/lifecycle, task parser/schema, handoff schema or CLI-surface expansion was introduced;
- focused native-fallback verification passed and the frozen oracle blob remained unchanged.

T034 cannot be accepted yet because three integrated baseline/Plan gaps prevent the required bootstrap/package/full-suite evidence. These gaps require upstream Design/Plan authority rather than Executor inference.

## Finding 1 — required package JSON protocol metadata was omitted from T034 scope

Canonical `develop` currently contains:

- `governance-skill/assets/CAPABILITIES.template.json` with `protocol_version: 1.13.0`;
- `governance-skill/assets/STATE.template.json` with `protocol_version: 1.13.0`.

The accepted Core is `Protocol-Version: 1.14.0`; existing package validation therefore fails before normal bootstrap/CLI verification can proceed.

### R1 Design/Plan resolution

T034 R1 explicitly authorizes the Executor to mechanically synchronize only these two fields to `1.14.0`:

- `governance-skill/assets/CAPABILITIES.template.json`;
- `governance-skill/assets/STATE.template.json`.

No other schema, key, default state, capability semantics or template behavior may change under this authority.

This is a completion of R-T034-3 package/protocol alignment, not a new product behavior.

## Finding 2 — D054 planning introduced a source Skill asset that the self-contained artifact omits

`governance-skill/assets/RUNBOOK.template.md` is now a canonical source Skill asset from the integrated D054 planning gate. Existing artifact parity intentionally requires the self-contained artifact's `assets/` tree to match canonical `governance-skill/assets/` byte-for-byte, but `src/agent_governance/artifact.py::SKILL_SOURCE_FILES` does not include the new template.

This is an integrated source/package baseline defect introduced after the original T034 gate, not evidence that T034 needs D054 runtime activation.

### R1 Design/Plan resolution

T034 R1 explicitly authorizes:

- `src/agent_governance/artifact.py` only as needed to add the existing `assets/RUNBOOK.template.md` to the current explicit Skill artifact allowlist;
- the already-authorized `tests/test_governance_artifact.py` only for the corresponding mechanical expected-file/parity update;
- other executor-owned package tests only when mechanically required by that exact asset-copy correction.

This authority does **not** activate D054 Consumer semantics, does not create `.agent-coordination/runbooks/`, does not install/copy the runbook template during Consumer bootstrap, does not add recipe persistence, and does not advance the routed Core protocol. T035 remains the owner of native runbook/recipe runtime readiness.

## Finding 3 — reference-integrity classifier mistakes SDD taxonomy for repository paths

The canonical reference-integrity harness extracts inline-code spans and then treats many slash-containing tokens as path candidates. The integrated SDD documentation now contains slash-separated stage/delta taxonomy that is prose, not a repository path. Representative currently failing forms include:

- `ADDED / MODIFIED / REMOVED / PRESERVED` in `AGENTS.md`, `governance-core/GOVERNANCE.md` and `governance-core/LIFECYCLE.md`;
- `Converge / Accept` in `governance-core/PROTOCOL.md`;
- `Converge / Accept / Evolve` in `governance-core/EXECUTION.md` and `governance-core/HANDOFF.md`;
- `Converge/Accept/Evolve` in `governance-core/LIFECYCLE.md`.

These are accepted SDD terminology. Rewriting their semantics merely to satisfy a heuristic reference parser is not required. The defect is in path/prose classification.

### R1 Design/Plan resolution

T034 R1 authorizes a narrow technical correction in:

- `tests/_helpers.py`;
- `tests/test_reference_integrity.py`;

so the reference-integrity harness distinguishes accepted slash-separated SDD taxonomy/prose from concrete internal repository paths.

The Executor owns the implementation technique, but the correction must preserve these constraints:

1. do not broadly exempt every token containing `/`;
2. existing concrete relative-path, dotfile, missing-path and parent-traversal negative controls remain effective;
3. Markdown links and concrete backticked repository paths that are path candidates under the existing contract remain checked unless a focused regression proves they are non-path prose;
4. the accepted SDD taxonomy forms above are not classified as repository paths;
5. no consumer-side path exemption, outside-repository protection, glob handling or missing-reference fail-closed behavior may be weakened;
6. add focused deterministic regression coverage for both the SDD-taxonomy non-path cases and preserved concrete-path cases.

This is a test-harness classification correction. It does not modify the frozen T034 semantic oracle or any committed Markdown.

## R1 authoritative scope additions

For the resumed T034 execution only, this review revises the original Task Contract by adding the following exact authorized paths/classes:

- `governance-skill/assets/CAPABILITIES.template.json` — only `protocol_version` -> `1.14.0`;
- `governance-skill/assets/STATE.template.json` — only `protocol_version` -> `1.14.0`;
- `src/agent_governance/artifact.py` — only the existing `RUNBOOK.template.md` artifact allowlist/copy parity correction;
- `tests/_helpers.py` — additionally the narrow reference path/prose classifier correction described above;
- `tests/test_reference_integrity.py` — focused regression coverage for that classifier correction;
- executor-owned package/reference tests only when mechanically necessary to prove these exact corrections.

All original T034 exclusions and acceptance semantics remain in force except where this review explicitly expands these paths. The frozen oracle `tests/test_t034_native_sdd_conformance.py` remains immutable.

## Rework procedure

Continue on the existing represented branch `feat/t034-native-sdd-executable-materialization` and preserve both submitted commits/history.

After this review authority is integrated into canonical `develop`:

1. synchronize the canonical remote;
2. safely reconcile the new current `develop` into the existing T034 branch without discard, branch recreation, force-push or represented-history rewrite;
3. reload current repository authority and load both the original Task Contract and this review;
4. preserve the accepted implementation already present unless a correction is mechanically required by the R1 fixes;
5. implement only the three resolutions above;
6. perform Code Review & Verify again against the original T034 contract plus this R1 revision;
7. update `handoffs/T034-executor-handoff.json` with the new implementation anchor and complete evidence;
8. commit the complete R1 rework and perform one planned final push under D048;
9. verify remote branch HEAD and return the canonical terminal fields.

If safe reconciliation cannot preserve represented history, or any R1 correction requires semantic oracle/Markdown/D054 runtime/T035/T021/T022 expansion, return `BLOCKED` instead of improvising.

## Required R1 verification

At minimum the resumed handoff must provide green evidence for:

- frozen `tests/test_t034_native_sdd_conformance.py`;
- complete Consumer trigger corpus/grader tests;
- focused bootstrap/validate and missing/tampered Core controls;
- complete Governance artifact/self-contained package suite including `RUNBOOK.template.md` parity;
- complete reference-integrity suite plus focused classifier regressions;
- stable CLI v1 non-regression suite;
- native-Windows canonical:
  - `uv run --locked ruff check .`;
  - `uv run --locked ruff format --check .`;
  - `uv run --locked python -m pytest`.

The final full suite must not be made green by skipping, xfail-ing, deleting or weakening existing deterministic claims.

## Disposition

`REWORK_REQUIRED`.

Do not open or merge an implementation PR from `83abcafc60c943040fa4aa70f242950220851981`. Resume T034 only after this review/re-entry authority is integrated into canonical `develop`.
