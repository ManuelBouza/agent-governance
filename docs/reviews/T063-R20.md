# T063-R20 — Hardened V8 Stage 7 Convergence

Status: ACCEPTED / V8 BLOCKED EVIDENCE VALID / FROZEN MAPPING NOT QUALIFIED / NO SUCCESSOR RUN REQUIRED  
Date: 2026-09-12  
Task: `docs/tasks/T063-adaptive-worker-routing-requalification.md`  
Readiness authority: `docs/reviews/T063-R19.md`  
Evidence branch: `test/t063-adaptive-worker-routing-requalification-v8`  
Authorized candidate: `afdae0050226d61a10269f63017e2fac99eef644`  
Terminal evidence HEAD: `b4afbe30508ab44b275d66bad92695c5d3571a9d`

## Stage 7 decision

The hardened v8 Stage 6 result is accepted as valid historical evidence. The formal v8 run remains incomplete and execution-invalid for run-level comparison, so its frozen `pilot_decision` remains `null` exactly as emitted.

No v8 replay, continuation, replacement arm, compensating provider call, or v9 successor run is required to decide the frozen adaptive mapping. The already completed valid quality evidence makes ADAPTIVE qualification mathematically unreachable under the prospectively frozen 4/4-per-probe and 12/12-global rules.

This review does not retroactively rewrite the v8 terminal classification or create a synthetic pilot decision.

## Evidence identity and boundary

Remote verification established:

```text
candidate: test/t063-adaptive-worker-routing-requalification-v8@afdae0050226d61a10269f63017e2fac99eef644
evidence:  test/t063-adaptive-worker-routing-requalification-v8@b4afbe30508ab44b275d66bad92695c5d3571a9d
compare:   exactly one commit ahead of candidate
changed:   only the two authorized v8 JSON evidence files
```

Changed files:

```text
handoffs/T063-adaptive-worker-routing-telemetry-v8.json
handoffs/T063-executor-handoff-v8.json
```

No product source, test, configuration, Markdown, schedule, oracle, threshold, profile or decision-taxonomy mutation occurred in Stage 6. `stage6_repairs=[]`.

## Provider-free and runtime gates

The Executor reports all mandatory pre-provider gates passing before scored execution:

```text
candidate identity / ancestry: PASS
native Windows:                PASS
Codex CLI:                     0.153.4
App Server:                    0.153.4
auth:                          chatgpt
deterministic tests:           67 passed
compileall:                    PASS
Ruff:                          PASS
oracle preparation:            PASS
D077 stable check:             PASS — 0.154.0 remained latest stable
```

No D076 material ephemeral artifact was created by the Executor.

## Persistence-barrier result

The v8 persistence correction itself did not block.

Provider accounting records 22 scored parent turns / child attempts and 22 reattachment retries. Every observed retry used the accepted `EMPTY_ROLLOUT` path, reused the exact child, rechecked parent residency, and created no new provider turn.

No `ROLLOUT_NOT_FOUND_FOR_EXACT_CHILD` event occurred in this run. Therefore v8 supplies positive execution evidence that the hardened two-condition adapter did not regress the already-qualified empty-rollout path; it does not supply a live positive occurrence of the newly hardened no-rollout classifier.

## Terminal blocker

Execution stopped fail-closed on scheduled arm 22, `R4-P2-CONTROL`, after public child correlation and reattachment, because the child-produced contract receipt contained:

```text
observed spawn_trigger_received:
T063-RW-8e2df1b053569394880b48aaf6039f9a

expected spawn_trigger_received:
T063_TRIGGER_80baf0e9971d0eb80702df696a117f61
```

The contract nonce and task-message digest matched. No replay, replacement, compensating scored call or semantic repair followed.

Under the frozen v8 Task Contract this receipt mismatch is correctly fail-closed and therefore leaves:

```text
run_execution_validity: INVALID
run_model_comparison_eligible: false
pilot_eligible: false
pilot_decision: null
```

## Receipt attribution boundary

Stage 7 does **not** adopt the Executor wording that the observed `T063-RW-*` value was proven to be a runtime-generated transport token.

R021 already established that `spawn_trigger_received` is a worker self-attestation, not a provider-signed/public exact spawn-argument receipt. The public V2 surface exposes exact child correlation through `subAgentActivity` but not the exact model-authored `spawn_agent.message` argument.

Official Codex `rust-v0.153.4` source shows that the parsed `spawn_agent` `message` is passed into `communication_from_tool_message`, which then constructs the child `InterAgentCommunication`; it does not define an Agent Governance-specific `T063-RW-*` rewrite. The published v8 candidate diff likewise contains no `T063-RW` literal.

Consequently the arm-22 mismatch cannot distinguish, from qualified public evidence alone, among:

- parent model supplied a different non-substantive spawn message;
- child received the expected trigger but misreported it in its final JSON;
- another unobserved transport-side condition occurred.

Canonical Stage 7 blocker diagnosis:

```text
WORKER_SELF_ATTESTED_TRANSPORT_RECEIPT_MISMATCH
ATTRIBUTION_AMBIGUOUS
```

It is not accepted as worker-quality evidence, and it is not accepted as proof of a Codex runtime rewrite defect.

## Valid scored evidence before the block

Twenty-one children completed with mandatory D063 receipts and remain valid model-quality evidence.

Observed aggregate state:

```text
ADAPTIVE: 11 attempts, 9 PASS, 2 valid quality FAIL
CONTROL:  10 attempts, 9 PASS, 1 valid quality FAIL
quality-evidence children:   21
efficiency-eligible PASSes:  18
```

Per-probe state at stop:

```text
P1 ADAPTIVE: 3/4 PASS
P1 CONTROL:  4/4 PASS

P2 ADAPTIVE: 4/4 PASS
P2 CONTROL:  3/3 PASS   # fourth CONTROL arm is the blocked arm 22

P3 ADAPTIVE: 2/3 PASS   # one remaining scheduled arm
P3 CONTROL:  2/3 PASS   # one remaining scheduled arm
```

These are historical v8 observations. Because the run is incomplete, they are not a complete matched-arm cost comparison and cannot support an accepted-quality efficiency claim.

## Monotone qualification proof

The frozen v8 qualification rule is conjunctive:

```text
per probe/profile qualification = 4/4 PASS
global ADAPTIVE qualification   = P1 4/4 AND P2 4/4 AND P3 4/4
```

The already valid observations establish:

```text
P1 ADAPTIVE = 3/4 -> permanently QUALITY_NOT_QUALIFIED
P3 ADAPTIVE = 2/3 with one arm remaining -> maximum possible 3/4
```

Therefore, regardless of the unexecuted outcomes of arms 22-24:

```text
ADAPTIVE global 12/12 qualification: IMPOSSIBLE
QUALIFIED_PROFILE_AND_USAGE_EFFICIENCY: unreachable
QUALIFIED_PROFILE_ROUTING_ONLY:          unreachable
ADAPTIVE_QUALITY_QUALIFIED_CONTROL_DEFICIENT: unreachable
```

If all remaining arms could hypothetically complete without any execution-validity blocker while preserving the 21 already scored results, the only reachable frozen complete-run taxonomy value would be:

```text
NOT_QUALIFIED
```

This is a logical consequence of prospectively frozen thresholds and valid first-attempt outcomes, not an outcome-conditioned rescore.

## Efficiency interpretation

No accepted-quality efficiency claim is available.

The frozen efficiency comparison is eligible only when both profiles globally qualify. ADAPTIVE is already unable to qualify. Raw partial token/duration totals therefore remain descriptive historical telemetry only and must not be promoted into a savings claim.

## T063 / R007 disposition

T063 has now answered its bounded calibration question sufficiently to stop provider-backed iteration:

```text
frozen adaptive mapping qualification: NOT QUALIFIED
formal v8 pilot_decision:               null (run incomplete; preserved)
new successor run required:             NO
v8 continuation/rerun authorized:       NO
v9 authorized:                          NO
accepted-quality efficiency claim:      NOT AVAILABLE
R007 global routing policy:             NOT ADOPTED
```

R007 remains `EVALUATING` as broader research authority. This review rejects only the exact frozen T063 calibration mapping; it does not prove that every possible future task-adaptive routing policy is invalid.

A materially different routing mapping may be explored only under a new explicit Human-selected objective with a fresh prospective design. It must not be represented as continuation or completion of v8.

## Coordinator and launch closure

`AG | agent-governance | T063 | root-8` is retired after the consumed v8 run.

The v8 Human launch authority is consumed. The active Task Contract remains historical evidence for the v8 experiment; its launch block must not be reused.

No Executor is authorized for T063 after this convergence.
