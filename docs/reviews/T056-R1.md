# T056-R1 — Codex Read-Only Child Requalification Review

Status: ACCEPTED_EXECUTION_PARTIAL_OBSERVABILITY  
Task: `docs/tasks/T056-codex-read-only-child-requalification.md`  
Executor handoff: `handoffs/T056-executor-handoff.json`  
Telemetry: `handoffs/T056-read-only-child-telemetry.json`  
Submitted HEAD: `7c5b7d1c637edcbbe2923550ab3576071d87b13e`  
Evidence PR: `#284`  
Integrated evidence commit: `855a9d1f8bc9778477718e1961a036fd6d9da952`  
Date: 2026-09-05  
Reviewer: ChatGPT Orchestrator

## Verdict

T056 is **accepted as an execution** with frozen qualification decision:

```text
PARTIAL_OBSERVABILITY
```

The Executor respected the frozen fail-closed rules. The run reached the corrected Codex 0.153.4 surface and established several positive facts, but the temporary controller terminated before the exact child could be reattached while its real parent remained continuously loaded. The mandatory child permission, usage, duration and reroute receipts were therefore unavailable.

This is not evidence that the 0.153.4 child permission surface fails. It is evidence that T056's first controller implementation failed to complete the measurement sequence.

## Repository boundary

The submitted branch was exactly one commit ahead of the frozen base and changed only:

```text
handoffs/T056-executor-handoff.json
handoffs/T056-read-only-child-telemetry.json
```

No product code, Markdown, dependency, global Codex configuration, D055, routing policy, Governance Core or persistent agent catalog changed.

## Accepted positive evidence

The following passed on installed Codex/App Server 0.153.4:

1. D042/RB001 freshness and isolated evidence baseline.
2. Human-visible root remained Codex / NEW / GPT-5.6 Sol / Medium.
3. Installed CLI and initialized App Server were exactly 0.153.4.
4. Native schema/help exposed every frozen mandatory capability.
5. Parent started with `permissions=":read-only"`, omitted legacy sandbox input, and returned:

```text
activePermissionProfile.id = ":read-only"
legacy sandbox = readOnly
```

6. Exactly one real Multi-Agent V2 child was uniquely correlated to the parent and expected task path.
7. Requested/resolved child compute state matched:

```text
requested: gpt-5.6-terra / low
resolved:  gpt-5.6-terra / low
```

8. Backend-served identity was not overstated.
9. No tracked mutation occurred and neither parent nor child attempted a write.

## Failure mechanism

The temporary controller treated `thread/loaded/list` output as thread objects when this installed surface returned thread ID strings. The controller then terminated the App Server before owner-controlled child reattachment.

Consequences:

```text
continuous parent residency: FAIL
child activePermissionProfile receipt: unavailable
child legacy sandbox projection: unavailable
exact child-turn token usage: unavailable
exact child-turn duration: unavailable
exact-child reroute receipt: unavailable
```

The Executor correctly did not infer missing data, inspect private persistence to upgrade the result, launch a second provider-backed probe, or alter global configuration.

## Acceptance criteria

```text
AC-T056-1   PASS
AC-T056-2   PASS
AC-T056-3   PASS
AC-T056-4   PASS
AC-T056-5   FAIL_PARENT_RESIDENCY
AC-T056-6   FAIL_UNAVAILABLE
AC-T056-7   PASS_CONFIGURED_THREAD_STATE_ONLY
AC-T056-8   FAIL_UNAVAILABLE
AC-T056-9   FAIL_UNAVAILABLE
AC-T056-10  PASS
AC-T056-11  PASS_PARTIAL_OBSERVABILITY
```

## Causal interpretation

T056 materially improves the evidence boundary beyond T055:

- the 0.153.4 host and capability surface are confirmed;
- the parent `:read-only` profile-ID provenance path works;
- real child correlation and requested/resolved compute receipts work on the same run.

However, T056 does **not** establish whether the exact child returns `activePermissionProfile.id=":read-only"` under owner-controlled reattachment. That remains the decisive unresolved gate.

The missing gate is attributable to the controller sequence, not to an observed contradictory child permission receipt.

## Successor requirement

A successor qualification may be specified because the failure is bounded and reproducible without changing the research question or vendor-surface hypothesis.

The successor must:

1. use the same normal installed Codex/App Server 0.153.4+ surface;
2. keep the root Sol / Medium;
3. use the same parent `permissions=":read-only"` design;
4. create exactly one parent and one real child;
5. parse `thread/loaded/list` according to the installed native schema and accept the documented/string-ID shape;
6. keep the parent App Server process alive through child reattachment;
7. capture the exact child permission, usage, duration and reroute receipts;
8. remain one provider-backed attempt under its own new Task Contract;
9. preserve all T056 safety and no-inference boundaries.

Do not rerun T056 itself. The new attempt must have a new Task ID and evidence files.

## Research disposition

R009 remains:

```text
Research-State: COMPLETE
Decision-State: EVALUATING
```

because the empirical question remains live under a bounded successor qualification. R008 and R007 remain deferred. No global policy decision is adopted.
