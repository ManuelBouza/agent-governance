# T063-R17 — V8 Stage 5 Readiness Review

Status: ACCEPTED / STAGE6_AUTHORIZED_AWAITING_HUMAN_START  
Date: 2026-09-12  
Task: `docs/tasks/T063-adaptive-worker-routing-requalification.md`  
Design authority: `docs/reviews/T063-R16.md`  
Prior convergence: `docs/reviews/T063-R15.md`  
Persistence research: `docs/research/R024-T063-V7-NO-ROLLOUT-REATTACH-RACE.md`

## Candidate reviewed

```text
branch: test/t063-adaptive-worker-routing-requalification-v8
HEAD:   83f38bd9813cfdd107486ad40d39df6335513ce8
base:   621e9ae0d73378b1013699e8726b315128c95891
```

Remote verification establishes that the candidate is exactly one commit above the post-R16 protected-base snapshot.

The candidate adds exactly 28 executable/test files relative to that base:

- 24 accepted v3-v7 package/test blobs promoted from the accepted v7 implementation state `5a1769fed3f93d86cbc2e72a6cc89d7a276089d6`;
- four v8 artifacts: `evals/adaptive_worker_routing_v8/__init__.py`, `__main__.py`, `runner.py`, and `tests/test_t063_adaptive_worker_routing_v8_reattach.py`.

No v7 terminal telemetry/handoff JSON is promoted into the v8 candidate.

## Preserved executable lineage

The v3-v7 executable/test substrate was materialized using the exact accepted blob identities from the v7 implementation head, including the two Stage 6 mechanics-preserving v7 repairs accepted by T063-R15.

V8 therefore changes no inherited probe, oracle, parent-surface, quality, replication, aggregation, efficiency or pilot-decision implementation. The only new material layer is the R16/R024 persistence adapter and its regression test.

## V8 materialized behavior

The v8 adapter:

1. installs the accepted v7 stack first;
2. substitutes only the App Server client used for same-child `thread/resume` persistence reattachment plus v8 evidence identity/receipt metadata;
3. preserves the accepted `EMPTY_ROLLOUT` classifier from v4;
4. recognizes `ROLLOUT_NOT_FOUND_FOR_EXACT_CHILD` only when the public `thread/resume failed:` error contains exactly one `no rollout found for thread id <id>` occurrence and `<id>` equals the exact child being resumed;
5. uses one shared `RESUME_MAX_ATTEMPTS` budget across both transient classes;
6. sleeps 0.2 seconds and rechecks exact parent loaded residency immediately before each retry;
7. invokes the base App Server `thread/resume` again with the same params object and same child id;
8. creates no replacement child, replayed parent turn or new provider/model turn;
9. fails immediately on wrong/missing child id or unrelated error;
10. records ordered transient classes and the shared-budget/residency/same-child/no-new-provider receipt.

The v8 evidence layer also adds the v7 terminal HEAD only as historical evidence provenance; no v7 observation enters v8 scoring.

## V8 conformance assets

The new Orchestrator-authored regression file covers:

- exact-child no-rollout classification;
- rejection of wrong or missing child id;
- preservation of the exact empty-rollout class;
- mixed `ROLLOUT_NOT_FOUND_FOR_EXACT_CHILD -> EMPTY_ROLLOUT -> success` using one shared attempt sequence;
- common-budget exhaustion across alternating transient classes;
- `resume -> sleep -> parent-residency check -> retry` ordering;
- exact same child and same params object across retries;
- `new_provider_turn_created=false` receipt semantics;
- parent-residency loss before retry;
- immediate failure for an unrelated resume error;
- v8 branch/evidence identity and historical boundary.

Inherited v3-v7 conformance tests remain mandatory Stage 6 gates.

## Orchestrator verification performed

Completed without provider/model calls:

```text
candidate/base ancestry:        VERIFIED REMOTELY
candidate commit count:         1
candidate delta boundary:       VERIFIED REMOTELY — 28 files
accepted v3-v7 blob reuse:      VERIFIED BY EXACT BLOB IDENTITIES
v7 terminal JSON excluded:      VERIFIED BY REMOTE DELTA
v8 runner/source inspection:    PASS — consistent with R16/R024 design
v8 regression-source inspection: PASS — required cases represented
GitHub commit CI/statuses:       none reported
provider/model calls:           0
```

This review does **not** claim repository-native pytest, Ruff or compileall success. No automatic CI status exists for this candidate and the Orchestrator environment has not executed those repository-native gates.

The Task Contract therefore requires Stage 6 on native Windows to run every deterministic/provider-free test, compile, lint, oracle and environment gate successfully **before the first provider/model call**. Any such gate failure blocks with zero scored provider calls.

## D076 review

The two-class persistence controller and its regression suite are material task-specific executable behavior and were correctly materialized by ChatGPT Orchestrator in Stage 5.

Stage 6 may diagnose and make only bounded represented mechanics-preserving repairs. Any change to the two retry classes, exact-child id matching, shared ten-attempt budget, retry delay/residency ordering, experiment schedule/profiles/oracles, quality semantics, thresholds or pilot-decision taxonomy requires Orchestrator re-entry.

## D077 readiness review

Official Codex release state was revalidated after v8 candidate publication on 2026-09-12:

```text
qualified pin:           0.153.4
latest stable:           0.154.0
latest stable tag:       rust-v0.154.0
newer stable observed:   none
version disposition:     PIN_RETAINED
```

R024 already established that 0.154.0 retains the relevant `no rollout found for thread id ...` persisted-read path, so the current stable is not a demonstrated blocker fix and D063 qualification is not extended to it.

The experiment therefore retains exactly Codex/App Server `0.153.4`.

If a stable release newer than `0.154.0` appears before the first v8 provider-backed call, Stage 6 MUST stop for D077 Orchestrator relevance classification.

## Readiness decision

T063 v8 Stage 5 is complete. The exact candidate above is authorized for Human-mediated Stage 6, subject to all Task Contract pre-provider gates.

V8 remains one clean homogeneous 24-arm run. V1-v7 observations are excluded from scoring. A valid `WORKER_QUALITY` FAIL remains execution-valid quality evidence and does not stop the frozen schedule; a non-quality validity blocker still stops fail-closed without compensating scored calls.

## Launch continuity

V7 `root-7` is retired after consumed execution and successor redesign.

V8 uses a NEW same-work-unit successor root:

```text
Executor:       Codex
Surface:        Codex Desktop / native Windows
Session:        NEW
Coordinator-ID: AG | agent-governance | T063 | root-8
Root model:     gpt-5.6-sol
Root reasoning: medium
Runtime:        exactly 0.153.4
App Server:     exactly 0.153.4
Auth:           chatgpt
```

The launch profile is separate from the thin Human transport prompt.