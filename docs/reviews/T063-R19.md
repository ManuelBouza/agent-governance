# T063-R19 — Hardened V8 Stage 5 Readiness Review

Status: ACCEPTED / STAGE6_AUTHORIZED_AWAITING_HUMAN_START  
Date: 2026-09-12  
Task: `docs/tasks/T063-adaptive-worker-routing-requalification.md`  
Design authority: `docs/reviews/T063-R16.md`  
Readiness revocation: `docs/reviews/T063-R18.md`  
Classifier research: `docs/research/R025-T063-V8-REATTACH-CLASSIFIER-HARDENING.md`

## Candidate reviewed

```text
branch: test/t063-adaptive-worker-routing-requalification-v8
HEAD:   afdae0050226d61a10269f63017e2fac99eef644
base:   8b3cc5af3e70367eff6e55ebd416a554c8096763
```

Remote comparison establishes exactly one candidate commit above the protected O276 base.

The candidate adds exactly 28 executable/test files:

```text
24 accepted v3-v7 package/test blobs
+ evals/adaptive_worker_routing_v8/__init__.py
+ evals/adaptive_worker_routing_v8/__main__.py
+ evals/adaptive_worker_routing_v8/runner.py
+ tests/test_t063_adaptive_worker_routing_v8_reattach.py
```

No historical v7/v8 terminal telemetry or handoff JSON is promoted.

## Withdrawn predecessor boundary

The earlier Stage 5 candidate:

```text
83f38bd9813cfdd107486ad40d39df6335513ce8
```

remains withdrawn by R18. It is not launch authority and is not to be executed.

The new candidate is rebuilt from the post-revocation protected base rather than extending the withdrawn candidate history.

## R025 hardening materialized

The new v8 adapter retains the accepted R022 `EMPTY_ROLLOUT` classifier and replaces the withdrawn textual/regex no-rollout classifier with safe structural parsing of the flattened v3 `AppServerError` representation.

`ROLLOUT_NOT_FOUND_FOR_EXACT_CHILD` now requires all of:

```text
exact method prefix: "thread/resume failed: "
safe parse:          ast.literal_eval
parsed value:        mapping
error.code:          type exactly int and value exactly -32600
error.message:       exactly "no rollout found for thread id <exact child id>"
```

Any wrong code, `bool` masquerading as integer, message prefix/suffix drift, malformed payload, non-mapping payload, wrong/missing child ID, unrelated `-32600` error or wrong method prefix fails classification and is not retried.

`ast.literal_eval` is a safe literal parser for this represented Python mapping and does not execute arbitrary expressions.

## Preserved persistence invariants

The adapter preserves one common `RESUME_MAX_ATTEMPTS=10` budget across:

```text
EMPTY_ROLLOUT
ROLLOUT_NOT_FOUND_FOR_EXACT_CHILD
```

For each retry:

```text
wait 0.2 seconds
-> exact parent loaded-residency check
-> identical thread/resume params object
-> same exact child id
```

It creates no replacement child, parent-turn replay or provider/model quality turn. Parent loss, exhaustion or nonmatching error fails closed.

The evidence receipt records ordered observed availability conditions, shared budget, retry count, residency checks, same-child reuse and `new_provider_turn_created=false`.

## Preserved scientific implementation

The v3-v7 executable/test substrate is promoted by exact accepted blob identities from v7 implementation HEAD `5a1769fed3f93d86cbc2e72a6cc89d7a276089d6`.

Therefore the hardened v8 layer changes no accepted:

- P1/P2/P3 oracle semantics;
- v6 public live parent-surface semantics;
- requested/resolved profile and D063 receipt semantics;
- 4-block / 24-arm v7 replicated schedule;
- valid quality-failure continuation;
- 4/4 per-probe and 12/12 global qualification rules;
- quality-adjusted usage aggregation;
- 10% exact-token materiality threshold;
- four-value pilot-decision taxonomy.

V1-v7 observations remain historical only and cannot enter v8 scoring.

## Conformance coverage

The Orchestrator-authored v8 regression suite now covers:

- exact `-32600` + exact canonical message + exact child positive classification;
- wrong child rejection;
- wrong JSON-RPC code rejection;
- `bool` code rejection despite Python `bool` subclassing `int`;
- exact-code message prefix drift rejection;
- exact-code message suffix drift rejection;
- malformed mapping rejection;
- non-mapping literal rejection;
- unrelated `-32600` invalid-request rejection;
- wrong RPC method prefix rejection;
- arbitrary-expression text rejected by safe literal parsing;
- accepted empty-rollout classification preservation;
- mixed no-rollout -> empty-rollout -> success under one shared budget;
- exact same params object and child across retries;
- retry ordering `resume -> sleep -> loaded-parent check -> retry`;
- common-budget exhaustion across alternating conditions;
- parent-loss fail-closed behavior;
- immediate unrelated-error failure;
- v8 branch/evidence/historical identity.

## Orchestrator verification performed

Completed without provider/model calls:

```text
candidate/base ancestry:             VERIFIED REMOTELY
candidate commit count:              1
candidate delta boundary:            VERIFIED — 28 files
accepted v3-v7 blob promotion:       VERIFIED BY EXACT BLOB IDS USED IN TREE
hardened v8 runner remote blob:      bf5e5dc012b9bf81906b31177ed92c5e319d6858
hardened v8 test remote blob:        b9851ca572286dce8d137c1fdf5b8bb0dd7dbfe2
new runner AST parse:                PASS
new test AST parse:                  PASS
structured-classifier smoke matrix:  PASS
GitHub automatic CI statuses:        none
provider/model calls:                0
```

A local full checkout attempt failed because the Orchestrator container could not resolve `github.com`. Consequently this review does **not** claim repository-native pytest, Ruff or compileall PASS.

Those checks remain mandatory Stage 6 pre-provider gates on native Windows. A failure must block with zero new scored provider calls.

## D076 review

The R025 structured classifier and negative conformance suite are material task-specific controller behavior and were materialized in Stage 5 by ChatGPT Orchestrator.

Stage 6 may make only bounded represented mechanics-preserving repairs. Any change to parser acceptance shape, retry conditions, attempt budget, retry ordering, experiment schedule/profiles/oracles, quality rules, thresholds or pilot taxonomy requires Orchestrator re-entry.

## D077 readiness review

Official GitHub release state was revalidated after hardened candidate publication:

```text
qualified pin:           0.153.4
latest stable:           0.154.0
latest stable tag:       rust-v0.154.0
published:               2026-09-09
newer stable observed:   none
version disposition:     PIN_RETAINED
```

R025 confirms reviewed later source retains the relevant rollout-resolution architecture and does not establish a blocker fix. D063 remains qualified specifically on 0.153.4, so v8 retains exactly Codex/App Server `0.153.4`.

If a stable release newer than `0.154.0` appears before the first provider-backed call, Stage 6 MUST stop for D077 relevance classification.

## Readiness decision

Stage 5 is complete for hardened v8 candidate `afdae005...`.

This exact candidate is authorized for Human-mediated Stage 6 subject to all Task Contract pre-provider gates.

No launch authorization survives for any other candidate HEAD, including withdrawn `83f38bd...`.

## Launch continuity

`AG | agent-governance | T063 | root-8` was reserved but never started, so it remains the NEW same-work-unit coordinator identity.

```text
Executor:       Codex
Surface:        Codex Desktop / native Windows
Session:        NEW
Coordinator-ID: AG | agent-governance | T063 | root-8
Root model:     gpt-5.6-sol
Root reasoning: medium
Runtime:        exactly 0.153.4
App Server:     exactly 0.153.4
Auth:           chatgpt
```
