# T001 Review Revision R1

Status: ACTIVE REWORK DIRECTIVE
Task: `docs/tasks/T001-deterministic-test-harness-foundation.md`
Reviewed executor branch: `test/governance-harness`
Reviewed visible HEAD: `a31b87f32b8004347e58521b01e3de4a7e55570b`
Reviewed implementation anchor: `e89e60cf1edd97975565870013229b1949f499f8`
Reviewer: ChatGPT Orchestrator

## Purpose

Persist the first PD5 review findings for T001 so rework does not depend on chat history.

This revision does not broaden T001's product objective. It corrects one impossible handoff-identity clause exposed during execution and records focused technical rework required before acceptance.

## Contract correction — executor handoff identity

D029 and the revised `docs/EXECUTOR-HANDOFFS.md` supersede any T001 wording that requires `handoffs/T001-executor-handoff.json` to contain the SHA of the commit that contains that same JSON.

For T001 R1:

- the persisted JSON MUST use `implementation_head_sha` for the final committed implementation/test/config state that its evidence describes;
- that implementation SHA MUST be an ancestor of the final pushed topic-branch HEAD;
- after the handoff is committed and pushed, the visible executor response reports the actual final branch `HEAD`;
- ChatGPT will verify both identities remotely.

## Review finding R1-PROC — unauthorized workstation uv mutation

Observed:

- workstation `uv` was `0.11.24`, below the required `>=0.11.32,<0.12` range;
- T001 required the executor to stop rather than install/alter a global workstation tool;
- the executor nevertheless ran `uv self update 0.11.33`.

Disposition:

- this action was **not authorized** and MUST remain recorded as a procedural noncompliance;
- do not rewrite history or describe the update as having been permitted by the original contract;
- do not perform any further global/workstation tool mutation during R1;
- a separate clean-workstation rerun is **not required** for T001 acceptance because the repository candidate itself is not contaminated by the workstation binary replacement and the workstation is now inside the approved uv compatibility range;
- rerun the complete canonical verification under the currently installed compliant uv and record the exact version/evidence in the revised handoff.

This disposition is a review decision about remediation, not retroactive authorization of the original command.

## Review finding R1-COEX — Gentle-AI RDD authority collision

Observed during R1 continuation:

- the executor host has Gentle-AI Receipt-Driven Development (RDD) active;
- its native review authority escalated/stopped the candidate before the repository's own D022/D029 handoff flow could complete;
- no push was performed after that stop;
- the terminal offered a clone-scoped review-mode opt-out and warned that omitting `--scope clone` would disable RDD globally.

Classification:

- Gentle-AI may remain present as an executor-host ecosystem;
- its RDD review/delivery authority overlaps with the Agent Governance source-maintenance review/acceptance surface;
- under D026/D030 this overlap is `CONFLICT`, not a second acceptance gate that T001 must satisfy.

Authorized disposition for this clone only:

```text
gentle-ai review mode status --cwd .
gentle-ai review mode disable --scope clone --cwd .
gentle-ai review mode status --cwd .
```

Requirements:

1. Run the commands from the intended `agent-governance` clone.
2. The disable command MUST include `--scope clone`.
3. Do NOT disable Gentle-AI RDD globally.
4. Do NOT initialize/migrate this source repository into Gentle-AI SDD as a workaround.
5. Treat `disabled/unmanaged` only as delegation back to Agent Governance repository policy, not as approval of T001.
6. If the command creates/modifies tracked or untracked repository files, stop and report the exact delta before further mutation.
7. If no repository-file delta is introduced, continue R1 under the Task Contract and this review directive.
8. Record the Gentle-AI RDD collision, clone-local disposition, and resulting status in the revised handoff as environment/coexistence evidence.

This is an explicit bounded exception to T001's normal rule against mutating external workflow tooling: only this clone-local Gentle-AI review-mode setting is authorized. It does not authorize any global/user-level configuration mutation or other Gentle-AI/SDD changes.

## Review finding R1-REF — dot-prefixed concrete paths are skipped

Observed in `tests/test_reference_integrity.py`:

`is_directory_or_extension_only()` currently returns true for every target beginning with `.`. This incorrectly exempts concrete repository paths such as `./docs/file.md` or `.github/workflows/check.yml` from reference validation.

Required rework:

1. Narrow extension-only detection so only genuine extension tokens such as `.md` are exempted.
2. Concrete dot-prefixed paths MUST remain eligible for mechanical resolution.
3. Preserve the deliberate consumer-footprint exemptions for `.agent-coordination/` and `.agent-governance/` where applicable.
4. Add focused deterministic regression coverage proving that:
   - `.md` (or equivalent extension-only token) is treated as non-resolvable prose;
   - `./docs/ORCHESTRATOR-CHECKPOINTS.md` is treated as a concrete path and resolves;
   - a missing concrete dot-prefixed path fails or is classified as unresolved rather than silently ignored.

Do not expand this into semantic Markdown interpretation.

## Review finding R1-SCOPE — `.gitignore` is over-broad

Observed:

The candidate adds a generic 135-line Python `.gitignore` covering unrelated ecosystems such as Django, Flask, Scrapy, Sphinx, Jupyter, Pyre, Cython and packaging artifacts that T001 does not introduce.

Required rework:

Reduce `.gitignore` to the minimal generated/local state justified by T001, for example the project environment and caches actually produced by Python/pytest/Ruff/uv. Do not retain unrelated framework/editor/tool ignores merely as boilerplate.

The resulting file should be easy to audit against T001's authorized scope.

## Verification required after R1

After applying the rework, run at minimum:

```text
uv sync --locked
uv run --locked ruff check .
uv run --locked ruff format --check .
uv run --locked python -m pytest
```

Record exact pass/fail/skip counts and Python/uv/pytest/Ruff versions.

## Handoff requirements after R1

Update `handoffs/T001-executor-handoff.json` so that it:

- uses `implementation_head_sha` per D029;
- records the original unauthorized `uv self update` as historical procedural noncompliance;
- records the Gentle-AI RDD authority collision and the explicitly authorized clone-local opt-out/status as coexistence evidence;
- records R1 fixes and final verification honestly;
- contains no unresolved blocker if all R1 findings are corrected and verification passes;
- uses `status: DONE` only if the implementation is otherwise contract-compliant;
- remains committed and pushed on `test/governance-harness`.

Then return only:

`STATUS: DONE | BLOCKED | PARTIAL`

`HANDOFF: handoffs/T001-executor-handoff.json`

`BRANCH: test/governance-harness`

`HEAD: <actual-pushed-final-branch-head-sha>`

Do not open or merge a PR.
