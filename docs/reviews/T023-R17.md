# T023 Review R17 — T061 MG1-v14 Stage 5 Re-entry Authority

Status: `STAGE5_REENTRY_AUTHORIZED / CANDIDATE_FREEZE_PENDING`

Date: 2026-09-07

Owner: ChatGPT Orchestrator

Controlling work unit: `T061`

Prior convergence review: `docs/reviews/T023-R16.md`

## Purpose

R16 accepted the terminal Stage 6 v13 blocker as a Stage 5 candidate-materialization defect: the frozen v4 `G3/source-maintainer/SKILL.md` was not byte-identical to its declared v3 G3 source even though T061-P3 requires exact copy equivalence.

This review authorizes D068 Stage 5 re-entry under Orchestrator ownership. It does not reopen or rewrite historical v13 evidence, does not authorize an Executor, and does not authorize provider/model calls.

## Historical evidence boundary

The following remain immutable historical evidence:

- v13 topic branch terminal HEAD: `d0ebe46a68c02c66dcfbb21c3dfaee43fb15c27f`;
- v13 Stage 5 reviewed HEAD: `f893a03d17596182db209322f5357cda75ea3781`;
- Candidate Freeze A: `a454091aff7bb932372a6057e2d9804f94e66320`;
- Holdout/oracle Freeze B: `7b990f4d60ba7ca0dfafe1b95785e007f8697c28`;
- terminal Executor handoff: `handoffs/T061-executor-handoff.json` at `d0ebe46a...`;
- historical corpus `MG1-T023-CORPUS-v7`, oracle `MG1-T023-TOPOLOGY-ORACLE-v13`, execution epoch `MG1-T023-EXECUTION-v13`, and presentation revision `MG1-T023-PRESENTATIONS-v4`.

No reset, rebase, amend, force-push, relabeling, or mutation of those historical identities is permitted as a repair mechanism.

## Accepted defect classification

The controlling T061 semantic requirement is unchanged:

- F2 v14 projection SHALL remain byte-identical to the corresponding v3 F2 Skill files;
- G3 v14 projection SHALL remain byte-identical to the corresponding v3 G3 Skill files;
- shared references SHALL remain byte-identical to the corresponding v3 shared references;
- B2 semantic bytes remain the approved Positive-Anchor Single Router bytes.

The specific correction is therefore materialization-only: the new prospective G3 `source-maintainer` projection must use the canonical v3 G3 source blob `e00edee8b1a1bacbb47b01bad4492f8ddf954628`, not the historical erroneous v4 blob `13f064d186af528557d1e389e271aaa684122eb1`.

## Prospective identity set

Because the correction occurs after Freeze B exposure, the corrected experiment SHALL use fresh prospective identities:

```text
Task: T061
MG1/T023 evaluation: v14
Reference candidate: B2
Challengers: F2, G3
Historical unscheduled candidates: B0, B1
Capability source epoch: MG1-2026-09-06-v4 (semantic meaning preserved)
Topology metadata revision: MG1-T023-TOPOLOGIES-v4
Presentation revision: MG1-T023-PRESENTATIONS-v5
Candidate hash manifest: MG1-T061-CANDIDATE-HASHES-v2
Corpus: MG1-T023-CORPUS-v8
Oracle: MG1-T023-TOPOLOGY-ORACLE-v14
Execution epoch: MG1-T023-EXECUTION-v14
Trial envelope: MG1-T023-TRIAL-ENVELOPE-v2 (preserved)
Required live cell: Codex / native Windows / GPT-5.6 Sol / Medium
Codex CLI baseline: 0.149.0
Prior observations allowed in v14 score: 0
```

Prospective topic branch:

`test/t023-skill-activation-topology-evals-v14`

The branch is based on current canonical `develop` at `f0811365698667bae9b0ab77a9300c8c9aa988b7`.

## Stage 5 freeze sequence

### Freeze C — corrected candidate freeze

Before any exact v14 acceptance prompt is authored, the Orchestrator SHALL materialize and remotely freeze:

- capability-source v4 projection on the current `develop` lineage;
- topology metadata revision v4 targeting `presentations-v5`;
- presentation manifest revision v5;
- B2 exact approved bytes;
- F2 exact v3 copies;
- G3 exact v3 copies, including corrected `source-maintainer` bytes;
- shared exact v3 copies;
- `candidate-hashes-v14.json` with correct SHA-256 and copy-equivalence provenance;
- generic harness/provenance code required to understand the v14 identities, provided it contains no exact v14 holdout prompt objects;
- deterministic candidate-integrity verification for the new projection.

Freeze C must be pushed and independently reconstructable from Git before the exact v14 holdout is authored.

### Freeze D — fresh holdout/oracle freeze

Only after Freeze C is remotely verified may the Orchestrator author and freeze:

- fresh corpus v8 with the same accepted semantic geometry as v13 but zero exact v13/V12 prompt reuse;
- oracle v14 preserving accepted thresholds, B2-first scheduling, challenger materiality and paired 2+1 rules;
- execution identity v14;
- v14 holdout-integrity assertions and holdout-specific harness validation.

The required corpus geometry remains:

- positive-consumer: 6;
- positive-source-maintainer: 6;
- positive-external-skill-trust: 6;
- negative: 10;
- near-miss: 30, balanced 6 cases on each of the 5 accepted axes;
- ambiguous: 4;
- cross-profile: 4;
- multi-intent: 4;
- total: 70;
- false-activation denominator: 40 (`negative + near-miss`).

Freeze C -> Freeze D must contain zero change to candidate/reference bytes, candidate hashes, topology/presentation semantics, or capability-source semantics.

## Preserved acceptance semantics

v14 preserves the T061 thresholds and selection semantics without relaxation:

- activation precision/recall/F1 `>= 0.95`;
- false activation rate `<= 0.05`;
- wrong-specialist rate `<= 0.05`;
- overactivation rate `<= 0.05`;
- overall semantic outcome accuracy `>= 0.95`;
- zero critical cross-profile violations;
- zero critical ambiguous-context permission broadening;
- deterministic/profile-isolation/source-independence/source-distribution/single-install non-regression required;
- B2 executes first and must qualify before F2/G3 may be scheduled;
- challenger materiality/tie-break rules remain unchanged from T061;
- paired 2+1 aggregation and critical any-occurrence gates remain unchanged.

No v13 observation may enter v14 scoring.

## Stage 5 execution boundary

- Provider/model calls during Stage 5: exactly `0`.
- No Codex Executor is authorized during this re-entry.
- ChatGPT Orchestrator owns candidate materialization and semantic conformance assets under D068/D052.
- Static/hash/provenance checks may be performed by the Orchestrator; runtime acceptance remains Stage 6 Executor work.
- A material Specify/Design/Plan defect discovered during re-entry requires another persisted re-entry decision rather than silent semantic tuning.

## Completion condition

Stage 5 re-entry is complete only when Freeze C and Freeze D are both remotely published and verified, the corrected candidate remains unchanged across the fresh holdout boundary, v14 static/integrity checks establish coherent materialization, and a later Orchestrator review explicitly marks T061 technically ready for a renewed Stage 6.

Technical readiness does not itself authorize Stage 6. A separate Human-selected launch objective remains required.
