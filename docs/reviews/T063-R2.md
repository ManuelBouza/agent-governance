# T063-R2 — Launch Authority and Frozen Evaluation Inputs

Status: `STAGE6_AUTHORIZED / AWAITING_HUMAN_CODEX_START`  
Date: 2026-09-09  
Authority: Human Owner / ChatGPT Orchestrator  
Task: `docs/tasks/T063-adaptive-worker-routing-requalification.md`  
Prelaunch correction: `docs/reviews/T063-R1.md`  
Research: `R007`, `R018`  
Measurement authority: `docs/decisions/D063-qualified-codex-read-only-child-measurement-surface.md`  
Routing authority: `docs/decisions/D075-coordinator-direct-execution-gate.md`  
Provider/model calls before launch: `0`

## Human launch selection

After O259 explicitly presented `launch T063` versus `resume T062`, the Human selected T063 (`correcto T063`).

This authorizes preparation and Human-mediated launch of T063 only.

It does **not** resume T062. T023-R33 remains the controlling Human Hold over T062 and R32 remains unconsumed.

## D055 / D058 launch profile

Freeze the Human-visible launch profile as:

```text
Executor:        Codex
Surface:         Codex Local / native Windows
Session:         NEW
Coordinator-ID:  AG | agent-governance | T063 | root-1
Root model:      gpt-5.6-sol
Root reasoning:  medium
Codex CLI:       exactly 0.153.4
App Server:      exactly 0.153.4
Auth category:   chatgpt
```

Rationale: T063 is a new ASSURED experimental work unit; Sol/Medium preserves the T054 control family while exact stable Codex/App Server 0.153.4 is the D063-qualified reference runtime and removes an unnecessary runtime confound.

The scored child matrix is frozen as:

| Probe | Arm | Requested model | Requested reasoning |
| --- | --- | --- | --- |
| P1 | CONTROL | `gpt-5.6-sol` | `medium` |
| P1 | ADAPTIVE | `gpt-5.6-luna` | `medium` |
| P2 | CONTROL | `gpt-5.6-sol` | `medium` |
| P2 | ADAPTIVE | `gpt-5.6-terra` | `medium` |
| P3 | CONTROL | `gpt-5.6-sol` | `medium` |
| P3 | ADAPTIVE | `gpt-5.6-terra` | `high` |

No model or effort substitution is authorized after launch. A missing model, effort, override field, or requested-to-resolved mismatch is `BLOCKED_PROFILE_RESOLUTION` before the affected scored turn.

Official availability was revalidated immediately before this freeze: the current stable Codex release remains 0.153.4 and current OpenAI model documentation exposes Sol, Terra and Luna with the required reasoning levels. Account/session-specific availability cannot be asserted by ChatGPT under D071 and therefore remains a mandatory live Executor preflight.

## Frozen repository baseline

The evaluation input baseline is the pre-launch-authority protected `develop` HEAD:

```text
69e910f329a2294c3b40df0f6ee983f9905f4677
```

All P1/P2 source answers and the P3 seed source are derived from that exact commit.

Expected evidence branch:

```text
test/t063-adaptive-worker-routing-requalification
```

The branch does not exist at authorization time. The Executor must create it from exactly `69e910f329a2294c3b40df0f6ee983f9905f4677` after safe synchronization and must use an exclusive T063 worktree. Do not merge, rebase or cherry-pick later `develop` Markdown into the evidence branch merely to read launch authority.

Read T063-R2 and the current checkpoint directly from `origin/develop` while leaving the frozen evaluation baseline unchanged.

## D075 topology classification

T063-R1 remains controlling:

```text
execution_target: CONTRACT_FIXED
underlying_delegation_eligibility: DELEGATED
```

The exact CONTROL/ADAPTIVE topology is experimental authority, not a free routing choice by the Coordinator. Coordinator-direct actions are limited to D075-conforming auxiliary microactions needed for synchronization, identity checks, oracle computation, evidence capture and next-action selection.

## Live fail-closed preflight

Before any scored child turn, the Executor must prove all of the following on the actual native host/account/session:

1. safe Git synchronization without discarding or overwriting unrepresented local work;
2. current `origin/develop` contains T063-R1, T063-R2 and the current launch checkpoint;
3. frozen evaluation commit `69e910...` exists unchanged;
4. evidence branch/worktree is exclusive and based exactly on `69e910...`;
5. native Windows Codex CLI is exactly `0.153.4`;
6. App Server initialize/native schema report exactly `0.153.4`;
7. authentication category is `chatgpt`;
8. the installed native schema exposes the D063-required parent/child relationship, loaded-parent residency, `activePermissionProfile`, requested/resolved model and reasoning, exact non-estimated child-turn usage, exact child-turn timing and exact-child reroute observation surfaces;
9. a D063 measurement parent can be started with `activePermissionProfile.id == ":read-only"`, approval `never`, and no contradictory broader legacy projection;
10. child model/reasoning override/configuration is available for every frozen requested profile;
11. tracked product state is clean before scored execution.

The outer T063 Coordinator may retain only the minimum write authority needed to create/persist the authorized JSON evidence and Git commit. The measured App Server parent and every scored child remain read-only as required by D063/T063.

If D063 cannot be revalidated, return `BLOCKED_MEASUREMENT_SURFACE`; do not infer or estimate mandatory receipts.

## Provider-use boundary

The Human selected T063 for execution. The authorized external provider destination for scored model work is OpenAI Codex under the already authenticated account/session only.

Authorized payload is limited to the frozen T063 probe messages and the repository/ephemeral-fixture content deliberately exposed to the measured children under this review and the Task Contract. Do not transmit credentials, secrets, unrelated repository material or a broader workspace payload merely because it is readable.

Ordinary provider usage required by the authorized T063 probes is within scope. No separate purchase, subscription/plan change, credential change or new billing arrangement is authorized. If the host requires additional Human commercial authorization, stop and return `BLOCKED` rather than expanding authority.

## Common child isolation rule

For every scored child:

- use a fresh child/thread;
- do not reuse a child across probes or arms;
- use the frozen requested model/reasoning for that arm;
- require D063 read-only permission/profile evidence;
- minimize inherited context; prefer no parent conversation history when the installed qualified surface supports it;
- give CONTROL and ADAPTIVE arms the exact same frozen task message and input within a probe;
- do not reveal the other arm's result;
- do not provide T063 oracle values;
- do not instruct children to read `docs/tasks/T063-*`, `docs/reviews/T063-*`, R007, R018 or D063;
- explicitly tell the child not to inspect task/review/oracle authority outside the bounded input;
- if a child nevertheless reads controlling/oracle material that reveals the expected answer or P3 seed defect, treat that probe as execution-invalid and stop without computing a pilot decision.

Persist task-message digests and enough tool/evidence metadata to demonstrate matched-arm equality and absence of oracle leakage without persisting private chain-of-thought or full transcripts.

## P1 frozen input — exact Git evidence inventory

Frozen HEAD:

```text
69e910f329a2294c3b40df0f6ee983f9905f4677
```

Frozen ordered paths:

```text
.python-version
agent-governance-source.json
pyproject.toml
code-health.json
src/agent_governance/profile.py
src/agent_governance/source_adapter.py
```

The root independently computes the exact Git blob SHA and Git-object byte size for each path from the frozen commit before either arm runs. Those answer values are oracle evidence and must not be included in the child message.

Freeze the P1 child task message semantically and byte-for-byte identically for both arms to require:

```text
At frozen Git commit 69e910f329a2294c3b40df0f6ee983f9905f4677, inspect only the six supplied tracked paths and return, for each path, the exact Git blob SHA, byte size from Git object metadata, and exists_at_frozen_head true/false. Also return the exact frozen HEAD evaluated and concise evidence commands/APIs used. Use Git/object metadata rather than filesystem size or inference. Do not modify repository state and do not inspect T063 task/review/oracle documents.
```

The six paths are appended to that message in the exact order frozen above.

P1 arm order remains:

```text
ADAPTIVE -> CONTROL
```

## P2 frozen input — static dependency/symbol map

Frozen in-scope package slice is exactly these five files at `69e910...`:

```text
src/agent_governance/__init__.py
src/agent_governance/artifact.py
src/agent_governance/engine.py
src/agent_governance/profile.py
src/agent_governance/source_adapter.py
```

Frozen symbol queries, in this order:

```text
ArtifactBuildError
build_artifact
Profile
resolve_profile
SourceContext
_bootstrap
```

`dependency edge` means only a static Python `ast.Import` or `ast.ImportFrom` edge whose resolved source and target are both among the five frozen files. Exclude runtime loading, package bootstrap effects, `sys.modules`, filesystem/package discovery and imports to modules outside this exact file set.

The root independently computes the exact edge set, acyclicity and six owners from the frozen commit before either arm runs. Oracle answers must not be included in the child message.

Freeze the P2 child task message semantically and byte-for-byte identically for both arms to require:

```text
At frozen Git commit 69e910f329a2294c3b40df0f6ee983f9905f4677, analyze only the supplied five Python files. A dependency edge is only an ast.Import or ast.ImportFrom relationship resolving between two files in that exact scope; exclude runtime/package-bootstrap/discovery effects and every external import. Return the sorted exact internal edge set, whether that graph is acyclic, the owning module for each of the six supplied symbols, and concise reproducible evidence. Do not modify repository state and do not inspect T063 task/review/oracle documents.
```

The five paths and six symbols are appended in the exact orders frozen above.

P2 arm order remains:

```text
CONTROL -> ADAPTIVE
```

## P3 frozen input — adversarial independent review

Seed source is exactly:

```text
path: src/agent_governance/profile.py
blob: 1e3ae205b4350adc18a2f3f5d631dd739056ba34
frozen commit: 69e910f329a2294c3b40df0f6ee983f9905f4677
```

Before either P3 arm, the root creates one ephemeral UTF-8 Python fixture outside tracked repository state by copying that exact blob and applying exactly one semantic mutation:

> In `validate_profile`, replace the branch that rejects a `Profile` whose `name` is not one of `ACTIVE_PROFILES` with a return of `Profile(name="source-maintainer")`; preserve all other source behavior and text as closely as mechanically possible.

Frozen material defect/oracle:

```text
unsupported Profile.name fails open to source-maintainer
-> invalid profile receives grants_source_maintenance == true
-> expected original behavior is ProfileError fail-closed rejection
severity: HIGH
mechanism: authority/profile escalation through invalid-name fallback
minimal proof: validate_profile(Profile(name="invalid")) returns source-maintainer rather than raising
minimal fix: restore fail-closed ProfileError for unsupported names
```

The root must independently verify the mutation and reproduction, compute the ephemeral fixture SHA-256, and record only the fixture digest/path metadata in telemetry before either child runs.

The P3 children receive only the ephemeral fixture input plus this frozen task message; they must not receive the seed/oracle above:

```text
Review only the supplied Python fixture for one material correctness or security defect. Return status; material findings with severity; mechanism/evidence; minimal reproduction or proof direction; and minimal fix direction. Do not modify the fixture or repository. Do not inspect repository task, review, research, decision or oracle documents; use only the supplied fixture as substantive input.
```

Use equivalent minimized context for both arms. A child read of T063-R2 or other oracle-bearing authority invalidates the probe.

P3 arm order remains:

```text
ADAPTIVE -> CONTROL
```

After both first attempts complete, delete the ephemeral fixture after capturing the required digest/evidence; do not commit it.

## First-attempt scoring and diagnostics

Preserve T063 exactly:

- every first attempt is immutable scoring evidence;
- no failed first attempt may be replaced by a retry;
- one fresh next-stronger ADAPTIVE child may be used only for diagnosis after a valid adaptive failure;
- CONTROL may be rerun once only for diagnosed host/tool instability;
- diagnostic turns do not rewrite first-attempt scores;
- no indefinite retries;
- no mid-run profile substitution.

Exactly six children/turns constitute the normal scored set. Any diagnostic child must be separately labeled and justified under the Task Contract.

## Evidence

Persist only authorized non-Markdown evidence on the T063 evidence branch:

```text
handoffs/T063-adaptive-worker-routing-telemetry.json
handoffs/T063-executor-handoff.json
```

Telemetry must include the Task Contract fields plus:

```text
execution_target: CONTRACT_FIXED
underlying_delegation_eligibility: DELEGATED
frozen_evaluation_head: 69e910f329a2294c3b40df0f6ee983f9905f4677
launch_authority_review: docs/reviews/T063-R2.md
oracle_leak_observed: true|false
```

Do not create or edit committed Markdown. Do not modify product source, tests, config, schemas, Task Contracts or scientific assets. The only normal committed branch delta from the frozen baseline is the two authorized JSON evidence files.

Before terminal response, verify remote branch HEAD equals the pushed terminal HEAD.

Return only:

```text
STATUS: COMPLETED | BLOCKED
HANDOFF: handoffs/T063-executor-handoff.json
BRANCH: test/t063-adaptive-worker-routing-requalification
HEAD: <actual remote pushed HEAD>
```

## D071 transport state

ChatGPT does not start or control Codex. After this authority and the corresponding checkpoint are integrated into protected `develop`, the state becomes:

```text
AUTHORIZED_AWAITING_HUMAN_CODEX_START
```

The Human must start a NEW Codex session using the frozen D055 profile and paste the complete transport prompt rendered by ChatGPT.
