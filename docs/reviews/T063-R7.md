# T063-R7 — V3 Block Acceptance and V4 Reattach-Barrier Authority

Status: `V3_BLOCKED_ACCEPTED / V4_STAGE5_CANDIDATE_PUBLISHED / V4_STAGE6_AUTHORIZED_AWAITING_HUMAN_CODEX_START`  
Date: 2026-09-11  
Authority: Human Owner / ChatGPT Orchestrator  
Task: `docs/tasks/T063-adaptive-worker-routing-requalification.md`  
Prior authority: `docs/reviews/T063-R6.md`  
V3 repair research: `docs/research/R022-T063-V3-EMPTY-ROLLOUT-REATTACH-RACE.md`  
Measurement authority: `docs/decisions/D063-qualified-codex-read-only-child-measurement-surface.md`  
Version authority: `docs/decisions/D077-version-sensitive-upstream-revalidation.md`  
Materialization authority: `docs/decisions/D068-library-first-candidate-materialization-executor-verification-boundary.md`; `docs/decisions/D076-stage6-ephemeral-executable-materialization-boundary.md`

## Verdict on v3

The T063 v3 terminal return is accepted as a valid **blocked execution record** but not as a scored adaptive-routing result.

Verified terminal state:

```text
branch: test/t063-adaptive-worker-routing-requalification-v3
HEAD:   746519abc6f159e959120f68d5c9f920d88d5797
status: BLOCKED
terminal classification: BLOCKED_EXECUTION_INVALID
pilot_decision: null
```

Provider accounting:

```text
scored parent turns:       1
scored child attempts:     1
valid scored children:     0
diagnostic child attempts: 0
compensating attempts:     0
invalid attempt:            P1 ADAPTIVE
```

The v3 P1 ADAPTIVE attempt is excluded from every future quality, token, duration and pilot calculation.

The failure is not attributed to:

- worker quality;
- requested/resolved profile mismatch;
- Codex Desktop using an unqualified runtime;
- D076 violation;
- Executor noncompliance.

The blocker is an App Server adapter persistence race: public child `Started` activity can precede durability of the first rollout metadata line needed by immediate `thread/resume`.

## Accepted v3 Stage 6 repair

Before the provider-backed attempt, Executor commit:

```text
4a1bc28b83bffecc706df0f2e42aafe29456c54e
```

made represented Ruff/formatting-only repairs to the published v3 harness/tests. Remote review found no change to probes, oracles, messages, profiles, scoring, evidence meaning or receipt semantics.

That repair is accepted as bounded D068/D076 Stage 6 technical repair.

Terminal v3 evidence reports:

```text
ephemeral_artifacts: []
executor_material_ephemeral_artifacts: []
raw response events used: false
```

D076 held.

## Runtime finding

The v3 preflight proved that the effective runtime available through the Human's Codex Desktop installation was exactly:

```text
Codex CLI / bundled codex.exe: 0.153.4
App Server:                    0.153.4
auth:                          chatgpt
```

No standalone CLI installation is required when the Desktop-bundled runtime continues to satisfy this exact gate.

## D077 disposition

R022 reviewed the resume path in:

```text
0.153.4
0.154.0
0.155.0-alpha.2
current upstream main
```

No reviewed higher version removes the material empty-rollout reattach race.

```text
version disposition: PIN_RETAINED
runtime:             0.153.4
upgrade fixes blocker: false
```

If a newer stable Codex release appears before Human launch of v4, D077 requires relevance classification before provider-backed execution.

## V4 Stage 5 candidate

ChatGPT Orchestrator has published:

```text
branch: test/t063-adaptive-worker-routing-requalification-v4
base:   9da2b6fed64ded9af8d38f67cd53cd066abef838
HEAD:   89da4d5b5fbeeb32e6dcf4dd4fde7839f043dfa5
```

The remote candidate is one commit ahead of that base and contains only eval/test material.

Eight v3 harness/test artifacts are reused from the accepted pre-provider repaired state at `4a1bc28b...` without semantic reinterpretation. The added v4 layer consists of:

```text
evals/adaptive_worker_routing_v4/__init__.py
evals/adaptive_worker_routing_v4/__main__.py
evals/adaptive_worker_routing_v4/runner.py
tests/test_t063_adaptive_worker_routing_v4_adapter.py
```

No Markdown or product source is part of the scientific candidate.

No v4 provider/model calls have occurred.

## V4 adapter boundary

V4 preserves the complete R6 experiment:

- frozen source/oracle baseline;
- P1/P2/P3 semantics;
- task-message digests;
- config-authoritative child task contract;
- requested child profile matrix;
- matched-arm order;
- first-attempt scoring;
- D063 permission/profile/usage/duration/reroute receipts;
- P3 seeded defect/oracle;
- pilot-decision definitions;
- no raw-response-event use.

The sole material adapter correction is:

```text
subAgentActivity Started
  -> exact child ID
  -> thread/resume same child
       if exact empty-rollout race:
         verify same parent is still loaded
         wait 0.2s
         retry same thread/resume
         maximum 10 attempts
       otherwise:
         fail immediately
  -> after successful resume, apply all existing D063 checks unchanged
```

A reattach retry must never spawn another child, start another provider/model turn, alter profile/task authority or replace a quality failure.

Exhaustion or parent-residency loss is `BLOCKED_MEASUREMENT_SURFACE` and authorizes no compensating scored call.

## Mandatory pre-provider Stage 6 gate

The Orchestrator environment could not execute the repository checkout/toolchain, so no claim is made that the new v4 adapter tests or Ruff have already run.

Before the first provider-backed scored parent turn, Codex Stage 6 must verify the exact published candidate with the repository-native locked toolchain. At minimum the gate must establish the semantic equivalent of:

```text
uv run --locked pytest -q \
  tests/test_t063_adaptive_worker_routing_v3_harness.py \
  tests/test_t063_adaptive_worker_routing_v4_adapter.py

uv run --locked ruff check \
  evals/adaptive_worker_routing_v3 \
  evals/adaptive_worker_routing_v4 \
  tests/test_t063_adaptive_worker_routing_v3_harness.py \
  tests/test_t063_adaptive_worker_routing_v4_adapter.py

uv run --locked python -m compileall -q \
  evals/adaptive_worker_routing_v3 \
  evals/adaptive_worker_routing_v4 \
  tests
```

Command syntax/mechanics remain D054 Executor-owned. The semantic gate is: both targeted suites pass, repository Ruff passes for the candidate, and Python compile passes **before any provider/model call**.

Bounded represented Stage 6 repair of formatting, typing or a technical implementation defect is allowed only when it preserves this review's frozen semantics. A repair that changes the retry classifier, retry policy, probes, task/profile authority, oracle, scoring, runtime pin or evidence meaning requires Orchestrator re-entry before provider execution.

## Scientific restart

V4 must be a homogeneous clean six-arm run:

```text
P1 ADAPTIVE -> CONTROL
P2 CONTROL  -> ADAPTIVE
P3 ADAPTIVE -> CONTROL
```

Historical T063 v1/v2/v3 results are evidence only and are excluded from v4 scoring:

```text
v1  3d8a9460988351383a90adfc6b76e2deff056504
v2  3ff745a8d29e031ca818c1bc618b15a54e0cbf2b
v3  746519abc6f159e959120f68d5c9f920d88d5797
```

Frozen source/oracle baseline remains:

```text
69e910f329a2294c3b40df0f6ee983f9905f4677
```

## Launch profile

Use a NEW failover root because root-3 observed the consumed v3 P1 attempt and its failure context.

```text
Executor/UI:      Codex Desktop on native Windows
Session:          NEW
Coordinator-ID:   AG | agent-governance | T063 | root-4
Root model:       gpt-5.6-sol
Root reasoning:   medium
Effective codex.exe required: exactly 0.153.4
App Server required:           exactly 0.153.4
Auth category:                 chatgpt
```

Use the Desktop-bundled `codex.exe` if and only if preflight proves it is exactly 0.153.4. Do not install or substitute a standalone CLI merely for ceremony. If the effective bundled runtime does not satisfy the exact pin, stop before scored provider work and return for Orchestrator review.

The child matrix remains:

| Probe | Arm | Requested model | Requested reasoning |
| --- | --- | --- | --- |
| P1 | ADAPTIVE | `gpt-5.6-luna` | `medium` |
| P1 | CONTROL | `gpt-5.6-sol` | `medium` |
| P2 | CONTROL | `gpt-5.6-sol` | `medium` |
| P2 | ADAPTIVE | `gpt-5.6-terra` | `medium` |
| P3 | ADAPTIVE | `gpt-5.6-terra` | `high` |
| P3 | CONTROL | `gpt-5.6-sol` | `medium` |

No model, reasoning or runtime substitution is authorized once provider-backed execution begins.

## D076 Stage 6 boundary

Codex may execute, diagnose, verify and make only bounded represented technical repairs to the published candidate.

Do not create a substantial new private/ephemeral controller, harness, fixture generator, oracle or workflow implementation. If the candidate needs such material, stop and return for Orchestrator Stage 5 re-entry.

Every non-candidate file-based executable aid actually used in verification remains subject to the handoff `ephemeral_artifacts` inventory.

Executor-authored committed Markdown remains prohibited.

## Evidence paths

Normal v4 evidence paths are:

```text
handoffs/T063-adaptive-worker-routing-telemetry-v4.json
handoffs/T063-executor-handoff-v4.json
```

The evidence must preserve exact parent/child provider-attempt accounting separately from local `thread/resume` RPC retries.

A blocked arm authorizes no compensating provider call unless this review explicitly allows it; this review does not.

## Pilot decision

The original T063 pilot definitions remain controlling. Only six fresh valid v4 first attempts may contribute to the v4 pilot decision.

R007 remains `EVALUATING`. V4 execution authority is not adoption of global adaptive worker routing.

## Human-mediated transport

D071 controls. ChatGPT does not start or control Codex.

After this review is integrated into `develop` and the candidate HEAD is revalidated, the Human starts a NEW Codex session with exact title:

```text
AG | agent-governance | T063 | root-4
```

## Terminal return

Return only:

```text
STATUS: COMPLETED | BLOCKED
HANDOFF: handoffs/T063-executor-handoff-v4.json
BRANCH: test/t063-adaptive-worker-routing-requalification-v4
HEAD: <actual remote pushed HEAD>
```
