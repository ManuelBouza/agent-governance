# T010-R1 — D036 deterministic assurance-audit contract acceptance

Review ID: T010-R1  
Task: T010  
Status: ACCEPTED  
Reviewer: ChatGPT Orchestrator  
Reviewed executor branch: `test/d036-deterministic-assurance-audit-contract`  
Reviewed executor HEAD: `421d68f2ce4c57709ea429450a3c09fd9e44f8d8`  
Implementation anchor: `bc517cbb02c5620dc2aa37c9506c54a7b346e669`  
Executor base: `develop@b043434b1ba58276e907d93242974e9e393c4ed5`

## Decision

T010 is **ACCEPTED**.

The remote implementation satisfies `docs/tasks/T010-d036-deterministic-assurance-audit-contract.md` and D040 Phase A. Acceptance applies to the exact reviewed executor HEAD above; later branch mutation requires a new review.

## Identity and scope evidence

Remote Git review established:

- returned HEAD `421d68f2ce4c57709ea429450a3c09fd9e44f8d8` is a handoff-only successor whose parent implementation anchor is `bc517cbb02c5620dc2aa37c9506c54a7b346e669`;
- the branch is exactly two commits ahead of base `b043434b1ba58276e907d93242974e9e393c4ed5` with that base as merge base;
- changed paths are exactly:
  - `tests/_helpers.py`;
  - `tests/fixtures/assurance_audit/policy_cases.json`;
  - `tests/test_assurance_audit_contract.py`;
  - `tests/test_canonical_layout.py`;
  - `tests/test_execution_control_contract.py`;
  - `tests/test_security_verification_contract.py`;
  - `handoffs/T010-executor-handoff.json`;
- no committed Markdown, dependency manifest, lockfile, Python-version file or production runtime was changed.

D041 was integrated into `develop` after T010 execution. D041 is Markdown-only executor-process policy and does not alter T010 executable semantics, fixtures, protocol authority or acceptance criteria; it therefore does not invalidate the reviewed T010 result.

## D040 protocol-authority result

T010 removes the duplicated mutable exact-current `SOURCE_PROTOCOL_VERSION` literal from executor-owned tests.

`protocol_version_from(...)` now reads the authoritative `Protocol-Version` declaration from `governance-core/GOVERNANCE.md` and accepts only exactly one valid strict SemVer declaration. Missing, malformed or duplicate declarations resolve as invalid rather than silently selecting one.

This establishes D040 Phase-A readiness while authoritative Core remains Protocol `1.12.0`. T010 does not activate Protocol `1.13.0` and does not route staged `ASSURANCE.md` as a required Core module.

## D036 deterministic assurance semantics

The synthetic fixture/evaluator contract covers the required A–I families:

1. scope/authorization completeness and non-expansion from reachability, credentials or tool capability;
2. monotonic assessment-profile ceilings including explicit intrusive authorization behavior;
3. all required finding states with missing/model-only evidence unable to manufacture `PASS`;
4. independent severity/confidence dimensions;
5. coverage gaps and bounded conclusions, including `NOT_ASSESSED`/`INCONCLUSIVE` non-pass behavior;
6. audit finding/remediation authorization separation;
7. point-in-time historical report state with independently mutable current posture;
8. D035 security and D033/D034 execution composition without cross-plane authority expansion;
9. staged `ASSURANCE.md`, current protocol authority and provider-neutral deterministic alignment.

The evaluator is test-local scaffolding over normalized synthetic facts and fixed timestamps. It does not create a production audit engine or real-system execution capability.

## Verification evidence

Executor handoff reports the required gates at the accepted implementation state:

```text
uv run --locked pytest -q tests/test_assurance_audit_contract.py
10 passed in 0.04s

uv run --locked pytest -q
154 passed in 0.86s

uv run --locked ruff check .
All checks passed!

uv run --locked ruff format --check .
13 files already formatted
```

The handoff also records no network requirement, model/LLM calls, credentials, external provider, real-system execution, package/dependency/config drift or privileged/destructive/remote/persistent operation.

## Acceptance conclusion

T010 has met its contract as a deterministic readiness task.

This acceptance does **not** activate D036 assurance routing or Protocol `1.13.0`. D040 Phase B remains ChatGPT-owned Markdown work after T010 integration and post-integration cleanup. L001 remains `CONTROL_FAILURE` until that activation proves the stronger single-authority control end-to-end.
