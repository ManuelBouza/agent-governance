# T004-R3 — Catalog-compatible model reselection

Status: ACTIVE REWORK DIRECTIVE  
Task: `T004`  
Reviewed executor branch: `eval/d032-agent-capability`  
Reviewed final HEAD: `eb20dc0fed2674190a82ef40aa0e02436c02ced4`  
Reviewed implementation anchor: `edc7fe186c0c84f6f30e3a2d8bbb4022ac609356`

## Review disposition

T004 remains **PARTIAL** and is not accepted.

R2 is accepted as diagnostic progress:

- structured OpenCode `error` events are extracted and sanitized before stderr fallback;
- focused deterministic coverage is green (`59 passed`);
- canonical suite is green (`173 passed`);
- temporary `d032-eval` configuration/agent preflight still passes under OpenCode `1.18.16`;
- OpenAI authentication presence was established read-only without exposing credential values;
- no dependency, global configuration, isolation or semantic-grading boundary changed;
- no transcript/result artifact was fabricated.

The required real capability baseline still has `0/18` completed sessions and `0/21` completed turns.

## D029 identity

The third executor return satisfies D029 identity:

- R2 implementation anchor: `edc7fe186c0c84f6f30e3a2d8bbb4022ac609356`;
- final pushed branch HEAD: `eb20dc0fed2674190a82ef40aa0e02436c02ced4`;
- the only commit after the implementation anchor changes only `handoffs/T004-executor-handoff.json`.

## R2 evidence and model-selection invalidation

The exact selected model for prior T004 attempts was:

`openai/gpt-5.6-terra`

R2 established under the intended pure/read-only OpenCode environment:

- an OpenAI auth entry is present;
- `openai/gpt-5.6-terra` is **not advertised/resolved** by `opencode models openai --pure`;
- exact `A-plain` trial 1 reaches the provider path but returns one sanitized structured `UnknownError` / `unknown-runtime` event, no response and zero tools;
- the single authorized minimal-context differential control reproduces the same structured class under the same provider/model and isolation.

This does not prove a precise provider backend defect, but it is sufficient to reject `openai/gpt-5.6-terra` as the T004 baseline target for installed OpenCode `1.18.16`. A model that the installed adapter cannot advertise/resolve is not an appropriate explicit model selection for a capability baseline.

The OpenCode adapter, corpus and D032 context remain viable candidates because the temporary agent resolves successfully and the minimal control fails in the same provider/model path.

## Strategy decision

Authorize **model reselection only** before considering an OpenCode version change or a second execution adapter.

This is the smallest architecture-preserving correction:

```text
T004 corpus/context/isolation
        │ unchanged
        ▼
OpenCode adapter 1.18.16
        │ unchanged
        ▼
explicit model selected from the adapter's effective OpenAI catalog
        │
        ▼
one smoke child
        │
        ├─ success -> full 18-session / 21-turn baseline
        └─ failure -> PARTIAL; no further model hopping
```

T004 remains an adapter-neutral capability baseline. The selected model identity is execution metadata and MUST be recorded exactly; changing the selected model here does not alter D032 semantics, corpus expectations or semantic grading authority.

## Authorized model-selection rule

Use `opencode models openai --pure` read-only and select exactly the **first advertised exact ID** in this Strategy-owned preference order:

1. `openai/gpt-5.6-sol`
2. `openai/gpt-5`
3. `openai/gpt-5-codex`
4. `openai/gpt-4.1`

Do not guess an ID that is absent from the effective catalog.

Rationale:

- prefer a current general-purpose GPT-5.6 model when the installed OpenCode catalog actually exposes it;
- otherwise use OpenCode-recommended GPT-5 / GPT-5 Codex / GPT-4.1 families in that order;
- do not retry `openai/gpt-5.6-terra` in T004 under OpenCode `1.18.16`;
- do not select Luna or another unlisted/special variant as an ad hoc workaround.

If none of the four exact IDs is advertised, stop `PARTIAL` and report only the sanitized matching OpenAI model IDs needed for Strategy to choose the next adapter/version action. Do not make a provider call in that case.

## Execution sequence

Continue on the existing branch:

`eval/d032-agent-capability`

1. Fetch/read this R3 directive from `origin/develop`.
2. Preserve all R1/R2 adapter code and isolation unless a model-independent regression is discovered.
3. Run existing focused tests and temporary-agent preflight.
4. Read the effective OpenAI catalog once and apply the exact preference rule above.
5. Record the selected model ID and why it won the deterministic preference rule.
6. Run exactly one fresh `A-plain` trial 1 with the newly selected model.
7. If that smoke child returns valid text with zero tool calls and successful process/session evidence, execute the full original T004 baseline with the same model.
8. If the smoke child fails, stop `PARTIAL`; preserve the R2 structured sanitized error and do not try the next model in the list.
9. Persist `evals/results/d032/T004-baseline.jsonl` only if all 21 original records validate across exactly 18 independent sessions.
10. Leave semantic grading `PENDING_CHATGPT` for every record.

The preference list selects one model from read-only metadata. It is **not** a runtime retry ladder.

## Preserve original T004 boundaries

Do not change:

- D032 corpus semantics or cardinality;
- Core bytes/digests injected from `INTERACTION.md`, `QUALITY.md`, `LIFECYCLE.md`;
- A-code preservation tokens;
- adapter-neutral normalized record schema;
- source revision binding to the controlling T004 base;
- fresh-session / multi-turn continuation semantics;
- zero-tool requirement;
- private-reasoning exclusion;
- source-worktree immutability;
- ChatGPT-only semantic PD5.

## Not authorized by R3

- no OpenCode upgrade/downgrade;
- no alternate execution adapter;
- no provider change;
- no new auth flow or credential mutation;
- no global configuration change;
- no iterative model probing;
- no isolation/tool-denial weakening;
- no mocks, pre-recorded responses or fabricated transcripts;
- no executor Markdown edits;
- no implementation PR.

## Verification

Run the focused harness and canonical gate:

```text
uv sync --locked
uv run --locked ruff check .
uv run --locked ruff format --check .
uv run --locked python -m pytest
```

If the full baseline succeeds, additionally prove:

- exactly 21 valid JSONL records;
- exactly 18 independent sessions;
- one model ID for every record, equal to the R3-selected model;
- zero tool calls in every record;
- successful process status;
- exact source/Core/corpus binding;
- semantic status `PENDING_CHATGPT`.

## Handoff requirements

Update `handoffs/T004-executor-handoff.json` under D029 with a new implementation anchor if any non-handoff implementation/test/result artifact changes, followed by a final handoff-only commit.

Report:

- effective catalog booleans for the four Strategy candidates;
- exact selected model or `none`;
- unchanged OpenAI auth-presence boolean only;
- adapter preflight result;
- smoke child outcome and structured sanitized failure if applicable;
- exact session/turn accounting;
- focused/full verification counts;
- baseline path/digest only if complete;
- no dependency/global-config/isolation drift.

## Acceptance effect

T004 remains unaccepted until a real complete 18-session / 21-turn baseline exists and ChatGPT performs semantic PD5 over all persisted user-visible responses.