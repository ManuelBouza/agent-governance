# T063-R15 — V7 Stage 7 Convergence Review

Status: ACCEPTED_BLOCKED / STAGE2_REENTRY_REQUIRED  
Date: 2026-09-12  
Task: `docs/tasks/T063-adaptive-worker-routing-requalification.md`  
Launch authority: `docs/reviews/T063-R14.md`  
Blocker research: `docs/research/R024-T063-V7-NO-ROLLOUT-REATTACH-RACE.md`  
Prior design: `docs/reviews/T063-R13.md`

## Terminal result reviewed

Human returned the exact Task Contract terminal shape:

```text
STATUS: BLOCKED
HANDOFF: handoffs/T063-executor-handoff-v7.json
BRANCH: test/t063-adaptive-worker-routing-requalification-v7
HEAD: 58e396c126e363428544b163cb2aa8c7e1ac8ed6
```

Remote verification confirms the branch currently resolves to the returned terminal HEAD.

Terminal evidence:

```text
initial authorized candidate: 9fb55e8f36570f6b91d0a23720ccc6a62a9d0b90
implementation head before scoring: 5a1769fed3f93d86cbc2e72a6cc89d7a276089d6
terminal evidence head: 58e396c126e363428544b163cb2aa8c7e1ac8ed6
terminal classification: BLOCKED_EXECUTION_INVALID
pilot_decision: null
```

## Provider-free gate review

The Executor reports all required pre-provider gates passed before scoring:

```text
candidate identity/base ancestry: PASS
Codex runtime 0.153.4:           PASS
App Server 0.153.4:              PASS
current stable release gate:     PASS
compileall:                      PASS
pytest:                          PASS — 55 passed
Ruff:                            PASS
oracle preparation:              PASS
```

No evidence indicates a provider call occurred before those gates completed.

## Stage 6 bounded-repair review

Two pre-scoring v7 adapter repairs were represented:

```text
c259648701510e90ed61fdd641022f8898db6880
  equivalent comparison rewrite for lint

5a1769fed3f93d86cbc2e72a6cc89d7a276089d6
  ArmSpec.reasoning field correction in the frozen schedule guard
```

Remote diff from the initial candidate to the implementation head changes exactly two lines in `evals/adaptive_worker_routing_v7/runner.py` and no other file.

Both changes are accepted as D076-bounded mechanics-preserving repairs. They do not alter probes, oracles, child profiles, 24-arm order, retry policy, quality semantics, token threshold or decision taxonomy.

The terminal commit from implementation head to returned HEAD adds only:

```text
handoffs/T063-adaptive-worker-routing-telemetry-v7.json
handoffs/T063-executor-handoff-v7.json
```

No executable or Markdown redesign was hidden in the terminal evidence commit.

## Execution accounting

The run consumed six scheduled parent/child attempts. Five children reached complete D063 measurement and all five were PASS:

```text
P1 ADAPTIVE PASS
P1 CONTROL  PASS
P2 CONTROL  PASS
P2 ADAPTIVE PASS
P3 ADAPTIVE PASS
```

The sixth scheduled arm, P3 CONTROL, was publicly correlated but blocked during first `thread/resume` before a fully measured child snapshot existed.

The adapter audit also records five earlier empty-rollout reattachment retries, each on the same child and each succeeding after one bounded retry without a new provider turn.

Terminal blocker:

```text
thread/resume failed: {'code': -32600, 'message': 'no rollout found for thread id <exact child id>'}
```

The Executor correctly made no replay, replacement spawn, diagnostic child attempt or compensating scored call after the blocker.

## Model-evidence interpretation

The v7 evidence is internally consistent:

```text
run_execution_validity: INVALID
run_model_comparison_eligible: false
pilot_eligible: false
scored_child_quality_eligible_count: 5
scored_child_efficiency_eligible_count: 5
root_model_failure_attributed: false
pilot_decision: null
```

The five PASS observations are valid historical child-quality evidence only. They do not form a complete matched comparison and cannot be combined with a successor clean run.

No v7 conclusion is permitted about global ADAPTIVE qualification, CONTROL qualification, exact-token efficiency, or the R007 global routing policy.

## Blocker classification

R024 establishes that `no rollout found for thread id <exact child>` is produced by the same persisted-read path involved in R022, but at an earlier visibility phase: no rollout is yet resolvable through live-writer, SQLite or filesystem surfaces.

Classification:

```text
cause: SAME_CHILD_ROLLOUT_DISCOVERY_VISIBILITY_RACE
failure domain: measurement adapter/specification
worker-quality failure: no
profile-resolution failure: no
runtime-upgrade requirement: no
```

Because v7's retry contract authorized only the exact empty-rollout family, fail-closed blocking was correct.

## D077 review

Official release state was revalidated on 2026-09-12:

```text
qualified pin: 0.153.4
latest stable: 0.154.0
disposition: PIN_RETAINED
```

`rust-v0.154.0` retains the same `no rollout found for thread id ...` path when rollout resolution fails. Upgrading to current stable is therefore not a demonstrated fix and D063 qualification is not extended.

## Stage 7 decision

V7 is **accepted as a correctly blocked historical execution**, not accepted as a successful qualification candidate.

Disposition:

```text
v7 terminal evidence: ACCEPTED AS HISTORICAL BLOCKED EVIDENCE
v7 executable candidate: DO NOT INTEGRATE INTO develop
v7 telemetry/handoff: DO NOT INTEGRATE INTO develop
v7 model comparison: INVALID / INCOMPLETE
v7 pilot decision: null
R007: remains EVALUATING
```

The terminal branch/HEAD remains the Git authority for v7 evidence.

## SDD re-entry

Earliest affected stage is Stage 2 Specify.

A clean successor must prospectively extend only the same-child persistence barrier to support two exact retryable classes:

```text
EMPTY_ROLLOUT
ROLLOUT_NOT_FOUND_FOR_EXACT_CHILD
```

The same ten-attempt budget applies across both classes, including mixed sequences. The exact correlated child ID must appear in the no-rollout error. All existing sleep, parent-residency, same-child/same-params, no-respawn/no-new-provider-turn and fail-closed invariants remain mandatory.

All other v7 experiment semantics remain preserved unless a later explicit Orchestrator review changes them.

A successor must execute a new homogeneous 24-arm run; no v7 observation enters its score.

## Current authority

No additional T063 provider/model execution is authorized by this review.

The next Orchestrator action is to revise the active Task Contract prospectively for the clean successor, complete Design/Plan & Trace and D068 Stage 5 materialization, then perform a new readiness review before any Human-mediated launch.