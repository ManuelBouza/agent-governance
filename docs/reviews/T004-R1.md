# T004-R1 — OpenCode adapter preflight and isolated-child rework

Status: ACTIVE REWORK DIRECTIVE  
Task: `T004`  
Reviewed executor branch: `eval/d032-agent-capability`  
Reviewed final HEAD: `d3b8fbfe71467e19a2225bb5fd779c82930214bd`  
Reviewed implementation anchor: `d1961944e2a6c00ff5c35f2eb7dc11c3d151535a`

## Review disposition

T004 is **not accepted**.

The executor correctly returned `PARTIAL` and failed closed. The required 18 independent sessions / 21 turns were not produced; completed sessions and turns are both zero, and no baseline transcript artifact exists. Semantic D032 PD5 therefore cannot be performed yet.

D029 identity is valid for this return: `d1961944...` is the implementation anchor and the only later commit to `d3b8fbfe...` changes only `handoffs/T004-executor-handoff.json`.

The implementation remains inside the authorized non-Markdown scope and the reported deterministic gate is green (`136 passed`; focused harness `22 passed`).

## Concrete adapter defect found by PD5

The current OpenCode adapter writes a custom agent named `d032-eval` with `prompt` and legacy `tools` fields, but **without a `description`**.

Current OpenCode agent documentation for the installed-era interface states that `description` is a required custom-agent configuration option. `mode` defaults to `all`, but for this adapter the intended use is a primary agent and SHOULD be made explicit.

Therefore the failed child execution cannot yet be attributed solely to the OpenCode host/provider/model. The temporary adapter configuration itself is not conformant with the documented custom-agent contract.

The current deterministic tests also validate corpus/record invariants but do not prove that the temporary OpenCode config resolves a runnable `d032-eval` agent before a provider call.

## Required rework

Continue on the existing branch:

`eval/d032-agent-capability`

Apply only the following rework.

1. **Correct the temporary custom-agent configuration.**
   - Add a non-empty `description` for `d032-eval`.
   - Make the intended `mode` explicit as `primary` unless the installed OpenCode interface proves another mode is required for `opencode run --agent d032-eval`.
   - Preserve deny-all tool/permission behavior. Do not relax tool denial, external-directory isolation, project/plugin/Skill disable controls, or disposable workspace/config/database isolation.
   - Prefer the current `permission` surface for effective denial; legacy `tools` may remain only if it is still useful as defense in depth and supported by the installed version.

2. **Add an adapter configuration smoke/preflight check before model trials.**
   - Under the exact temporary environment/config used by a child trial, use a read-only documented OpenCode capability such as `opencode agent list` to prove that `d032-eval` is loadable/resolvable.
   - The preflight must not invoke the model, mutate global configuration, or inspect/copy credentials.
   - Add focused deterministic coverage for the generated agent/config shape and the preflight success/failure handling where practical without mocking a successful model response.

3. **Improve fail-closed diagnostics without leaking secrets or hidden reasoning.**
   - If the isolated child still exits non-zero or emits no valid JSON event stream, capture enough sanitized stderr/log classification to distinguish at least configuration/agent resolution, provider/model/auth/network, and malformed/empty event output.
   - `--print-logs` / debug logging may be used only for bounded diagnosis and must not cause credential/environment dumps to be committed.
   - Persist diagnostics in the handoff, not as fabricated trial records.

4. **Re-run the real capability baseline only after adapter preflight passes.**
   - Use real model calls; no mocks, pre-recorded answers, fabricated transcripts, or model self-grading.
   - Run exactly 3 independent trials for each required case/session and the two-turn continuation for the D case: 18 independent sessions / 21 turns total.
   - Persist `evals/results/d032/T004-baseline.jsonl` only if the complete record set passes the existing fail-closed record validation.
   - Semantic fields remain `PENDING_CHATGPT`.

5. **If the corrected/preflighted adapter still cannot produce a response, stop `PARTIAL`.**
   - Do not weaken isolation.
   - Do not silently switch provider/model merely to obtain a green run.
   - Report the exact sanitized failure class and enough evidence for Strategy to decide whether an OpenCode/provider compatibility task or an alternate execution adapter is required.

## Preserve accepted T004 behavior

Do not change:

- D032 corpus semantics or case cardinality;
- Core bytes injected from `INTERACTION.md`, `QUALITY.md`, and `LIFECYCLE.md`;
- source/Core digest binding;
- exact A-code preservation tokens;
- adapter-neutral normalized record semantics;
- fresh-session requirements;
- zero tool-call requirement;
- private-reasoning exclusion;
- ChatGPT-only semantic PD5 boundary;
- source-worktree immutability during child trials;
- no-dependency / standard-library-first constraint.

## Prohibited scope

- no Markdown edits by the executor;
- no new production/runtime Governance API;
- no new dependency;
- no global OpenCode/Gentle-AI/provider configuration mutation;
- no OpenCode upgrade/downgrade unless separately authorized;
- no weakening of security/isolation controls;
- no PR creation/merge by the executor.

## Verification

After focused adapter/harness tests, run the canonical gate:

```text
uv sync --locked
uv run --locked ruff check .
uv run --locked ruff format --check .
uv run --locked python -m pytest
```

If the real baseline succeeds, additionally verify the persisted JSONL contains exactly 21 valid records across exactly 18 independent sessions and that every record has zero tool calls, successful process status, source/Core/corpus binding, and `PENDING_CHATGPT` semantic grading.

## Handoff requirements

Update `handoffs/T004-executor-handoff.json` under D029 with a new `implementation_head_sha` and final pushed branch HEAD.

The handoff must report:

- corrected agent/config shape and adapter preflight evidence;
- installed OpenCode version and explicit model;
- focused/full verification counts;
- exact trial/session/turn accounting;
- result artifact path/digest if and only if the complete baseline exists;
- sanitized failure class if still `PARTIAL`;
- tool-denial/isolation evidence;
- no global config/dependency drift;
- `.atl` remains local/uncommitted and Gentle-AI RDD remains clone-local disabled/global unchanged.

## Acceptance effect

T004 remains unaccepted. No implementation PR is allowed until the complete baseline exists and ChatGPT performs semantic PD5 over the real transcripts.