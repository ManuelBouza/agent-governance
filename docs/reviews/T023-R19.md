# T023 Review R19 — T061 MG1-v14 Stage 6 Launch Gate

Status: `STAGE6_AUTHORIZED / AWAITING_CODEX_EXECUTOR_START`

Date: 2026-09-07

Owner: ChatGPT Orchestrator

Controlling work unit: `T061`

Stage 5 readiness: `docs/reviews/T023-R18.md`

Prior v13 launch authority: `docs/reviews/T023-R15.md` — historical only

## Human selection

The Human Owner explicitly selected renewed T061 D068 Stage 6 on 2026-09-07T17:57:59Z by replying `go` after O242 reported `READY_FOR_STAGE6_AWAITING_HUMAN_LAUNCH`.

This review is the new current Stage 6 launch authority for MG1/T023 v14. It supersedes R15 only for current launch authority; it does not rewrite or reinterpret the historical v13 launch, blocker, handoff, Freeze A or Freeze B.

## Authorized execution target

Represented Stage 6 topic branch:

`test/t023-skill-activation-topology-evals-v14`

Reviewed Stage 5 branch HEAD at authorization:

`a8caf6338dbae3a3ce4cefdc7965acd0d37f1557`

Prospective freeze boundaries:

- Candidate Freeze C: `1fc38f979d67ff29649f69ae39b8d46d2523518b`;
- Holdout/oracle Freeze D: `f8cee7c72688f0486211474d2678d018772e2a58`.

Current canonical `develop` at authorization:

`31b6514b6f8870fde7c6685dff7e84ed9c5cce8e`

Remote launch revalidation established:

- the v14 topic branch still points to `a8caf633...`;
- Freeze D is a descendant of Freeze C;
- Freeze C -> Freeze D changes exactly `corpus.json`, `oracle.json`, and `verify_v14_holdout_integrity.py`;
- Freeze D -> reviewed Stage 5 HEAD changes only technical harness/tests plus R18;
- no post-freeze mutation is present in candidate/reference bytes, `candidate-hashes-v14.json`, corpus, oracle, topology/presentation semantics, capability-source semantics, or the v14 integrity guards;
- corrected `presentations-v5/G3/source-maintainer/SKILL.md` remains Git blob `e00edee8b1a1bacbb47b01bad4492f8ddf954628`;
- candidate-hash manifest remains blob `f78b587b76ef0c06669b15fa4d0e85b393cab5b0` with identity `MG1-T061-CANDIDATE-HASHES-v2`;
- oracle remains blob `91d3e472b162486e8e099d77fd60503413a21d88` with identities `MG1-T023-TOPOLOGY-ORACLE-v14`, `MG1-T023-EXECUTION-v14`, `MG1-T023-CORPUS-v8`, and Candidate Freeze C `1fc38f...`.

The known fact that `develop` contains later orchestration Markdown is not permission to rebase, merge, reset, cherry-pick or otherwise rewrite the represented v14 scientific branch before its own integrity/freshness gates establish safety.

## Executor launch profile

Concrete Executor: `Codex`

Surface: `ChatGPT desktop / Codex / native Windows`

Session: `NEW`

Coordinator-ID: `AG | agent-governance | T061 | root-2`

Model: `GPT-5.6 Sol`

Reasoning effort: `Medium`

Codex CLI: exactly `0.149.0`

Rationale:

- T061 freezes the scientific live cell to Codex/native Windows/GPT-5.6 Sol/Medium and exact CLI `0.149.0`;
- OpenAI Help was rechecked on 2026-09-07 and continues to list GPT-5.6 Sol in Codex, Medium as a GPT-5.6 Sol reasoning level, and Codex CLI `0.144.0` as the minimum for GPT-5.6, so the frozen `0.149.0` baseline remains compatible;
- no upgrade to a newer model or CLI is authorized merely because one is available;
- D060 continuity prefers `CONTINUE` only when the prior coordinator is safely recoverable. The current Orchestrator runtime cannot establish that the historical v13 `AG | agent-governance | T061 | root-1` session is recoverable and clean for the materially new v14 authority, so this launch uses explicit failover `NEW ... root-2` rather than guessing.

If exact CLI `0.149.0`, native Windows, GPT-5.6 Sol or Medium cannot be realized, fail closed before acceptance calls and return `BLOCKED` for Orchestrator re-entry. Do not silently upgrade or broaden the host cell.

## Stage 6 authority boundary

R19 authorizes only D068 Stage 6 Execute / Diagnose / Repair / Verify for the represented v14 candidate under the existing T061 semantics as prospectively superseded by R17/R18.

It does not authorize:

- D068 Stage 7 convergence/acceptance/integration;
- release topology selection;
- T024;
- D066 gap changes;
- T058 re-entry;
- semantic changes to B2/F2/G3 presentations, corpus v8, oracle v14, capability-source semantics, topology/presentation semantics, trial-envelope semantics, thresholds, scheduling, materiality or aggregation rules;
- history rewriting of v13 or v14 freeze evidence.

Executor technical repairs are allowed only inside D054/D068 mechanics while preserving the approved semantics and Design. A suspected semantic/specification/design/plan/oracle defect is a stop-and-return condition for Orchestrator re-entry, not Executor authority to tune the experiment.

T061 remains `ASSURED`. D065 applies: before substantial Stage 6 work, and again before final Code Review & Verify, the Executor must evaluate whether bounded delegation materially improves verification while preserving exclusive branch/worktree ownership, semantic authority, evidence traceability and the no-unapproved-oracle-change boundary.

## Mandatory execution order

The Executor MUST fail closed in this order:

1. synchronize with GitHub and establish a safe current local baseline without discarding unrepresented local work;
2. verify the represented v14 branch HEAD, Freeze C/Freeze D ancestry, candidate/reference immutability and current frozen identities;
3. run the provider-free v14 candidate-integrity and holdout-integrity guards;
4. run the frozen deterministic regression, scheduler simulation, repository lint/format/pytest/code-health/symbol-map gates required by the harness;
5. prove provider/model calls issued so far = `0`;
6. only after all deterministic/provider-free gates PASS, resolve and verify the frozen native-Windows backend/workspace profile;
7. only after backend/workspace PASS, run the unchanged synthetic Skill canary and require `2/2 PASS`;
8. only after canary PASS, run B2 Stage R under the frozen v14 corpus/oracle/execution epoch;
9. if B2 does not qualify or reaches valid qualification futility, terminate `BLOCKED / NO QUALIFYING SINGLE-FAMILY REFERENCE` and leave F2/G3 unscheduled;
10. if B2 qualifies, establish it as the same-epoch reference and run F2/G3 only as permitted by the frozen scheduler/materiality rules;
11. perform Executor-owned Code Review & Verify, including the second D065 delegation evaluation;
12. create and push `handoffs/T061-executor-handoff.json` on the represented task branch before terminal response.

No chat-only/local-only evidence is sufficient for completion. Stage 6 terminal evidence must be represented in remote Git.

## Required terminal response

Return only:

```text
STATUS: <COMPLETED|BLOCKED>
HANDOFF: handoffs/T061-executor-handoff.json
BRANCH: test/t023-skill-activation-topology-evals-v14
HEAD: <remote pushed HEAD sha>
```

## Transport prompt

Use the following prompt for the new Codex coordinator:

```text
Operate as the Agente de IA Ejecutor for ManuelBouza/agent-governance.

Synchronize with GitHub and establish a safe current local baseline from canonical remote state before loading repository instructions or execution authority, per repository policy. Do not discard unrepresented local work.

This is the renewed T061 MG1/T023 v14 Stage 6 execution. Use the represented task branch:
test/t023-skill-activation-topology-evals-v14

Preserve Candidate Freeze C 1fc38f979d67ff29649f69ae39b8d46d2523518b, Holdout/oracle Freeze D f8cee7c72688f0486211474d2678d018772e2a58, and reviewed Stage 5 HEAD a8caf6338dbae3a3ce4cefdc7965acd0d37f1557. Do not rebase or rewrite the scientific branch merely because develop contains later orchestration Markdown.

Load current repository instructions and current launch authority from develop, then execute T061 under the prospective v14 authority in:
docs/tasks/T061-mg1-v13-positive-anchor-reference-evaluation.md
docs/reviews/T023-R17.md
docs/reviews/T023-R18.md
docs/reviews/T023-R19.md

Use exactly Codex CLI 0.149.0 on native Windows with GPT-5.6 Sol / Medium. If that exact cell cannot be realized, fail closed before acceptance calls.

Begin with deterministic/provider-free gates and prove model/provider calls = 0 before host preflight. Only after deterministic PASS may you resolve the frozen host profile; only after host PASS may you run the unchanged synthetic canary; only after canary 2/2 PASS may you schedule B2. F2/G3 remain unscheduled unless B2 qualifies.

Preserve Orchestrator-owned semantic assets. Any material requirement/Design/Plan/oracle defect requires stop and Orchestrator re-entry rather than semantic repair.

T061 is ASSURED. Evaluate bounded delegation before substantial work and again before final Code Review & Verify, while preserving ownership/evidence constraints.

Before terminal response, create and push handoffs/T061-executor-handoff.json. Return only STATUS/HANDOFF/BRANCH/HEAD as required by R19.
```

## Current launch state

`AUTHORIZED_AWAITING_EXECUTOR_START`

The current ChatGPT Orchestrator runtime has no connected general Codex session-execution transport. Plugin discovery performed at authorization time found OpenAI developer/documentation and Codex Security integrations, but no tool capable of starting or controlling the required native-Windows Codex coordinator. Therefore this review creates real launch authority but claims no Executor session, deterministic gate, host preflight, canary, B2/F2/G3 observation or provider/model call.
