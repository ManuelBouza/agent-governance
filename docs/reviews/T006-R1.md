# T006-R1 — D035 deterministic security-verification contract

Review: ACCEPTED  
Task: T006  
Executor branch: `test/security-verification-contract`  
Reviewed final HEAD: `080cf745a9555a70e4f6d3d487c8d817905d4a80`  
Implementation anchor: `2323f88d32744286ecde1d7bf05b65e4238cdbd4`  
Base: `develop@3daf77989d68d32062e60d960a8cd587458ca82d`

## Scope review

Remote comparison against the declared base shows only authorized non-Markdown paths:

- `tests/_helpers.py`;
- `tests/fixtures/security_verification/policy_cases.json`;
- `tests/test_security_verification_contract.py`;
- `handoffs/T006-executor-handoff.json`.

No Markdown, Governance Core, dependency, lockfile, Python-version, runtime, provider, scanner, network, model, credential, consumer-footprint, CI/ruleset or D036 implementation change is accepted.

## Contract fidelity

The reviewed deterministic harness covers all required T006 fixture families A-H:

- explicit security authority and source conflict;
- class-aware freshness including `CURRENT`, `STALE`, `UNKNOWN`, `CONFLICT`, `SUPERSEDED`;
- applicable active known-bad precedence despite model recommendation/statistical-commonness metadata;
- independent evidence and `PASS | BLOCK | HUMAN_EXCEPTION` outcomes;
- exact-scope, compensating-evidence-supported and expiry-sensitive Human exceptions;
- temporal posture invalidation without rewriting historical task acceptance;
- non-expanding composition between security outcome and D033/D034 execution authorization;
- Protocol `1.12.0`, `SECURITY.md` and provider-neutrality alignment.

The evaluator is test-local and consumes normalized synthetic facts with explicit timestamps/versions. It does not parse arbitrary security prose, use wall-clock time, call networks/models/providers, access credentials or mutate real systems.

## Critical negative evidence

The reviewed cases mechanically prove:

```text
applicable ACTIVE known-bad -> BLOCK
model/reviewer claim != security evidence
security PASS != execution authorization
execution authorization != security PASS
historical acceptance != permanent current security posture
```

The active known-bad case remains blocking even with `model_recommends = true`, `historically_common = true`, and synthetic prevalence `99`.

Human exception validity is exact for control, target and version, requires verified compensating evidence, is non-transitive to child/unrelated scope, and expires deterministically.

## Verification evidence

Executor handoff reports:

```text
uv run --locked pytest -q tests/test_security_verification_contract.py
9 passed

uv run --locked pytest -q
144 passed

uv run --locked ruff check .
PASS

uv run --locked ruff format --check .
PASS
```

The handoff records no unresolved issues and no required network/model/provider/credential/real-system boundary.

## Disposition

T006 is `ACCEPTED` at exact final HEAD `080cf745a9555a70e4f6d3d487c8d817905d4a80`.

Acceptance authorizes integration of that exact reviewed branch state only. Any branch advancement requires re-review. D036 remains outside T006 and MUST NOT start until T006 integration and post-integration cleanup close.
