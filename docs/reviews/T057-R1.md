# T057-R1 — Codex Read-Only Child Requalification v2 Review

Status: ACCEPTED_QUALIFIED_READ_ONLY_CHILD_SURFACE  
Task: `docs/tasks/T057-codex-read-only-child-requalification-v2.md`  
Executor handoff: `handoffs/T057-executor-handoff.json`  
Telemetry: `handoffs/T057-read-only-child-telemetry.json`  
Submitted HEAD: `4dd957aaf76235376ace709bf5117378c89e46aa`  
Telemetry anchor: `41383251e9786e8b2e39e2083ed4d487fac63b95`  
Evidence PR: `#296`  
Integrated evidence commit: `947c5ed1edcff86603a4c3e8d3cf9bf96eabdfc6`  
Frozen launch base: `20ed0e64dd6c98f38be42cd3cc28fcc220d06c5e`  
Date: 2026-09-05  
Reviewer: ChatGPT Orchestrator

## Verdict

T057 is **accepted** with the frozen qualification decision:

```text
QUALIFIED_READ_ONLY_CHILD_SURFACE
```

The successor corrected only T056's temporary `thread/loaded/list` parsing/parent-residency defect while preserving the frozen experimental controls. Every mandatory T057 acceptance gate passed on installed Codex/App Server 0.153.4.

This qualification establishes a supported, auditable read-only child measurement surface for the bounded semantics defined by T057. It does **not** prove per-turn backend-served model identity, reactivate adaptive routing by itself, change D055, or establish any savings/cost claim.

## Repository boundary

The submitted branch is exactly two commits ahead of its frozen base and changes only:

```text
handoffs/T057-read-only-child-telemetry.json
handoffs/T057-executor-handoff.json
```

No product code, Markdown, dependency, Governance Core, routing policy, D055, persistent agent catalog or global Codex configuration changed.

## Accepted host and controller evidence

The run established:

```text
Codex CLI:                    0.153.4
App Server initialize:        0.153.4
native schema:                0.153.4
transport:                    stdio / JSON-RPC
thread/loaded/list shape:     object -> data: array<string>
auth category:                chatgpt
provider-backed attempts:     1
compensating attempts:        0
root:                         GPT-5.6 Sol / Medium
coordinator:                  AG | agent-governance | T057 | root-1
```

The controller resolved the installed native schema before provider-backed work, parsed loaded thread IDs as strings, and kept the same App Server process alive through child reattachment.

## Parent permission receipt

The parent was started with:

```text
permissions = ":read-only"
legacy sandbox request = omitted
approval = never
```

Supported lifecycle evidence returned:

```text
activePermissionProfile.id = ":read-only"
legacy sandbox.type = readOnly
legacy sandbox.networkAccess = false
```

No contradictory broader projection was observed.

## Real child and continuous parent residency

Exactly one real Multi-Agent V2 child was correlated through supported relationship evidence:

```text
task/path:        t057_read_only_child
child thread:     01a071f3-3555-7200-bd7a-ba7b6a9e514c
parent thread:    01a071f3-1e27-71f0-b5f3-60c633e784e4
matching children: 1
```

Immediately before child reattachment the native loaded-thread set contained both the parent and exact child IDs. The parent remained present after reattachment and the same App Server process stayed alive through capture.

The T056 causal defect is therefore closed.

## Child permission receipt

The exact child was reattached through the parent-owned surface without configuration overrides. The request did not supply permissions, sandbox, model, reasoning, cwd, approval or environment overrides.

Supported child lifecycle evidence returned:

```text
activePermissionProfile.id = ":read-only"
legacy sandbox.type = readOnly
legacy sandbox.networkAccess = false
```

No contradictory broader projection was observed.

This satisfies the decisive read-only child provenance gate that T055 and T056 could not close.

## Compute-profile evidence and boundary

For the exact child:

```text
requested model:       gpt-5.6-terra
requested reasoning:   low
resolved provider:      openai
resolved model:         gpt-5.6-terra
resolved reasoning:     low
reroute observed:       false
backend-served profile: UNVERIFIED
```

The requested/resolved configured thread state matches. The review preserves the T057 boundary: resolved thread configuration plus absence of `model/rerouted` is not an authoritative per-turn backend execution receipt.

## Exact attributable usage

For exact child turn `01a071f3-364c-7f80-b936-a99d971bfc06`, supported server telemetry reports:

```text
input tokens:             22513
cached input tokens:      0
cache-write input tokens: 0
output tokens:            23
reasoning output tokens:  0
total tokens:             22536
context window:           258400
estimated:                false
```

Two identical usage snapshots were observed for the same thread/turn and were correctly recorded once rather than summed.

## Exact attributable duration

For the same exact child turn:

```text
startedAt:   1788618159
completedAt: 1788618164
durationMs:  4516
```

The duration comes from the supported exact-child Turn timing surface, cross-checked through thread turn listing. Controller wall-clock and parent timing were not substituted.

## Reroute evidence

The installed surface supports `model/rerouted`. Exact-child event capture remained continuous and returned zero reroute notifications.

Accepted interpretation:

```text
reroute_observed = false
backend_served_profile_verified = false
```

Absence of a reroute notification is not promoted into backend identity proof.

## Safety evidence

Accepted safety facts:

- exactly one provider-backed parent/child attempt;
- no compensating second attempt;
- no child write probe;
- no shell/write tool attempt by parent or child;
- tracked worktree remained clean through the provider-backed probe;
- temporary controller/schema artifacts stayed outside tracked product state and were removed as required;
- no dependency, repository configuration, global Codex configuration or alternate binary change;
- no SQLite/JSONL/private rollout state used to create or upgrade passing evidence.

## Acceptance criteria

```text
AC-T057-1   PASS
AC-T057-2   PASS
AC-T057-3   PASS
AC-T057-4   PASS
AC-T057-5   PASS
AC-T057-6   PASS
AC-T057-7   PASS_CONFIGURED_THREAD_STATE_ONLY_BACKEND_UNVERIFIED
AC-T057-8   PASS
AC-T057-9   PASS
AC-T057-10  PASS_NO_REROUTE_OBSERVED_BACKEND_UNVERIFIED
AC-T057-11  PASS
AC-T057-12  PASS
```

## Research disposition

T057 supplies the empirical evidence needed to close the measurement-surface question raised by R008/R009.

The normative adoption is recorded separately in:

`docs/decisions/D063-qualified-codex-read-only-child-measurement-surface.md`

Under D057, after D063 and the registry transition are integrated:

```text
R008 -> COMPLETE / DECIDED
R009 -> COMPLETE / DECIDED
```

R007 does **not** automatically return to `EVALUATING`. Its measurement-substrate blocker is cleared, but a corrected routing evaluation must still remove the T054 P2 semantics confound, use a first-attempt-qualified mapping, and receive its own explicit D057 transition before execution.

R012 remains a separate post-T057 delegation-policy decision and is not decided by this review.
