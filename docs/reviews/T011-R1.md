# T011-R1 — Assurance active-routing deterministic verification acceptance

Review ID: T011-R1  
Task: T011  
Status: ACCEPTED  
Reviewer: ChatGPT Orchestrator  
Reviewed executor branch: `test/assurance-active-routing-verification`  
Reviewed executor HEAD: `c231eec0cf5e96b676df9932402a3166fa4589c2`  
Implementation anchor: `b83b04d3562d5cdd2d5a820e7230a3552bd70616`  
Executor base: `develop@eecf676934d619606b5e64a9a6519b2ad47970cc`

## Decision

T011 is **ACCEPTED**.

The remote implementation satisfies `docs/tasks/T011-assurance-active-routing-verification.md`. Acceptance applies to the exact reviewed executor HEAD above; later branch mutation requires a new review.

## Identity and scope evidence

Remote Git review established:

- reviewed HEAD is exactly two commits ahead of the contractual base, with that base as merge base;
- implementation anchor `b83b04d3562d5cdd2d5a820e7230a3552bd70616` changes only `tests/_helpers.py` and `tests/test_assurance_audit_contract.py`;
- final HEAD `c231eec0cf5e96b676df9932402a3166fa4589c2` is a handoff-only successor adding `handoffs/T011-executor-handoff.json`;
- no committed Markdown, Core Markdown, dependency manifest, lockfile or production runtime changed.

## Verification model

T011 preserves `governance-core/GOVERNANCE.md` as the sole current protocol-version authority.

The deterministic test model now distinguishes state consequences rather than duplicating a mutable current-version literal:

```text
ASSURANCE STAGED
  -> Protocol 1.12.0 historical readiness state
  -> not routed
  -> not required Core

ASSURANCE ACTIVE
  -> Protocol >= 1.13.0
  -> routed
  -> required Core
```

`1.12.0` is retained as a historical staged fixture and `1.13.0` as the minimum compatible active-state threshold; neither is an independently authored exact-current protocol authority.

The implementation derives Assurance required-Core membership from the canonical `GOVERNANCE.md` routing surface and validates that ACTIVE implies routing plus required-Core membership.

## Verification evidence

The persisted handoff reports on the implementation branch:

```text
uv run --locked pytest -q tests/test_assurance_audit_contract.py
10 passed in 0.02s

uv run --locked pytest -q
154 passed in 0.66s

uv run --locked ruff check .
All checks passed!

uv run --locked ruff format --check .
13 files already formatted
```

The executor also verified a disposable composition of the T011 executable changes with exact PR #81 candidate HEAD `1207637673619b40234556d0b37cac8401ffc83a` without modifying or pushing that candidate:

```text
focused pytest: 10 passed in 0.02s
full pytest: 154 passed in 0.61s
ruff check: PASS
ruff format check: PASS
result: PASS
```

The handoff records no Markdown mutation, dependency/lockfile drift, production runtime change, provider/model/network/live-system behavior, or CodeGraph/SDD initialization.

## Acceptance conclusion

T011 provides the executable verification readiness required after OP012 correctly exposed the staged-only assertion gap.

This acceptance does **not** activate Protocol `1.13.0`, does not merge PR #81, and does not by itself recover L001. After T011 acceptance is integrated, the T011 implementation may be integrated into `develop`; the PR #81 activation candidate must then be reconciled onto that current baseline and re-run through OP012 before D040 Phase B may merge.
