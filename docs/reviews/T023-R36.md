# T023-R36 — T062 v15 Stage 6 continuation authorization after fresh provider approval

Status: `STAGE6_CONTINUATION_AUTHORIZED / AWAITING_HUMAN_CODEX_CONTINUE`  
Date: 2026-09-12  
Authority: Human Owner / ChatGPT Orchestrator  
Task: `T062 / T023 v15 RIQ-NBC`  
Prior convergence review: `docs/reviews/T023-R35.md`  
Scientific evidence: `handoffs/T062-executor-handoff.json`

## Human approval consumed by this review

The terminal T062 handoff at remote scientific HEAD
`5a2a3effeabede6640d10a8aa80ae6f70008764c` records that the Human supplied the fresh explicit approval required by T023-R35 after the managed-safety disclosure.

The recorded approval is limited to the complete R35 envelope:

- destination: OpenAI Codex only;
- scientific cell: native Windows / GPT-5.6 Sol / Medium / Codex CLI exactly `0.149.0`;
- payload: frozen T062 v15 evaluation prompts plus only candidate/fixture/workspace content deliberately exposed by the frozen harness for backend/workspace/model preflight, synthetic canary and B2/F2/G3 acceptance;
- exclusions: no unrelated repository data, credentials, secrets, private tokens or payload outside the frozen evaluation envelope;
- usage/cost: existing OpenAI account/plan only, at most `1264` provider/model attempts total, comprising at most `4` synthetic-canary attempts and at most `1260` acceptance attempts, with the frozen `180s` timeout per attempt;
- commercial exclusions: no separate purchase, subscription/plan change, credential change or new billing arrangement.

The handoff classifies `human_authorization_required: false` and `orchestrator_persisted_continuation_authorization_required: true`. This review performs that required Orchestrator persistence. It does not broaden the Human approval.

## Scientific frontier revalidation

Remote verification establishes:

```text
scientific branch:
  test/t023-skill-activation-topology-evals-v15

previous R35 scientific HEAD:
  abeecd957b9a7c05cfb9b35c8f2c162373380af6

current scientific HEAD:
  5a2a3effeabede6640d10a8aa80ae6f70008764c

terminal Stage 5 scientific HEAD:
  3e0d0b71cf382db502e186622f40a23bcd915390

Candidate Freeze E:
  5b025087bc7b6996f683a34fdd1ce441d3d6dd82

Holdout/oracle Freeze F:
  5b8ac55980ecdbb6a2bf3784812b933647f2f13d
```

`5a2a3eff...` is exactly one commit above `abeecd957...`. That new commit modifies only `handoffs/T062-executor-handoff.json`.

The cumulative comparison `3e0d0b71... -> 5a2a3eff...` contains three commits and only one path: `handoffs/T062-executor-handoff.json`. No executable/scientific asset, frozen candidate/reference byte, harness, oracle, trial envelope, source/test/configuration path or committed Markdown changed on the scientific branch.

Provider/model calls remain exactly `0`. No backend/workspace/model behavioral preflight, synthetic canary, acceptance observation, scoring output or topology selection has occurred.

## D077 revalidation

Official `openai/codex` release metadata was refreshed on 2026-09-12 immediately before this authorization. Current stable remains:

```text
Codex stable: 0.154.0
release tag:  rust-v0.154.0
```

The R34/R35 baseline therefore has not advanced. T062 keeps disposition `PIN_RETAINED` at exact Codex CLI `0.149.0` because that version is a frozen scientific-cell variable and no provider-backed scientific execution has yet occurred.

The default installed `0.153.4` is not the authorized runtime. The exact cached `0.149.0` binary must be verified and used. If official stable advances beyond `0.154.0` before provider execution, stop for D077 reclassification.

## D076 boundary

D076 applies prospectively. Stage 6 may use small subordinate execution mechanics and may diagnose or boundedly repair the already-published harness without changing approved semantics.

If execution requires first-pass creation of a substantial controller, harness, oracle, semantic fixture generator, workflow state machine or equivalent material executable artifact, stop before creating it and return for Orchestrator Stage 5 re-entry. Any file-based ephemeral executable aid actually used must be classified in terminal handoff evidence.

## Continuation authority

T062 Stage 6 is now reauthorized for the same governed work unit:

```text
Executor:      Codex
Session:       CONTINUE
Coordinator-ID: AG | agent-governance | T062 | root-1
Runtime:       native Windows
Model:         GPT-5.6 Sol
Reasoning:     Medium
Codex CLI:     exactly 0.149.0
Starting HEAD: 5a2a3effeabede6640d10a8aa80ae6f70008764c
```

Before any provider/model call the Executor must reload current `origin/develop`, including this R36 and the current checkpoint, and revalidate:

1. remote scientific HEAD is still exactly `5a2a3effeabede6640d10a8aa80ae6f70008764c`;
2. the only cumulative Stage 6 path above `3e0d0b71...` remains the handoff;
3. Freeze E/F ancestry and integrity remain valid;
4. provider/model attempts remain exactly `0`;
5. native Windows / GPT-5.6 Sol / Medium / exact Codex CLI `0.149.0` is the actual live cell;
6. official stable Codex has not advanced beyond `0.154.0`;
7. the R35 Human approval envelope is used exactly as persisted here, without expansion.

After those gates pass, resume at the backend/workspace/model behavioral preflight. Then run the unchanged synthetic canary and require `2/2 PASS`; only then run the frozen independent B2/F2/G3 acceptance schedule within the T062 ceilings.

Do not modify frozen scientific semantics, attempt budgets, model, reasoning, runtime or CLI version. Do not select a topology. Do not reuse T063 observations as T062 scoring. Do not touch T058.

If the managed safety boundary rejects the provider-backed operation again despite R36, do not bypass it; return `BLOCKED` with exact rejection and actual attempt counts.

## Result

```text
T062 Stage 6:                   REAUTHORIZED
Scientific HEAD:               5a2a3effeabede6640d10a8aa80ae6f70008764c
Provider/model calls consumed: 0
Scientific observations:       0
Fresh Human provider approval: PERSISTED
D077:                          PIN_RETAINED at Codex CLI 0.149.0
Next action:                    Human-mediated Codex CONTINUE on root-1
```
