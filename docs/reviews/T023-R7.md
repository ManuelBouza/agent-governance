# T023 Review R7 — MG1-v8 host-capability preflight

Status: `BLOCKED / HOST_CAPABILITY_PREFLIGHT`  
Task: `T023`  
Controlling revision: `docs/tasks/T047-mg1-v8-cost-bounded-host-evaluation.md`  
Oracle: `MG1-T023-TOPOLOGY-ORACLE-v8`  
Execution epoch: `MG1-T023-EXECUTION-v8`  
Executor branch: `test/t023-skill-activation-topology-evals-v8`  
Submitted HEAD: `a00ce1d87de6a2c955f4080a6c539bf781369f0a`  
Implementation HEAD: `76a591c2d7a3f48d5e4b780712a78bef4c8e212f`  
Base: `develop@23475b7fbcf427c4dac8dcb06796409660434aa2`

## Decision

The MG1-v8 run is accepted as terminal diagnostic evidence for its frozen execution method. T023 remains `BLOCKED`; no topology is accepted or rejected from v8 candidate behavior because the mandatory host-capability preflight failed before any acceptance prompt was issued.

The submitted branch is valid evidence-only technical work for the blocked state. It starts from the exact canonical base, contains no committed Markdown authored by the Executor, does not modify the Orchestrator-owned oracle/corpus/trial-envelope/presentation assets, imports zero prior observations, and records zero v8 acceptance observations.

## Host-capability result

T047 requires a synthetic local Skill canary to pass twice under one permitted Codex sandbox before any acceptance prompt may run. `read-only` is tried first; `workspace-write` is permitted only as the fallback when the required body-read/use path is denied.

Observed result:

- `read-only` repetition 1: `FAIL`; every attempted `mx-canary/SKILL.md` body read was rejected by the Codex execution policy; the exact full body nonce was not recovered and host-observable body read/use was not established.
- `workspace-write` repetition 1: `FAIL`; required body-read commands were again rejected; no unexpected workspace mutation occurred.
- Because each permitted profile requires two passing repetitions, a first-repetition failure makes that profile incapable of satisfying the frozen preflight gate. No second repetition was required after either terminal first failure.
- Selected sandbox: none.
- Acceptance prompts issued: `0`.
- Scored observations: `0`.
- Selection: none; `BLOCKED / HOST_CAPABILITY_PREFLIGHT`.

The abbreviated model answer `quartz-heron` is not the canary body nonce. The frozen synthetic body nonce is `The quartz heron carries seven indigo pebbles at noon.`; the harness correctly records `body_nonce_correct=false`. Therefore the response does not prove an unobserved successful body load.

## Cost evidence

The v8 preflight achieved its intended cost-control purpose: the host defect was detected after only two non-scored synthetic provider calls and before the 40-case acceptance corpus.

Provider telemetry nevertheless shows a large fixed Codex context cost even under the reduced host surface:

- read-only canary: `82,593` input tokens, `61,184` cached input tokens, `1,149` output tokens, `577` reasoning-output tokens, five policy-rejected tool calls;
- workspace-write canary: `49,502` input tokens, `45,056` cached input tokens, `1,474` output tokens, `1,120` reasoning-output tokens, two policy-rejected body-read calls plus one unsuccessful patch attempt.

This confirms that the preflight gate prevents a potentially very expensive invalid acceptance run, while also showing that future host-envelope work should continue to minimize fixed Codex context/tool surface before another live epoch.

## Technical verification

Executor evidence records:

- frozen input validation: PASS;
- focused harness tests: 51 PASS;
- full deterministic suite: 456 PASS;
- profile isolation: 48 PASS;
- Consumer/source independence: 8 PASS;
- Ruff check/format: PASS;
- frozen D052 assets modified: false;
- prior epoch observations imported: 0;
- candidate/Core/runtime/profile semantic drift: none represented in the submitted diff.

The implementation adds the v8 canary, host-profile selection, minimal-surface controls, host-surface drift detection, 180-second attempt handling, cost telemetry, and deterministic futility/materiality scheduling. These technical changes may be integrated with the terminal evidence, but they do not establish live host compatibility or any T023 topology result.

## Classification and re-entry

Classification: `HOST_CAPABILITY_PREFLIGHT`, not candidate semantic failure, not capacity exhaustion, and not an acceptance-score result.

Re-entry: `Specify`.

Before a successor acceptance run, Orchestrator work must establish how the required Codex/native-Windows cell exposes a reproducible host-observable local-Skill body load under a bounded execution envelope. Do not respond by weakening the requirement that candidate-body activation be distinguishable from metadata discovery, and do not enable a broader execution surface merely to make the canary pass without a corresponding safety/reproducibility design.

A future host/runtime state may reuse the unchanged v8 canary only if it can satisfy the frozen preflight exactly; otherwise a prospective controlling revision is required. No v8 or prior acceptance observation may be imported into a successor score.
