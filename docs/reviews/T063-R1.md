# T063-R1 — Prelaunch Official-Codex Revalidation and Topology Classification Correction

Status: `ACCEPTED_PRELAUNCH_CORRECTION / EXECUTOR_NOT_LAUNCHED`  
Date: 2026-09-09  
Authority: ChatGPT Orchestrator  
Task: `docs/tasks/T063-adaptive-worker-routing-requalification.md`  
Research: `docs/research/R018-CODEX-SUBAGENT-RUNTIME-REVALIDATION.md`  
Provider/model calls: `0`

## Purpose

This review records the fresh official OpenAI/Codex revalidation requested before T063 launch and corrects one governance-classification inconsistency in the already-defined Task Contract.

It does not launch T063, freeze the final launch matrix, modify probes/oracles, or adopt adaptive worker routing as policy.

## Official revalidation conclusion

R018 freshly inspected current OpenAI Subagents/model/best-practice guidance, the official Codex release stream, and exact `openai/codex@rust-v0.153.4` stable source.

The evidence supports the existing two-stage architecture:

```text
Stage A — D075/D065
  decide local/direct vs delegated vs contract-fixed topology

Stage B — R007/T063
  only after worker execution is applicable, evaluate child model/reasoning profile
```

Official Codex `0.153.4` runtime guidance itself distinguishes immediate critical-path/tightly coupled local work from bounded independent sidecar work suitable for subagents. This is compatible with D075 plus D065 anti-triggers.

No D075 normative change is required.

## Correction — scored T063 topology is CONTRACT_FIXED

The original T063 wording repeatedly says the scored probes are `DELEGATED` under D075/D065.

That is true as **underlying semantic eligibility** but is not the exact D075 Stage-A execution-target label during the experiment.

T063 makes topology material by requiring:

- one fresh CONTROL child and one fresh ADAPTIVE child per probe;
- fixed matched-arm order;
- equivalent bounded context/tool/permission surface;
- exact profile and D063 measurement receipts;
- first-attempt scoring without child reuse.

D075 says topology that is materially fixed by a controlling Task Contract is `CONTRACT_FIXED`.

Therefore, for all scored T063 probe turns, this review prospectively controls the interpretation as:

```text
execution_target: CONTRACT_FIXED
underlying_delegation_eligibility: DELEGATED
```

Meaning:

- `CONTRACT_FIXED` explains **why the exact matched child topology must be used during T063**;
- `DELEGATED` records that the selected units are intentionally substantial/read-heavy enough that normal D065/D075 routing would independently consider worker delegation eligible rather than using trivial microactions solely to manufacture a subagent benchmark.

Where T063 says, in substance, "all scored probes are fixed DELEGATED units" or "Stage A is already DELEGATED", read that wording subject to this correction.

This correction changes no task content or score.

## T063 evidence correction

Terminal T063 evidence SHALL record both fields or their semantic equivalents:

```text
execution_target: CONTRACT_FIXED
underlying_delegation_eligibility: DELEGATED
```

The final evidence must not claim that the experimental matched topology was a spontaneous normal-routing decision by the coordinator.

AC-T063-3 is therefore interpreted as:

> All scored probes have `execution_target = CONTRACT_FIXED` because the evaluation topology is material, while each probe independently satisfies D075/D065 material-delegation eligibility; delegation-worthiness itself is not an experimental variable.

AC-T063-12 remains unchanged in intent: D075 Stage-A policy and R007 Stage-B adaptive-compute policy are separate claims.

## Current stable Codex runtime

At this review, the official stable Codex release is:

```text
codex-cli 0.153.4
```

This matches the D063 qualified reference runtime. `0.154.0-alpha.*` entries are pre-releases and are not preferred for the scored T063 run merely because they are newer.

The final launch review must still verify the actual native executable/App Server and D063 capabilities immediately before execution.

## Model-profile research disposition

Fresh official guidance supports the intended T063 profile hypotheses:

```text
root/CONTROL hypothesis: GPT-5.6 Sol / Medium
P1 ADAPTIVE hypothesis:   GPT-5.6 Luna / Medium
P2 ADAPTIVE hypothesis:   GPT-5.6 Terra / Medium
P3 ADAPTIVE hypothesis:   GPT-5.6 Terra / High
```

These values are **not frozen by this review** and are not a global routing policy.

Why Sol remains the preferred root hypothesis for this pilot:

- T054 used Sol/Medium as its CONTROL baseline;
- R010 still defers global Astra adoption;
- current Codex 0.153.4 can default to Astra when no explicit model is configured, so T063 must explicitly pin its root rather than accidentally change the experiment through a default;
- current OpenAI Subagents guidance continues to position GPT-5.6 as the normal starting point for demanding agent work and Terra/Luna for lighter bounded subagent classes.

The exact matrix is frozen only after a separate Human `launch T063` selection and immediate host/account capability revalidation.

## Runtime/profile exposure boundary

Stable `0.153.4` source supports model/reasoning spawn overrides and defaults Multi-Agent V2 override exposure to enabled, but the exposure is still configuration/runtime dependent.

Therefore T063 must preserve its existing fail-closed gates:

```text
D063 surface missing -> BLOCKED_MEASUREMENT_SURFACE
requested/resolved profile mismatch -> BLOCKED_PROFILE_RESOLUTION
```

Documentation or source availability cannot substitute for the live native preflight.

## Preserved boundaries

- T063 remains `NOT_AUTHORIZED_PENDING_SEPARATE_HUMAN_LAUNCH`.
- No Executor coordinator exists for T063 yet.
- No scored child was spawned.
- No provider/model call was issued.
- No adaptive routing decision is adopted.
- T062 remains on T023-R33 Human Hold; R32 remains unconsumed.
- T024 remains unauthorized.
- T058 remains frozen.

## Result

```text
fresh official revalidation: PASS
D075 conclusion: unchanged / supported
T063 design: valid subject to this topology-label correction
T063 scored topology: CONTRACT_FIXED
probe underlying eligibility: DELEGATED
current preferred stable runtime for launch review: Codex 0.153.4
Executor launch: NOT AUTHORIZED
provider/model calls: 0
```
