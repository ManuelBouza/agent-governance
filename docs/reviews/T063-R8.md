# T063-R8 — Canonical V4 Task Contract and Thin Transport Authority

Status: `ACCEPTED / STAGE6_AUTHORIZED_AWAITING_HUMAN_START`  
Date: 2026-09-11  
Authority: Human Owner / ChatGPT Orchestrator  
Task: `docs/tasks/T063-adaptive-worker-routing-requalification.md`  
Prior launch review: `docs/reviews/T063-R7.md`  
Task Contract template: `maintainer-skill/references/TASK-CONTRACT-V4-TEMPLATE.md`  
Template usage rule: `maintainer-skill/references/TASK-CONTRACT-TEMPLATE-USAGE.md`

## Finding

The prior T063 launch authority had drifted into an undesirable split: the active Task Contract still represented earlier/historical launch state while R7/O265 and the Human-visible prompt carried material v4 execution semantics.

That violates the repository's intended authority model:

```text
Task Contract = canonical executable work authority
review/checkpoint = convergence/frontier/authorization
transport prompt = thin pointer/bootstrap only
```

The long `root-4` prompt is therefore rejected as a transport artifact. Its substantive instructions have been normalized into the active T063 Task Contract or already-existing referenced Git authority.

## Canonical v4 contract

`docs/tasks/T063-adaptive-worker-routing-requalification.md` is now the complete active T063 v4 execution carrier. It contains the durable v4 candidate identity, reattachment barrier, frozen matrix/order, D063 evidence requirements, D076 boundary, D077 launch gate, scoring, pilot outcomes, stop/re-entry conditions, evidence paths and terminal return.

This normalization does **not** change the authorized scientific candidate:

```text
branch: test/t063-adaptive-worker-routing-requalification-v4
HEAD:   f06c8f48f7b1d59dff9fc117cca5b42453ad23e8
base:   9da2b6fed64ded9af8d38f67cd53cd066abef838
```

It does not authorize additional provider calls, change probe/oracle semantics, change the six-arm matrix/order, alter D063 qualification, or alter the v4 same-child retry design accepted by R7/R022.

## Template adoption

The generalized v4 Task Contract structure is persisted in the Maintainer Skill package:

```text
maintainer-skill/references/TASK-CONTRACT-V4-TEMPLATE.md
maintainer-skill/references/TASK-CONTRACT-TEMPLATE-USAGE.md
```

For every new or materially revised source-product executable Task Contract, the future Maintainer Skill Orchestrator route must use that template as its structural starting point/readiness checklist. Historical contracts remain historical until materially revised for new execution authority.

The Maintainer Skill remains `DESIGN-APPROVED / NOT YET RELEASED`; this review does not bypass its existing `SKILL.md` implementation/release gate. The package references and mandatory future-use requirement are materialized now so the final Skill cannot omit them.

## Thin transport rule

For T063 v4, the Human-visible transport prompt is limited to:

- exact Coordinator title;
- canonical repository;
- NEW session indicator;
- Task Contract path;
- exact authorized candidate branch@HEAD;
- instruction to synchronize/load canonical Git authority and execute the Task Contract;
- instruction to return only the Task Contract-defined terminal result.

D055 model/reasoning/session launch profile is presented to the Human separately from the transport prompt.

No experiment matrix, retry logic, evidence schema, detailed exclusions, version runbook, acceptance criteria or stop rules are to be duplicated into transport.

## Verdict

```text
T063 v4 Task Contract:        CANONICAL / COMPLETE
R7 scientific authorization: PRESERVED
v4 candidate HEAD:            UNCHANGED
provider/model calls here:    0
long root-4 transport prompt: REJECTED
thin transport:               REQUIRED
global R007 policy:           UNCHANGED / EVALUATING
```

Stage 6 remains authorized only through the canonical T063 Task Contract and exact v4 candidate. Human-mediated launch under D071 remains required.
