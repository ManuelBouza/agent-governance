# T023-R29 — v15 RIQ-NBC Design / Plan & Trace

Status: STAGE4_PLAN_TRACE_COMPLETE / READY_FOR_STAGE5  
Date: 2026-09-08  
Owner: ChatGPT Orchestrator  
Task: T062  
Strategy: RIQ-NBC

## Result

SDD Stage 3 Design and Stage 4 Plan & Trace are complete for the T023 v15 reference-independent topology evaluation.

The Human `go` selecting continuation of the T023 line authorizes continuation through Orchestrator-owned Stage 5 under the integrated plan. It does **not** authorize D068 Stage 6, an Executor launch, a synthetic canary, or any provider/model call. Stage 6 still requires a separate future Human-selected launch after Stage 5 readiness is durably reviewed.

## Canonical authoring baseline

Design/Plan authoring was bootstrapped and validated from:

`develop@468c708df31e53ad69057213e6e462495d63d7ca`

D061 pre-write gate:

- topic branch `docs/t023-v15-design-plan` created from that exact SHA;
- exact branch/base relationship verified before first content mutation;
- all authoring writes explicitly target the topic branch;
- post-first-write `develop` remained exactly `468c708df31e53ad69057213e6e462495d63d7ca`.

D062 effective server-side control was revalidated from GitHub:

```text
ruleset id: 22339910
name: Protect long-lived branches
enforcement: active
targets: refs/heads/main, refs/heads/develop
pull request required: true
deletion blocked: true
non-fast-forward blocked: true
bypass actors: none
current connected actor bypass: never
```

## Executable contract

New Task Contract:

`docs/tasks/T062-t023-riq-nbc-v15-reference-independent-evaluation.md`

T062 is the Stage 5 authority after this review/checkpoint package is integrated on `develop`.

T062 is not Stage 6 launch authority.

## Scientific branch and contamination boundary

The prospective branch is:

`test/t023-skill-activation-topology-evals-v15`

It MUST be created in Stage 5 from the then-current protected `develop` HEAD, not from the historical scientific branch.

The clean historical Stage 5 head:

`aea43441a424fe18003176cb05b5594b8b561a68`

is an explicit blob/provenance source only.

The provider-backed v14-r1 terminal:

`67884f52912aeb51821d8f7e8ae7753c40b608fc`

is neither an authorized ancestor nor an import source. Historical `evidence/**`, old handoffs and provider-backed outputs are excluded from v15 materialization.

This design prevents provider-backed evidence from entering the new scientific branch while preserving byte-equivalent candidates through explicit Git provenance.

## Fixed identities

```text
strategy:              RIQ-NBC
evaluation:            MG1-T023-EVALUATION-v15
candidate hashes:      MG1-T023-CANDIDATE-HASHES-v3
capability source:     MG1-2026-09-06-v4
topology metadata:     MG1-T023-TOPOLOGIES-v4
presentation:          MG1-T023-PRESENTATIONS-v5
corpus:                MG1-T023-CORPUS-v9
oracle:                MG1-T023-TOPOLOGY-ORACLE-v15
execution:             MG1-T023-EXECUTION-v15
trial envelope:        MG1-T023-TRIAL-ENVELOPE-v3
candidate freeze:      Freeze E
holdout/oracle freeze: Freeze F
```

## Exact byte provenance

The historical candidate manifest at the clean Stage 5 head is:

```text
path: evals/skill_activation_topology/candidate-hashes-v14.json
Git blob: f78b587b76ef0c06669b15fa4d0e85b393cab5b0
identity: MG1-T061-CANDIDATE-HASHES-v2
```

Direct Git reads confirmed the source Git blobs for all nine fixed files:

```text
B2/agent-governance/SKILL.md          1558c74a524b399b100b05dd6f1041011b8e1949
F2/consumer-governance/SKILL.md       64a16f403713bde80a8ebebcda9e31dbaa8fea53
F2/source-maintainer/SKILL.md         13f064d186af528557d1e389e271aaa684122eb1
G3/consumer-lifecycle/SKILL.md        6b0261a2c677f72eb7efd88ad442eeb3b4b15bd8
G3/source-maintainer/SKILL.md         e00edee8b1a1bacbb47b01bad4492f8ddf954628
G3/external-skill-trust/SKILL.md      308ea420ab9740b3c574a87cc436a5ea8b90f0c6
shared/consumer-lifecycle.md           f3898d0770486e023d50d48e89091be8033fceec
shared/source-maintainer.md            68e5bb75bcff20a72638b22514e0a1ba68c6f8ac
shared/external-skill-trust.md         737223e1b61b1c9efc6e10264735355ad1576059
```

T062 carries the corresponding nine exact SHA-256 values and v5-to-v3 copy-equivalence mapping. No wording change is permitted.

## Freeze architecture

### Freeze E — candidate/provenance

Stage 5 first materializes only candidate/reference bytes, candidate-hash v3, required preserved topology/presentation/capability metadata and provider-free candidate-integrity mechanics.

The fresh holdout identities MUST NOT be exposed before Freeze E.

Freeze E is committed/published, then verified from GitHub for:

- fresh-`develop` ancestry;
- explicit source provenance from `aea43441...`;
- nine Git-blob/content equivalences and SHA-256s;
- F2/G3/shared copy-equivalence to v3;
- no provider-backed evidence/handoff import;
- no v9/v15 holdout identity leakage.

### Freeze F — fresh holdout/oracle

Only after remote Freeze E verification, Stage 5 materializes:

- corpus v9;
- oracle v15 / execution v15;
- trial-envelope v3;
- holdout-integrity guard.

Freeze E -> Freeze F cannot change candidates, candidate hashes, presentation/topology metadata or capability-source semantics.

Any later Stage 5 harness adaptation is mechanics-only and cannot drift frozen semantic inputs.

## Corpus v9 design

The fresh corpus remains 70 cases with FAR denominator 40:

```text
positive-consumer 6
positive-source-maintainer 6
positive-external-skill-trust 6
negative 10
near-miss 30
ambiguous 4
cross-profile 4
multi-intent 4
```

The five near-miss axes remain six cases each.

Every v15 prompt is new after Freeze E. Provider-free holdout integrity must assert exact prompt overlap `0` against every prior exposed T023 acceptance corpus required by the contract, at minimum:

- v12 `3e5bec392d0b8e5804c4efaad74b795b08dc9779`;
- v13 `d0ebe46a68c02c66dcfbb21c3dfaee43fb15c27f`;
- v14/v14-r1 corpus v8 at `aea43441a424fe18003176cb05b5594b8b561a68`, with Freeze D lineage `f8cee7c72688f0486211474d2678d018772e2a58`.

A required historical corpus that cannot be resolved is a holdout-integrity failure, not permission to omit the check.

## Scheduler and budget design

After common pre-acceptance gates and canary, candidate scheduling is independent.

Audit/interleaving order:

```text
for r1, then r2:
  ordered case
    B2 -> F2 -> G3
then r3 only for unstable candidate/case pairs
no r4
```

B2 qualification does not gate F2/G3 scheduling. B2 completes its entire frozen measurement even if interim science is non-qualifying. Exact scientific futility may stop only the affected F2/G3 candidate; global integrity/host/oracle/evidence failure stops the epoch.

Budget:

```text
per candidate base: 140 valid acceptance observations
per candidate max:  210
all candidates base: 420
all candidates max:  630
max model attempts per scheduled observation: 2
acceptance attempt ceiling: 1260
synthetic canary: 2 valid observations, max 4 attempts
Stage 6 absolute provider/model attempt ceiling including canary: 1264
per-attempt timeout: 180s
```

Stage 5 provider/model calls remain exactly `0`.

## Selection design

All B2/F2/G3 candidates first face the unchanged absolute gates.

If B2 qualifies, the existing relative materiality rule applies to split candidates.

If B2 is validly measured but scientifically non-qualifying, B2 is ineligible and non-blocking; an absolute-qualified split candidate must satisfy D074 admissibility dominance. The relative F1 `+0.03` condition is removed only in this regime; absolute F1 `>= 0.95` remains.

If multiple split candidates remain eligible, preserve the specified deterministic tie-break:

1. higher F1;
2. lower FAR;
3. lower median observed context;
4. fewer entrypoints;
5. exact tie -> F2.

If B2 measurement is technically/epoch invalid, fail closed: no scientific non-qualification regime and no topology selection.

## Verification design

Orchestrator-owned D052 provider-free conformance consists of:

- candidate guard for exact identities/provenance/bytes/copy-equivalence/contamination boundary;
- holdout guard for Freeze ancestry, corpus geometry/freshness, frozen semantic path boundary, scheduler, thresholds, selection regimes and budgets;
- characterization tests for frozen input loading and independent scheduler semantics.

A separately Human-launched Stage 6 must run deterministic verification before any model/provider call:

1. candidate guard;
2. holdout guard;
3. ruff check;
4. ruff format check;
5. full pytest;
6. required code-health/symbol-map checks;
7. characterization tests;
8. native Windows backend/workspace/version preflight;
9. synthetic canary 2/2;
10. B2/F2/G3 acceptance schedule.

Future Executor handoff path is `handoffs/T062-executor-handoff.json`.

## Risk and rollback

Primary risks:

- accidental inheritance of provider-backed v14-r1 evidence;
- candidate-byte drift during fresh branch materialization;
- holdout leakage before candidate Freeze E;
- incomplete historical prompt-exclusion set;
- scheduler logic accidentally restoring B2 gating;
- conflating a technical B2 invalidity with scientific non-qualification;
- provider-call budget interpreted as observations rather than attempts.

Controls are frozen in T062 and provider-free guards.

Before Freeze E, a defective v15 branch may be abandoned. After Freeze E/F, freezes are immutable; defects require a successor iteration/branch/epoch, never history rewriting.

## Human / ownership boundaries preserved

- no candidate or holdout bytes were materialized by this Design/Plan review;
- provider/model calls during Stage 3/4: `0`;
- no Executor was launched;
- Stage 5 remains Orchestrator-owned;
- Stage 6 remains separately Human-gated and Executor-owned;
- T024 remains unauthorized;
- D066 intentional gaps remain unchanged;
- T058 remains frozen;
- historical scientific branches remain immutable.

## Stage 5 readiness

Once T062, R29 and the matching O254 checkpoint are integrated on `develop`, Stage 5 Candidate Materialize is authorized by the Human-selected continuation already recorded for this objective.

Stage 5 MUST begin with a fresh `develop`/D061/D062 revalidation and branch creation from that exact new canonical HEAD.

No Stage 6 authority is created by this review.