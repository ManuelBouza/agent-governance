# T023 Review R9 — MG1-v10

Status: `BLOCKED / EXECUTION_ADAPTER_TRACE_CLASSIFICATION_DEFECT`

Date: 2026-09-02

Owner: ChatGPT Orchestrator

## Scope reviewed

- Task: `T023`
- Controlling revision: `docs/tasks/T049-mg1-v10-windows-workspace-acl-compatible-restart.md`
- Canonical base: `a8660573bf9319c065f7ed0adc61ed262989b06e`
- Submitted Executor branch: `test/t023-skill-activation-topology-evals-v10`
- Submitted HEAD: `bfabf4e4d40a2e64c78cc4c2aff7ffe2aa907594`
- Executed live-runner commit: `cd0f97b0022176efdabe34e7d7142ff3344fa841`
- Reviewed implementation HEAD: `3dfe8cf94b3dea7cdee35cd2fcec84cf027b6c09`
- Evidence/infrastructure integration PR: `#254`
- Integration merge: `c0c29cd7f338b3395b7b7f06955265018e030b5b`

## Accepted technical findings

V10 corrected the V9 workspace-ACL confound.

The selected complete host profile was:

- Codex CLI `0.149.0`;
- native Windows 11;
- native backend `unelevated`;
- logical sandbox `read-only`;
- workspace profile `windows-inherited-acl-mkdir-v1`;
- GPT-5.6 Sol / Medium;
- user config and execpolicy rules ignored;
- dangerous bypass and interactive approval absent.

The provider-free workspace gate passed with exact-root enumeration/readability and exact nonce while issuing zero provider/model calls.

The unchanged synthetic Skill canary then passed `2/2` with exact body nonce and host-observed successful `SKILL.md` reads.

The deterministic gates were green:

- Ruff check PASS;
- Ruff format check PASS;
- full pytest PASS: `470` tests;
- focused harness tests PASS: `65` tests;
- full deterministic regression PASS;
- profile-isolation regression PASS;
- Consumer/source-independence regression PASS.

## Terminal defect

Exactly one acceptance prompt was issued: `WX01 / B0 / r1 / a1`.

The raw host trace shows successful `Get-Content` reads of the candidate `SKILL.md` and required reference files with `exit_code=0`.

The executed runner nevertheless classified the attempt as `HOST_SURFACE_DRIFT / REQUIRED_SKILL_BODY_READ_REJECTED` because its access-rejection detector matched ordinary policy text contained inside successful command output rather than restricting rejection detection to failed relevant command events.

This is an Execution Adapter trace-classification defect. It is not candidate behavior and does not establish a topology result.

The Executor stopped new scheduling and performed no retry. The affected observation was not scored.

Technical review subsequently corrected the extractor and added regression coverage, but that corrected runner is not the immutable runner that exposed the holdout prompt.

## Acceptance decision

- Acceptance prompts issued: `1`.
- Scored observations: `0`.
- Selection performed: `false`.
- No candidate/topology result is accepted or inferred.
- `WX01/B0/r1/a1` MUST NOT be retroactively rescored from the corrected extractor.
- V10 MUST NOT resume under a different runner identity without new persisted Orchestrator authority.

V10 is therefore closed `BLOCKED / EXECUTION_ADAPTER_TRACE_CLASSIFICATION_DEFECT`.

Re-entry stage: `Plan & Trace`.

## Code-health finding

The submitted V10 `evals/skill_activation_topology/harness.py` is approximately `3,133` lines, up from approximately `2,864` lines at the V10 canonical base: about `+269` net lines in an already oversized module.

This is not the cause of the V10 blocker, but it is now a material maintainability and agent-context risk. Additional MG1 execution-method work MUST NOT continue growing this monolith without a prospective code-health/refactoring control.

The next executable maintenance work should establish a code-size ratchet and modularize the harness while preserving behavior before another MG1 acceptance epoch is launched.

## Required next boundary

1. Preserve V10 evidence and the unscored exposed observation unchanged.
2. Do not relaunch T023 immediately.
3. Integrate a code-health/refactoring Task Contract that preserves MG1 semantics while decomposing the harness and adding mechanical size/complexity guardrails.
4. After that refactor is accepted, define any prospective successor MG1 execution epoch required for the trace-classifier correction and holdout-exposure boundary.
