# T063-R11 — V6 Stage 5 Readiness Review

Status: ACCEPTED / STAGE6_AUTHORIZED_AWAITING_HUMAN_START  
Date: 2026-09-11  
Task: `docs/tasks/T063-adaptive-worker-routing-requalification.md`  
Prior convergence: `docs/reviews/T063-R10.md`  
Research: `docs/research/R023-T063-V5-LIVE-SPAWN-RECEIPT-PERSISTENCE-GAP.md`

## Candidate reviewed

```text
branch: test/t063-adaptive-worker-routing-requalification-v6
HEAD:   af2de380569285b63e33de3f53628cadcf4052b6
base:   d5ff447d3ad162ddfc8afd8baadeac67154d7a6a
```

Remote comparison verifies that the candidate is exactly one commit ahead of the recorded protected-base snapshot.

The candidate contains 20 added executable/test files relative to that base:

- the accepted v3 core harness package and its harness test;
- the accepted repaired v4 adapter and its adapter test;
- the accepted v5 model-evidence adapter and its evidence test;
- the new v6 package (`__init__.py`, `__main__.py`, `runner.py`);
- the new v6 parent-surface regression test.

The promoted v3/v4/v5 files were materialized from their exact accepted v5 candidate blobs rather than rewritten. In particular, the accepted repaired v4 blobs remain:

```text
evals/adaptive_worker_routing_v4/runner.py
  9d1703dbe9c6fc0000112da2c9775f6e3c76b8d8

tests/test_t063_adaptive_worker_routing_v4_adapter.py
  e1cc19f22cddcbf1c73e89c1cb1e85fcc4206cb6
```

No v5 terminal telemetry/handoff evidence was promoted into the v6 candidate.

## V6 materialized correction

The v6 adapter wraps the accepted v5 stack and changes only parent-surface measurement.

At live spawn correlation time it captures:

```text
exact App Server client
exact parent thread id
exact parent turn id
notification start index
exact public spawn item id
exact child id returned by the existing v3 public correlation
```

At parent validation it inspects only public `item/started` / `item/completed` notifications for that exact parent thread/turn and:

1. deduplicates Started `subAgentActivity` representations by public item ID;
2. requires exactly one unique Started activity matching the exact child/task;
3. rejects another/different Started activity;
4. rejects conflicting representations of the same logical item;
5. rejects the same forbidden parent item types in the live window and completed snapshot;
6. preserves the exact final `PARENT_SPAWNED` requirement;
7. permits the completed snapshot to omit the already-observed live Started activity;
8. rejects a completed snapshot that contradicts the live child/task correlation.

The adapter does not use internal/raw response events and does not change v4 reattachment or v5 model-evidence semantics.

## Deterministic regression coverage

The new v6 regression asset covers:

- the exact v5 failure shape: live `item/started` + `item/completed` for one logical activity while the completed parent snapshot contains no `subAgentActivity`;
- deduplication of those two notifications by item ID;
- rejection of a second unique Started activity;
- filtering of notifications from another parent/turn;
- missing exact activity;
- wrong child/task;
- forbidden live parent activity;
- forbidden completed parent activity;
- contradictory completed activity;
- final parent response drift.

The new Python material was parsed successfully with Python AST before publication.

## Orchestrator execution limitation

A fresh local clone of the published v6 branch was attempted for provider-free execution, but this environment again could not resolve `github.com`.

Therefore this review does **not** claim local pytest, Ruff or compileall success.

That limitation is fail-closed by the Task Contract: Stage 6 must execute every deterministic provider-free gate successfully before the first provider/model call. A failure there terminates v6 with zero new provider calls.

## D076 review

The parent-surface adapter is substantial task-specific executable measurement logic and therefore was correctly materialized by ChatGPT during Stage 5 rather than delegated to the Executor as an ephemeral Stage 6 aid.

Stage 6 remains limited to execution, diagnosis, bounded represented repair and verification. Any material replacement harness/controller discovered later requires re-entry.

## D077 launch review

Official Codex release state revalidated on 2026-09-11:

```text
qualified pin:              0.153.4
latest stable:              0.154.0
newest observed prerelease: 0.155.0-alpha.3.9
newer stable than 0.154.0:  none
```

R023 established that 0.154.0 retains the relevant live-item/history persistence distinction and does not repair the v5 measurement assumption. D063 qualification does not automatically extend to 0.154.0.

Disposition:

```text
PIN_RETAINED
```

If a stable Codex release newer than 0.154.0 appears before the first v6 provider call, execution must stop for D077 relevance classification.

## Scientific continuity

The v6 run remains a clean successor. Historical v1-v5 evidence is excluded:

```text
v1  3d8a9460988351383a90adfc6b76e2deff056504
v2  3ff745a8d29e031ca818c1bc618b15a54e0cbf2b
v3  746519abc6f159e959120f68d5c9f920d88d5797
v4  4135a13ce8daa4f6b1fcabe45063364fbbdd16f1
v5  3f9830a65a152ad595653961205e0ca52b9c5ccc
```

The root remains an experimental constant (`gpt-5.6-sol / medium`). The worker matrix, arm order, probes/oracles, config-authoritative child contract, first-attempt scoring and model-evidence taxonomy remain unchanged.

## Readiness decision

Stage 5 is complete and the exact candidate above is authorized for Human-mediated Stage 6 launch, subject to the Task Contract's pre-provider gates.

Launch continuity:

```text
Executor:       Codex
Session:        NEW
Coordinator-ID: AG | agent-governance | T063 | root-6
Root model:     gpt-5.6-sol
Root reasoning: medium
Runtime:        exactly 0.153.4
App Server:     exactly 0.153.4
Auth:           chatgpt
```

No provider/model call was consumed during v6 Stage 5/readiness work.
