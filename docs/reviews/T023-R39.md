# T023-R39 — T064 Freeze G methodological failure and v17 successor re-entry

Status: ACCEPTED / T064 TERMINATED BEFORE HOLDOUT / T065 AUTHORIZED_FOR_STAGE5  
Date: 2026-09-12  
Authority: Human Owner / ChatGPT Orchestrator  
Scope: T023 selective capability routing Stage 5

## Finding

T064 published its first candidate/analysis boundary as:

```text
branch: test/t023-selective-capability-routing-evals-v16
Freeze G: 2a1742b04af589166da6bf1bab9a74d0429af1d9
base develop: 73c57208bb4363fa994ee6132293b2312cc33320
provider/model calls: 0
scientific observations: 0
confirmatory holdout authored: no
```

Remote verification confirmed that the v16 confirmatory corpus did not exist at Freeze G.

Before any Freeze H materialization, provider/model call, or scientific observation, Orchestrator review found two material evaluation defects in the published Stage 6 harness design.

### F1 — routing instrumentation could contaminate the router

The Freeze-G Codex output schema explicitly enumerated canonical routing capabilities and required execution-oriented fields such as `task_success` during routing-only trials.

Because the structured schema is model-visible execution context, those labels can prime the very routing decision being measured and the `task_success` requirement can encourage specialist execution during a stage intended to isolate routing.

This violates the intended D078 separation:

```text
routing qualification
before
specialist execution
```

and weakens reference independence of host-native routing observation.

### F2 — conditional execution estimand was too weak

The Freeze-G end-to-end analysis treated disposition equality as the condition for `P(execution success | routing correct)`.

Correct routing under T064 requires the exact topology-independent capability set and its topology-specific activation projection, not merely matching `ROUTE`, `NONE`, or `ABSTAIN`.

The conditional execution estimand therefore must be conditioned on exact routing correctness.

## Disposition

These are material methodology defects, not technical implementation defects that may be silently repaired after a published Freeze G.

Per T064 rollback/fail-closed rules:

- do not rewrite Freeze G;
- do not author Freeze H for T064;
- do not execute T064 provider/model work;
- preserve the failed epoch as evidence;
- open an explicit successor epoch.

T064 is therefore terminal `FAILED_STAGE5_METHODOLOGY_BEFORE_HOLDOUT`.

No experimental result was consumed and no candidate received preferential evidence.

## Retained controls

The successor may retain without statistical penalty because no confirmatory observation occurred:

- D078 capability taxonomy and `ROUTE/NONE/ABSTAIN` semantics;
- exact B2/F2/G3 candidate bytes from historical pre-holdout Freeze E;
- topology mapping `MG1-T023-TOPOLOGIES-v4`;
- 90-case development boundary;
- 270-case fresh routing-confirmatory geometry;
- 30-case repeated-measure reliability subset;
- disjoint 60-case end-to-end reserve;
- corpus SLO versus inferential-interval distinction;
- paired comparison, Holm correction, non-inferiority margin `-0.02`, and context materiality ratio `0.85`;
- maximum two end-to-end finalists;
- absolute Stage 6 attempt ceiling `1264`.

No T064 prompt/holdout exists to reuse because Freeze H was never authored.

## Required v17 correction

The successor harness must satisfy all of the following before its candidate/analysis freeze:

1. routing-only evaluator instructions and structured schema are domain-neutral: they must not enumerate Agent Governance capability or entrypoint labels;
2. routing-only schema must not require task-success or specialist-execution judgments;
3. observed entrypoint activation and observed capability loading are determined from host trace/body-read evidence, not model self-report;
4. routing disposition is derived from trace plus a generic clarification signal: non-empty observed activation/capability -> `ROUTE`; empty plus clarification -> `ABSTAIN`; otherwise -> `NONE`;
5. routing-only trials use a neutral planning/no-execution suffix so full specialist mission execution is not required to score routing;
6. e2e execution uses a separate phase-specific output contract;
7. `P(execution success | routing correct)` uses exact routing correctness, including exact capability set and topology activation set;
8. behavioral preflight and synthetic canary have explicit pass/fail semantics rather than only process-return validity;
9. candidate routing trace remains the scoring authority if structured self-report conflicts with host trace.

## Successor

Allocate:

```text
Task: T065 / T023 v17
Branch: test/t023-selective-capability-routing-evals-v17
Freeze I: candidate / analysis / corrected harness
Freeze J: fresh confirmatory holdout / oracle / trial envelope
```

T065 is Stage-5-only until a later readiness review. No Executor or provider/model use is authorized by this review.

## Frozen adjacent state

- T064 Freeze G remains immutable failed evidence.
- T062/v15 remains frozen unconsumed.
- T063 remains closed/frozen `NOT QUALIFIED`.
- T058 remains frozen.
- T024 remains blocked until T023 selects a topology.
