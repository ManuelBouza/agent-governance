# T033-R1 — Native Windows Portability Baseline review

Review ID: `T033-R1`  
Task: `T033`  
Disposition: `REWORK_REQUIRED`  
Date: 2026-08-22  
Reviewed base: `develop@c581d25d3cfb162d3c4e13838db2af3d1fb3887f`  
Submitted executor HEAD: `e97ad4f7593548ad3b6277f265a41be48d812133`  
Submitted implementation anchor: `4f5bc9d7ec7ce9c6aef68c6a23db9eb6fe5860ef`

## Scope and identity review

The submitted branch `fix/t033-native-windows-portability-baseline` is two commits ahead and zero behind the reviewed base, with merge-base exactly `c581d25d3cfb162d3c4e13838db2af3d1fb3887f`.

The net diff contains only authorized non-Markdown implementation/configuration/test/handoff paths:

- `.gitattributes`;
- `src/agent_governance/artifact.py`;
- `src/agent_governance/engine.py`;
- `tests/conftest.py`;
- `tests/test_consumer_governance_cli.py`;
- `tests/test_consumer_governance_cli_v1.py`;
- `tests/test_governance_artifact.py`;
- `tests/test_repository_context.py`;
- `tests/test_shared_governance_engine.py`;
- `handoffs/T033-executor-handoff.json`.

No committed Markdown blob changed. The handoff records implementation anchor `4f5bc9d7ec7ce9c6aef68c6a23db9eb6fe5860ef`; submitted HEAD `e97ad4f7593548ad3b6277f265a41be48d812133` is the handoff-only successor.

## Acceptance criteria

### AC-T033-1 — PASS

The submitted implementation removes the unconditional Windows import failure while retaining POSIX `flock` where available and adding a fail-closed Windows mutex backend. The handoff reports focused backend/error controls and repeated concurrent append verification, and the full native-Windows suite reached execution rather than failing at `fcntl` import.

### AC-T033-2 — PASS

The added root `.gitattributes` declares `* text=auto eol=lf`. The handoff reports a disposable native-Windows checkout with `core.autocrlf=true` yielding `i/lf w/lf` and `eol=lf` for representative measured/formatted paths, without a workstation-global Git setting change or committed Markdown blob normalization.

### AC-T033-3 — PASS for LF fixture repair

The submitted fixture changes explicitly suppress platform newline translation where canonical LF bytes are required. The reported repository-context, canonical byte/digest, and full-suite results no longer exhibit the CRLF byte-count/canonicalization failures that motivated T033.

### AC-T033-4 — PASS

Artifact tree traversal and inventory ordering are now keyed by canonical relative POSIX strings instead of platform-specific `Path` ordering. Focused regression coverage includes a case whose expected ordering differs from Windows case-folded `WindowsPath` comparison.

### AC-T033-5 — FAIL

The handoff reports the canonical native-Windows suite as:

```text
318 passed, 7 skipped
```

Those seven skips are not pre-existing host-declared exclusions. The submitted implementation changes existing Consumer/bootstrap security negative controls by introducing helpers that catch Windows `WinError 1314` during symlink creation and call `pytest.skip(...)`.

This conflicts directly with the T033 contract:

- the explicit exclusions prohibit weakening/removing deterministic assertions merely to make Windows pass;
- the stop/escalation conditions explicitly state that if implementation would require **skipping failing tests**, the executor must stop rather than normalize that into acceptance;
- the authorized test/fixture portability scope was for canonical LF construction and focused portability coverage, not for reducing existing unsafe-symlink negative-control execution.

A suite made green by converting seven existing negative controls into host-capability skips therefore does not satisfy AC-T033-5, even though the four originally diagnosed portability classes appear technically repaired.

## Required T033 rework

T033 must remain on the existing `fix/t033-native-windows-portability-baseline` branch and preserve the accepted portions of the submitted portability implementation unless a correction requires otherwise.

The rework must remove the acceptance weakening introduced by `symlink_or_skip`-style handling. The native-Windows verification path must execute evidence equivalent to the existing unsafe-symlink/collision/source-marker negative-control claims rather than reporting them as skipped solely because the host lacks ordinary symbolic-link creation privilege.

The executor owns the technical mechanism. It may use a platform-specific faithful fixture/test technique, a narrowly scoped verification mechanism, or another deterministic approach, provided that:

- the existing security/fail-closed claim is actually exercised on native Windows;
- POSIX coverage is not weakened;
- no permanent Full Access, Windows security weakening, global workstation configuration change, or Codex-specific repository dependency is introduced;
- no Markdown, T021, RCAB semantics, Governance Core semantics, accepted digest/byte expectations, or unrelated scope is changed;
- the final canonical verification has no T033-introduced skips for these existing negative controls.

If the executor cannot produce semantically equivalent native-Windows coverage without changing the accepted product/security semantics, it must return `BLOCKED` rather than skip the claim.

After correction, rerun the complete T033 verification matrix, persist an updated terminal handoff, commit the complete authorized rework, perform the normal final push, and return only the canonical completion fields.

## Disposition

T033 is **REWORK_REQUIRED**.

No implementation PR may be accepted or merged from submitted HEAD `e97ad4f7593548ad3b6277f265a41be48d812133`.

The authoritative rework instructions are this review plus the unchanged T033 Task Contract. Chat transport must not add implementation semantics beyond pointing the executor to those persisted authorities.
