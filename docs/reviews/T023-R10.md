# T023 Review R10 — MG1-v11

Status: `BLOCKED / EXECUTION_ADAPTER_SCHEDULER_DEFECT`

Date: 2026-09-03

Owner: ChatGPT Orchestrator

## Scope reviewed

- Task: `T023`
- Controlling revision: `docs/tasks/T051-mg1-v11-post-v10-holdout-rotation-restart.md`
- Canonical base: `950972590746c3637ddb96002e4bb7bb988b28a5`
- Submitted Executor branch: `test/t023-skill-activation-topology-evals-v11`
- Submitted HEAD: `d05c3033fd597480f5e5f105176684eed68385c5`
- Executed live-runner commit: `4b0fd7a4ee53b99d9ca9627e5fca524a0a9a3d51`
- Reviewed implementation/evidence HEAD: `2b6dfba40397c7cc758c38b313dd0c7c5ba09bd6`
- Final handoff: `handoffs/T023-executor-handoff.json`

The submitted terminal HEAD differs from the reviewed implementation/evidence HEAD only by the final handoff artifact. The reviewed implementation/evidence HEAD differs from the immutable executed runner only by persisted run evidence; no post-exposure scheduler correction was applied to the v11 runner.

## Accepted technical findings

V11 successfully reached the intended live acceptance path.

Deterministic pre-provider gates passed, including:

- Ruff check and format check;
- deterministic code-health size/complexity/dependency/cycle checks;
- deterministic AST symbol map;
- v11 oracle/corpus harness validation;
- focused T023/T050 characterization/code-health tests: `81` PASS;
- full pytest: `486` PASS;
- exact holdout-rotation proof: `39` cases unchanged, v10-exposed `WX01` retired, fresh `WX00` added with preserved semantic fields;
- zero prior acceptance observations imported;
- frozen presentation/topology/trial-envelope assets unchanged.

The accepted T050 architecture remained intact. The `harness.py` facade remained at its accepted `212`-line ratchet and no replacement monolith was introduced.

The provider-free workspace gate passed under the selected complete profile:

- Codex CLI `0.149.0`;
- native Windows 11;
- native backend `elevated`;
- logical sandbox `read-only`;
- workspace profile `windows-inherited-acl-mkdir-v1`;
- GPT-5.6 Sol / Medium;
- user config and execpolicy rules ignored;
- dangerous bypass, interactive approval, broad ACL grants and parent ACL mutation absent.

The unchanged synthetic Skill canary then passed `2/2` with exact body nonce and successful host-observed `SKILL.md` reads.

## Terminal scheduler defect

V11 issued exactly two fresh acceptance prompts, both for the first scheduled case/candidate pair `WX00/B0`, repetitions `r1` and `r2`. Both produced valid structured observations and successful host-observed candidate-body/reference reads.

No result from those observations is accepted or used for topology inference in this review.

After the base pair completed, `RunContext._conditional_third()` invoked `conditional_third_specs(inputs, [candidate], trials)` using the full corpus. The helper iterates every corpus case and requires each supplied case/candidate pair to already contain repetitions `1` and `2`. Adaptive execution, however, calls the helper immediately after each individual pair. It therefore raised `WC01--B0: paired base repetitions incomplete` on the first not-yet-scheduled case.

This is a deterministic Execution Adapter scheduler defect. It is not candidate behavior, provider instability, host-surface drift or an oracle-semantic defect.

The failure is reconstructable from `failure-evidence.json` and the executed runner source. The Executor correctly stopped rather than changing runner identity after holdout exposure.

## Acceptance decision

- Synthetic canary prompts issued: `2`.
- Acceptance prompts issued: `2`.
- Completed valid v11 observations: `2`.
- Scored observations: `0`.
- Prior observations imported: `0`.
- Selection performed: `false`.
- No candidate/topology result is accepted or inferred.

The two `WX00/B0` observations are immutable historical diagnostic evidence only. They MUST NOT be rescored, imported into a successor score, rerun as v11 evidence or used to tune candidates, thresholds, selection semantics or the replacement stimulus.

Because the exact `WX00` prompt has now been model-visible, a successor acceptance epoch MUST retire it prospectively before issuing any new acceptance prompt.

V11 is therefore closed `BLOCKED / EXECUTION_ADAPTER_SCHEDULER_DEFECT`.

Re-entry stage: `Plan & Trace`.

## Required successor boundary

A successor epoch may preserve all v11 semantics and the now-proven host/preflight path, but MUST prospectively address both exposure and scheduler correctness before model spend:

1. assign new corpus/oracle/execution identities and replace the exposed `WX00` stimulus with a fresh semantically equivalent cross-profile case while preserving class/fixture/capability/outcome/forbidden-capability meaning and final denominators;
2. import zero v11 observations into successor scoring;
3. correct conditional-third scheduling so a decision for one completed case/candidate pair depends only on that pair plus already-observed candidate-level critical state; unscheduled unrelated corpus pairs must not be treated as incomplete errors;
4. add provider-free deterministic characterization proving at minimum: a completed agreeing pair advances to the next case without error; a disagreeing pair schedules exactly one third repetition; unscheduled unrelated pairs do not block either path; and no fourth repetition is possible;
5. include a provider-free adaptive-scheduler dry run using an injected/fake observation adapter before any synthetic canary or acceptance call;
6. preserve the accepted T050 code-health ratchet, modular boundaries and symbol-map checks;
7. preserve v11 candidates, presentations, trial envelope, thresholds, D050 selection, 2+1 semantics, futility/materiality, host/model/CLI and preflight method unless a separately persisted defect requires upstream change.
