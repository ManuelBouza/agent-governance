# T023 Review R15 — T061 MG1-v13 Stage 6 Launch Gate

Status: `STAGE6_AUTHORIZED / AWAITING_CODEX_EXECUTOR_START`

Date: 2026-09-07

Owner: Human Owner / ChatGPT Orchestrator

Controlling Task Contract: `docs/tasks/T061-mg1-v13-positive-anchor-reference-evaluation.md`

Prior Stage 5 review: `docs/reviews/T023-R14.md`

## Human authorization

The Human Owner explicitly lifted the T061 Human Hold and selected continuation of T061 on 2026-09-07.

This review satisfies the Task Contract condition that Stage 6 requires a separate Human-selected launch objective. It authorizes the Agente de IA Ejecutor to perform D068 Stage 6 for T061 and does not authorize Stage 7 acceptance, topology selection, T024, D066 changes, T058 re-entry, or any semantic change to frozen v13 assets.

## Frozen execution anchor

The represented Stage 6 branch remains:

`test/t023-skill-activation-topology-evals-v13`

Verified remote Stage 5 branch HEAD at launch preparation:

`f893a03d17596182db209322f5357cda75ea3781`

Frozen identities remain:

- Candidate Freeze A: `a454091aff7bb932372a6057e2d9804f94e66320`
- Holdout/oracle Freeze B: `7b990f4d60ba7ca0dfafe1b95785e007f8697c28`
- candidate hash manifest Git blob: `6d3a8025a1923dd5738f65e8aa07488cbd58e245`
- corpus: `MG1-T023-CORPUS-v7`
- oracle: `MG1-T023-TOPOLOGY-ORACLE-v13`
- execution epoch: `MG1-T023-EXECUTION-v13`
- trial envelope: `MG1-T023-TRIAL-ENVELOPE-v2`

Remote comparison Freeze B -> represented Stage 5 HEAD contains only the Stage 5 control Markdown (`T061`, `T023-R14`, checkpoint) and no candidate/corpus/oracle/harness mutation.

Current canonical `develop` at launch preparation is `9f01b3e2d925b50185ac1dc815d54fb4c7a8d1d3`. The represented v13 branch is intentionally divergent because the later `develop` commits record Stage 5 frontier closure and D070/O239 interaction/hold state. This known divergence is not authority to rebase, merge, reset, cherry-pick, or otherwise rewrite the frozen v13 candidate before the Executor establishes the exact Task Contract freshness/integrity gates. The Executor shall refresh canonical remote references, preserve unrepresented local work, verify the represented branch/Freeze B ancestry and use the v13 topic branch as the Stage 6 execution surface.

## Executor Launch Profile

```text
Executor: Codex
Surface: ChatGPT desktop / Codex / native Windows
Session: NEW
Coordinator-ID: AG | agent-governance | T061 | root-1
Host-Display-Title: n/a until the host creates the session
Model: GPT-5.6 Sol
Effort: Medium
Codex CLI baseline required by T061: 0.149.0
Rationale: first T061 Executor launch; the frozen scientific cell requires GPT-5.6 Sol/Medium, and Medium is the D055 normal center of gravity for bounded multi-file verification/diagnosis.
```

Current official OpenAI Help was rechecked on 2026-09-07 before this launch recommendation. GPT-5.6 Sol remains available in Codex on eligible paid plans, Medium remains a GPT-5.6 Sol reasoning setting, and GPT-5.6 requires Codex CLI 0.144.0 or newer. The T061-pinned CLI 0.149.0 therefore remains within the currently documented compatibility floor. This compatibility check does not permit upgrading the frozen T061 cell to a different model or CLI version.

## Stage 6 execution order

The Executor is authorized to execute only the already persisted T061 Stage 6 scope, in this order:

1. synchronize canonical GitHub remote state and establish safe local topology without discarding unrepresented work;
2. verify the represented v13 branch, Freeze B ancestry, candidate/reference hashes, corpus/oracle/trial-envelope identities, and known `develop` divergence;
3. run the complete deterministic gate required by T061/R14, including focused v13 integrity/harness tests, Ruff/format/code-health/full pytest and required profile/source-independence/source-distribution/single-install checks;
4. prove provider/model calls during deterministic gates equal `0`;
5. resolve the exact frozen native-Windows/Codex-CLI-0.149.0 backend/workspace profile; if exact 0.149.0 cannot realize the frozen envelope, stop before provider-backed acceptance and return for Plan & Trace re-entry;
6. run the unchanged synthetic Skill canary and require `2/2 PASS` before any acceptance prompt;
7. execute Stage R for B2 only under corpus v7/oracle v13;
8. if B2 is non-qualifying/futile, stop terminally with F2/G3 unscheduled;
9. only if B2 qualifies, execute F2/G3 under the same v13 corpus/oracle/host/model epoch and frozen D050 materiality/tie-break rules;
10. perform technical Code Review & Verify, apply only bounded non-semantic repairs allowed by T061, persist `handoffs/T061-executor-handoff.json`, commit/push the terminal Stage 6 state, and return the minimal handoff pointer required by the Task Contract.

## Delegation boundary

T061 is `ASSURED`, so D065 applies. The Codex coordinator must evaluate bounded delegation before substantial work and again before final technical Code Review & Verify.

Eligible delegation may be used for independent read-heavy verification, noisy deterministic evidence review, or an independent technical review when the child surface satisfies the required permission/isolation/evidence boundary. Delegation must not alter the frozen acceptance candidate set, B2-first scheduler, 2+1 trial method, host/model cell, corpus, oracle, thresholds, or one-writer/worktree safety. The final Executor handoff must record the required D065 delegation posture/evidence.

## Semantic read-only boundary

The Executor MUST NOT semantically edit:

- `docs/AGENT-GOVERNANCE-CAPABILITY-SOURCE.md`;
- `docs/tasks/T061-mg1-v13-positive-anchor-reference-evaluation.md`;
- v13 topology/presentation semantics;
- any `presentations-v4/**` candidate/reference bytes;
- corpus v7 prompts/classes/expected semantics;
- oracle v13 thresholds/selection/scheduling/semantic gates;
- trial-envelope semantics.

A suspected defect in those assets is an Orchestrator SDD re-entry condition, not Executor repair authority.

## Transport prompt

Use the following transport prompt in the Codex coordinator after applying the launch profile above:

```text
Operate as the Agente de IA Ejecutor for ManuelBouza/agent-governance.

Synchronize with GitHub and establish a safe current local baseline from the canonical remote state before loading repository instructions or execution authority, per D042/RB001. Do not discard unrepresented local work.

Use test/t023-skill-activation-topology-evals-v13 as the represented Stage 6 task branch. Preserve its frozen candidate/holdout ancestry; do not rebase or rewrite it merely because current develop has later orchestration Markdown.

Load current repository instructions and current canonical launch authority from develop, then execute exactly:
docs/tasks/T061-mg1-v13-positive-anchor-reference-evaluation.md
docs/reviews/T023-R15.md

Return only the output required by that persisted authority.
```

## Transport state

The ChatGPT Orchestrator surface used to author this gate does not expose a general Codex Executor invocation capability. Plugin discovery found no installed/available general Codex execution transport that can substitute for the required native-Windows Codex coordinator. Therefore no Executor session, provider/model call, deterministic gate, preflight, canary, or acceptance observation is claimed by this review.

Stage 6 is authorized but remains `AWAITING_CODEX_EXECUTOR_START` until the Human Owner starts the above NEW Codex coordinator on the supported native-Windows surface.
