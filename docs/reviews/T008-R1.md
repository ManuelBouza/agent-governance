# T008-R1 — EGLL deterministic learning-detector MVP review

Status: REWORK_REQUIRED  
Task: T008  
Reviewed executor branch: `test/egll-deterministic-learning-detectors`  
Reviewed executor HEAD: `59f0b8bc443636c5a7fbf5d417f185d528cc63e2`  
Implementation anchor: `6cf30513ef545fc276807a22272633f436b956bd`  
Reviewed base: `bb1a2de8db622141fc975d1c341e82b9bdc4c3c6`

## Review result

The executor correctly returned `PARTIAL` because T008's focused implementation is present and scoped correctly, but the required full-suite gate is not green.

Independent remote review confirms:

- T008 changes only `tests/_governance_learning.py`, `tests/fixtures/governance_learning/cases.json`, `tests/test_governance_learning.py`, and `handoffs/T008-executor-handoff.json`;
- all five required MVP fingerprints are implemented with deterministic positive/negative controls and machine-readable findings;
- the implementation introduces no committed Markdown, Governance Core, dependency/configuration, network, model/provider, ruleset, Actions, or consumer-footprint change;
- the focused T008 suite and Ruff gates are reported green;
- the full pytest gate fails because the reviewed base already contains protocol-version drift: `governance-core/GOVERNANCE.md` declares `Protocol-Version: 1.12.0`, while `tests/_helpers.py` pins `SOURCE_PROTOCOL_VERSION = "1.11.0"` and `tests/test_execution_control_contract.py` explicitly asserts `1.11.0`.

This baseline failure is outside T008's authorized scope. The executor was correct not to alter unrelated existing tests or Governance Core to force the gate green.

## Disposition

T008 is **not accepted yet** because its contract requires the full suite to pass.

Do not expand T008 by chat-only instruction.

A separate prerequisite correction, T009, is required to restore the canonical deterministic baseline. After T009 is accepted/integrated and its branch lifecycle is closed, the T008 executor SHALL update its existing task branch from current `develop`, rerun all T008 verification gates, refresh the T008 handoff, commit/push, and return the canonical minimal task response.

No T008 detector semantics are changed by this review.

## Rework acceptance

T008 may be reconsidered when remote evidence shows:

1. T009 is integrated and the canonical full suite is green on `develop`;
2. the existing T008 implementation branch incorporates that corrected `develop` baseline without dropping or materially changing the reviewed EGLL implementation;
3. focused T008 pytest passes;
4. full pytest passes;
5. Ruff check and Ruff format check pass;
6. the refreshed handoff accurately records the new base/head and verification results;
7. T008 remains within its original scope and exclusions.
