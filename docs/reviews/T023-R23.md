# T023 Review R23 — T061 v14-r1 Stage 5 Local Validation Convergence

Status: `BLOCKED_ACCEPTED / FORMAT_ONLY_REPAIR_REQUIRED`

Date: 2026-09-08

Owner: ChatGPT Orchestrator

Controlling work unit: `T061`

Validated repair branch: `test/t023-skill-activation-topology-evals-v14-r1`

Validated implementation head: `ddb58c5467c2cef831e5f3202fab8a7c6017e2ba`

Terminal local-validation evidence head: `6ef1478122379578adc535a05d759658ea1d0847`

Evidence artifact: `handoffs/T061-stage5-local-validation.json`

## Outcome

The Human-returned Codex Local Stage 5 provider-free validation result is accepted as a valid `BLOCKED` result.

The validation materially narrows the remaining Stage 5 blocker to one deterministic formatting gate. It also resolves the previously open host-cell realizability question for Codex CLI `0.149.0` without changing the frozen scientific cell.

No Stage 6 authority is created by this review.

## Remote evidence verified

Human-returned terminal output:

```text
STATUS: BLOCKED
EVIDENCE: handoffs/T061-stage5-local-validation.json
BRANCH: test/t023-skill-activation-topology-evals-v14-r1
HEAD: 6ef1478122379578adc535a05d759658ea1d0847
```

Remote Git verification established:

- `test/t023-skill-activation-topology-evals-v14-r1` resolves exactly to `6ef1478122379578adc535a05d759658ea1d0847`;
- terminal evidence commit parent is exactly `ddb58c5467c2cef831e5f3202fab8a7c6017e2ba`;
- comparison `ddb58c5467... -> 6ef147812...` is ahead by exactly one commit and adds only `handoffs/T061-stage5-local-validation.json`;
- the evidence artifact identifies `implementation_head_sha = ddb58c5467c2cef831e5f3202fab8a7c6017e2ba` and `implementation_parent_sha = a8caf6338dbae3a3ce4cefdc7965acd0d37f1557`;
- Candidate Freeze C remains `1fc38f979d67ff29649f69ae39b8d46d2523518b`;
- Holdout/oracle Freeze D remains `f8cee7c72688f0486211474d2678d018772e2a58`;
- the historical Stage 6 terminal branch/head remains `test/t023-skill-activation-topology-evals-v14@75f52f64adddfc738ab1d5362efdfe90cf327786`;
- the validation invocation modified no scientific asset, Markdown, implementation, test, harness, configuration or dependency before its evidence-only terminal commit.

The returned Stage 5 validation evidence is therefore remote, represented, ancestry-consistent and evidence-only.

## Accepted provider-free results

The following results are accepted for implementation head `ddb58c5467...`:

- candidate Freeze C integrity: `PASS`;
- holdout/oracle Freeze D integrity: `PASS`;
- `ruff check`: `PASS`;
- code-health check: `PASS`;
- code-health map: `PASS`;
- profile abstraction/source-maintainer deterministic tests: `PASS` — `48 passed`;
- Consumer/source-separation characterization tests: `PASS` — `8 passed`;
- v14 provider-free scheduler simulation: `PASS` — `3 passed`, provider/model calls `0`;
- full locked pytest: `PASS` — `477 passed`;
- T061 harness provider/model calls: exactly `0`;
- synthetic canary, B2, F2 and G3: not run.

These results supersede the corresponding deterministic failures reported at the older `a8caf633...` Stage 5 head wherever the same gate is now green on `ddb58c5467...`.

## Remaining blocker — Ruff format check only

`uv run --locked ruff format --check .` remains `FAIL` because seven files would be reformatted:

- `evals/skill_activation_topology/_harness/evidence.py`;
- `evals/skill_activation_topology/_harness/frozen_inputs.py`;
- `evals/skill_activation_topology/_harness/runner.py`;
- `evals/skill_activation_topology/_harness/scheduler_simulation.py`;
- `evals/skill_activation_topology/_harness/scoring.py`;
- `tests/test_skill_activation_topology_harness.py`;
- `tests/test_skill_activation_topology_v11.py`.

No formatting write was attempted during the validation invocation, as required by its VERIFY/DIAGNOSE-only boundary.

This is a Stage 5 technical-mechanics defect. It does not require a new candidate/corpus/oracle identity while the change remains formatter-only and all frozen scientific assets remain byte-identical.

## Host-cell resolution

The prior open question whether Codex CLI exactly `0.149.0` is realizable on the supported native-Windows host is resolved `YES` for the current host.

Accepted evidence:

- default desktop-bundled `codex` remains `0.153.4`;
- official package `@openai/codex@0.149.0` was resolved through npm metadata;
- an isolated `npm exec` invocation reported exactly `codex-cli 0.149.0` with exit code `0`;
- the realization used a dedicated temporary cache/work area;
- temporary artifacts were removed;
- no repository dependency/configuration changed;
- no persistent replacement of the Human's default Codex installation is required;
- a fresh realization requires network access.

Therefore the scientific live-cell requirement remains unchanged and no semantic host-cell decision is currently required. A later Stage 6 launch must still realize and verify exact `0.149.0` before provider-backed evaluation rather than use default `0.153.4`.

## Authorized Stage 5 continuation

The existing Human-selected T061 Stage 5 re-entry remains active. The next repair is narrowly authorized as formatter-only technical materialization.

Permitted tracked changes are limited to formatter output on the seven paths listed above. The continuation MUST NOT:

- modify any `presentations-v5/**` byte;
- modify `candidate-hashes-v14.json`, `corpus.json`, `oracle.json`, `topologies.json`, presentation/capability-source semantics, Freeze C or Freeze D;
- weaken tests, add ignores/exclusions, or alter Ruff/configuration merely to satisfy the gate;
- run synthetic canary, B2, F2, G3 or any provider-backed T061 evaluation;
- issue T061 harness provider/model calls;
- start Stage 6.

After formatter-only materialization, all provider-free deterministic gates SHALL be rerun on the resulting implementation head, including candidate/holdout integrity, Ruff lint/format check, code-health, deterministic subsets, scheduler and full locked pytest.

Because the same repair branch and same local coordinator remain coherent, D060 continuity prefers:

```text
Session: CONTINUE
Coordinator-ID: AG | agent-governance | T061 | root-3
Branch: test/t023-skill-activation-topology-evals-v14-r1
```

D071/D072/D073 Human-mediated transport/title rules remain controlling.

## Readiness consequence

T061 is NOT `READY_FOR_STAGE6` yet.

Readiness may be reassessed only after:

1. the formatter-only repair is represented remotely;
2. all provider-free gates are green on that repaired implementation head;
3. Freeze C/Freeze D remain intact;
4. T061 harness provider/model calls remain `0`; and
5. the exact CLI `0.149.0` realization path remains usable.

A later Stage 6 still requires a separate Human selection and a new persisted launch gate. R19 remains consumed and MUST NOT be reused.

## Preserved boundaries

- no scientific conclusion exists for B2/F2/G3;
- no topology is selected;
- T024 remains blocked;
- D066 gaps remain unchanged;
- T058 remains frozen;
- historical Stage 6 terminal evidence at `75f52f64...` remains immutable;
- local-validation evidence commit `6ef147812...` remains durable evidence and must not be rewritten.
