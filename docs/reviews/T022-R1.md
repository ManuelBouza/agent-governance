# T022 Review — Source Maintainer Profile Over Legacy Adapters

Status: `ACCEPTED`

## Identity

- Task: `T022`
- Task Contract: `docs/tasks/T022-source-maintainer-profile-over-legacy-adapters.md`
- Canonical verification base: `7ad68b2774dafc58208737af66f88fb67cec2e53`
- Implementation HEAD: `a32a906a636e49b9392129d42925c30fef07e027`
- Submitted Executor HEAD: `35ba704c8e2997f414a5f2a79b23c33b821b8016`
- Executor handoff: `handoffs/T022-executor-handoff.json`
- Integration PR: `#223`
- Integration merge: `361e6700d74d47f5805b9af838c1cfa9519766b2`

## Review result

T022 is accepted.

The submitted implementation activates `source-maintainer` as a second explicit runtime profile over the shared deterministic engine while preserving `consumer` as the default profile. Source context is accepted only through the exact, versioned `agent-governance-source.json` product signal; directory-name inference alone is rejected.

The source adapter routes live source records, including the normative `governance-core/`, without copying Core into a Consumer footprint. It rejects ambiguous source/Consumer roots, missing or wrong-type required records, unsafe links, malformed or unsupported signals, and source writes outside flat `handoffs/*.json`.

The source-maintainer command surface is isolated from Consumer mutation operations. Consumer `bootstrap`, `state`, `event`, `skill`, `ecosystem`, and `archive` are rejected under the source profile before managed Consumer state can be created. Consumer profile routing does not expose the source-only `context` operation.

## Scope review

The exact delta from canonical base contains only authorized non-Markdown product/test/evidence files:

- `agent-governance-source.json`
- `src/agent_governance/engine.py`
- `src/agent_governance/profile.py`
- `src/agent_governance/source_adapter.py`
- `tests/test_profile_abstraction.py`
- `tests/test_source_maintainer_profile.py`
- `handoffs/T022-executor-handoff.json`

No Governance Core semantics, committed Markdown, dependency/toolchain configuration, source persistence convergence, independent second Skill runtime, or unrelated downstream topology work is introduced.

## Verification evidence

Executor evidence on the implementation HEAD records:

- focused source-profile/adapter suite: `48 passed`;
- Consumer/profile/shared-engine/artifact regression: `110 passed`;
- full deterministic suite: `405 passed`;
- Ruff check: PASS;
- Ruff format check: PASS (`33 files already formatted`);
- `py_compile`: PASS;
- `git diff --check`: PASS;
- live source-root check: neither `.agent-governance` nor `.agent-coordination` exists.

Code Review & Verify reports no findings and no upstream re-entry requirement.

## Acceptance criteria

- Explicit source-product identity: PASS — strict versioned signal with negative controls for missing, partial, wrong-version, wrong-product and wrong-profile cases.
- Legacy source record routing: PASS — live Core and required policy/task/checkpoint/handoff/test/eval records resolve through the adapter without Consumer initialization.
- Consumer non-regression: PASS — Consumer remains the default; exact seven-command Consumer surface remains green.
- Cross-profile mutation isolation: PASS — Consumer mutations are unavailable under source-maintainer; source write resolution is restricted to non-Markdown handoff JSON.
- Live source Core/no Consumer state: PASS — source context points to live `governance-core/`; no Consumer footprint is materialized at the source root.

## Next boundary

T023 is not yet executable. Its contract additionally requires MG1 — the Orchestrator-owned Skill activation topology/eval pre-registration gate — integrated into `develop` with frozen D050 candidate definitions, D052 corpus/expected outcomes, host/model matrix, repeated-trial method, metric definitions, and victory/non-regression thresholds before comparative results are observed.
