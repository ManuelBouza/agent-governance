# T023 Review R11 — MG1-v12

Status: `BLOCKED / NO QUALIFYING SINGLE-FAMILY REFERENCE — VALID EXPERIMENTAL RESULT`

Date: 2026-09-04

Owner: ChatGPT Orchestrator

## Scope reviewed

- Task: `T023`
- Controlling revision: `docs/tasks/T052-mg1-v12-pair-scoped-scheduler-restart.md`
- Canonical execution base: `2696378eb7aef2815cca393102d53f6e25367f29`
- Submitted Executor branch: `test/t023-skill-activation-topology-evals-v12`
- Submitted terminal HEAD: `3e5bec392d0b8e5804c4efaad74b795b08dc9779`
- Immutable live-runner commit: `7b79ba6589becea6d71ab95c7c36e2e158225675`
- Reviewed implementation/evidence HEAD: `32fee8e50ee2fb59c6a9fcc147777f0c2b356e10`
- Handoff: `handoffs/T023-executor-handoff.json`
- Evidence root: `evals/skill_activation_topology/evidence/mg1-v12-codex-windows-gpt-5.6-sol-medium/`
- Oracle: `MG1-T023-TOPOLOGY-ORACLE-v12`
- Execution epoch: `MG1-T023-EXECUTION-v12`
- Corpus: `MG1-T023-CORPUS-v6`

The submitted branch is based exactly on the frozen V12 launch base and contains no Executor-authored Markdown. D052-owned corpus/oracle/presentations/trial-envelope semantics were not modified by the Executor.

The source `develop` branch advanced independently after launch only through non-controlling persistent-Codex-coordinator research; that later research does not alter V12 experiment identity or this convergence result.

## Method acceptance

V12 corrected the V11 execution-adapter scheduler defect and successfully reached a terminal experiment decision.

Accepted pre-provider evidence:

- Ruff check PASS;
- Ruff format check PASS;
- code-health PASS;
- deterministic symbol-map PASS;
- focused harness/code-health tests PASS: `77`;
- full pytest PASS: `488`;
- corpus v6 rotation proof PASS: `39` unchanged cases plus preregistered `WX00 -> WX00R` replacement;
- frozen candidate/presentation/topology/trial-envelope integrity PASS;
- provider-free adaptive-scheduler simulation PASS;
- full scripted B0/B1 reference-stage dry run: `160` scheduled observations;
- provider/model calls during deterministic scheduler gate: `0`.

The provider-free scheduler simulation established:

- agreeing pair -> no `r3`, then forward progress;
- disagreeing pair -> exactly one `r3`, then forward progress;
- no `r4`;
- critical terminal state stops reconfirmation work;
- unscheduled unrelated pairs do not cause failure.

Accepted host profile:

- Codex CLI `0.149.0`;
- native Windows 11;
- native Windows backend `elevated`;
- logical sandbox `read-only`;
- workspace profile `windows-inherited-acl-mkdir-v1`;
- GPT-5.6 Sol / Medium;
- user config ignored;
- execpolicy rules ignored;
- dangerous bypass absent;
- interactive approvals absent.

The provider-free workspace gate passed. The unchanged synthetic local-Skill canary then passed `2/2` with host-observed `SKILL.md` body reads.

No capacity event or post-preflight host-surface drift invalidated acceptance execution.

## Live execution result

V12 issued `72` fresh acceptance prompts and produced `72` valid observations.

Conditional third repetitions executed: `2`.

No prior-epoch observation entered V12 scoring/futility state.

Reference-stage outcomes:

- `B0`: `FUTILE_QUALIFICATION`;
- `B1`: `FUTILE_QUALIFICATION`;
- `F2`: `NOT_SCHEDULED_NO_REFERENCE`;
- `G3`: `NOT_SCHEDULED_NO_REFERENCE`.

Selected candidate: none.

The terminal selection is:

`BLOCKED / NO QUALIFYING SINGLE-FAMILY REFERENCE`.

## Why B0 is terminally non-qualifying

B0 finalized one false activation among the frozen `11` negative/near-miss cases.

The false activation is on `WM02`, a near-miss case concerning maintainer policy for an unrelated Go library. The final case aggregate host-observed activation of the B0 `agent-governance` entrypoint even though the expected entrypoint set is empty.

At the futility boundary B0 had:

- false activations: `1`;
- negative/near-miss denominator: `11`;
- optimistic final false-activation rate: `1/11 = 0.090909...`;
- frozen maximum permitted false-activation rate: `0.05`.

Future unfinished observations cannot reduce the fixed numerator/denominator below the threshold. Qualification is therefore mathematically impossible.

B0's other optimistic bounds do not rescue qualification:

- precision: `0.966666...`;
- recall: `1.0`;
- F1: `0.983050...`;
- semantic accuracy: `0.95`;
- wrong-specialist rate: `0.0`;
- overactivation rate: `0.025`.

The terminal failed bound is specifically `false_activation_rate`.

## Why B1 is terminally non-qualifying

B1 independently finalized one false activation among the same frozen `11` negative/near-miss cases.

The false activation is on `WM05`, a near-miss case that asks for a linting Skill while explicitly stating Agent Governance is not installed. The final aggregate host-observed activation of `agent-governance-router` although the expected entrypoint set is empty.

B1 therefore has the same terminal mathematical bound:

- false activations: `1`;
- negative/near-miss denominator: `11`;
- optimistic final false-activation rate: `0.090909...`;
- frozen maximum: `0.05`.

Its remaining optimistic qualification bounds match B0's relevant best-case values, and the terminal failed bound is likewise `false_activation_rate`.

The two reference candidates fail on different near-miss stimuli. This is accepted candidate/topology behavior under the frozen V12 method, not evidence of a common single-stimulus execution artifact.

## D050 consequence

D050 requires the B0/B1 single-family reference stage to establish at least one qualifying reference before challengers may execute.

Because both B0 and B1 have valid terminal `FUTILE_QUALIFICATION` certificates:

- no qualifying single-family reference exists;
- Stage C is forbidden;
- F2/G3 were correctly not scheduled;
- no inference about F2/G3 quality may be drawn from V12;
- no topology is selected.

The cost-bounded method therefore operated as intended: it stopped after `72` valid observations rather than spending the pathological full-completion ceiling.

## Post-live verification change

The immutable live runner is `7b79ba6589becea6d71ab95c7c36e2e158225675`.

After live execution, the Executor changed only `evals/skill_activation_topology/_harness/provenance.py` in technical code, plus persisted evidence, before the reviewed implementation/evidence HEAD `32fee8e50ee2fb59c6a9fcc147777f0c2b356e10`.

The live runner's generic complete-evidence verifier had assumed the full reference base schedule even when the adaptive runner legitimately terminated early with `BLOCKED_NO_REFERENCE`. The post-live change makes terminal verification understand that already-produced early-futility state and independently recompute:

- finalized case aggregates;
- both qualification-futility certificates;
- empty partial reference metrics;
- blocked reference-selection state;
- terminal selection.

The post-live change did **not** alter aggregation, scheduling, scoring, futility semantics, frozen assets, raw observations or the live-run terminal decision. No additional model observation was issued to obtain or change the result.

This verifier correction is accepted as technical Code Review & Verify work. It is not retrospective rescoring or a changed experimental runner.

## Acceptance decision

MG1-v12 is accepted as a valid terminal experiment with status:

`BLOCKED / NO QUALIFYING SINGLE-FAMILY REFERENCE`.

This is materially different from the V7-V11 method/host/adapter blockers. V12 provides valid candidate-level evidence under the frozen method.

Accepted facts:

- V12 method/preflight execution is valid;
- `72` valid fresh observations are authoritative V12 evidence;
- B0 is non-qualifying by exact futility;
- B1 is non-qualifying by exact futility;
- no reference exists;
- F2/G3 were correctly not executed;
- no topology is selected.

Not accepted/inferred:

- F2/G3 are not accepted or rejected on behavior;
- no result from prior epochs enters V12 score;
- no threshold relaxation is authorized;
- no post-hoc corpus/candidate change is authorized;
- no automatic V13 rerun is authorized.

## Re-entry boundary

Re-entry stage: `Explore / Specify`.

The next MG1 work, if pursued, must treat V12 as a substantive design result rather than an execution defect.

A successor may investigate why the unified B0/B1 family overactivates on near-miss maintenance/Skill language and whether a revised reference-family design can satisfy the frozen product intent. Any candidate/presentation redesign must be prospective.

Because V12 exposed many corpus prompts, a future acceptance epoch that changes candidate behavior MUST use a prospectively justified fresh holdout boundary rather than reusing V12 evidence as an acceptance score. V12 results may inform design/root-cause research only after this epoch is closed.

Do not rerun V12, relax the `0.05` threshold, execute F2/G3 outside D050, or interpret non-execution as challenger failure.
