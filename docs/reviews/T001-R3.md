# T001 Review Revision R3

Status: ACTIVE REWORK DIRECTIVE
Task: `docs/tasks/T001-deterministic-test-harness-foundation.md`
Prior review: `docs/reviews/T001-R2.md`
Reviewed executor branch: `test/governance-harness`
Reviewed visible HEAD: `87a16f4ee4564a57e62e0dc323b6bbeafc72d718`
Reviewed implementation anchor: `5bee541e02ac0f940f07a349e0485d430dd2d985`
Reviewer: ChatGPT Orchestrator

## Purpose

Persist the final focused PD5 findings found while crossing the R2 candidate against all T001 acceptance criteria and source-toolchain neutrality rules.

R2 successfully closes all three R2 implementation findings. R3 does not reopen those findings, D029, D030, the historical uv disposition, or the deterministic test design. It is limited to one source-toolchain coupling cleanup plus one stale handoff metadata correction.

## Accepted R2 outcomes

No further remediation is required for these areas unless later work regresses them:

- root dotfiles are no longer misclassified as extension-only prose;
- `.gitignore` contains exactly the five T001-authorized generated-state patterns;
- concrete reference resolution canonicalizes paths with `resolve(strict=False)` and rejects `..` escapes outside the repository root;
- focused reference-integrity regressions pass;
- canonical locked verification reports 81 passed, 0 failed, 0 skipped under Python 3.13.14 / uv 0.11.33 / pytest 9.1.1 / Ruff 0.16.2;
- D029 handoff identity remains correct;
- D030 clone-local Gentle-AI RDD opt-out remains in effect without committed Gentle-AI/SDD repository assets;
- no Markdown was edited by the executor.

## Review finding R3-TOOLCHAIN-COUPLING — Ruff configuration names unrelated/external surfaces

Observed in `pyproject.toml`:

```toml
extend-exclude = [
    "**/*.md",
    "**/*.ipynb",
    ".atl",
    ".venv",
    "handoffs",
]
```

T001/D025 require explicit protection of committed Markdown and permit ordinary project-local generated environment state. They do not authorize encoding a Gentle-AI project-local surface into the canonical source toolchain, and T001 explicitly excludes unrelated notebook/Jupyter scope.

`.atl/` is a Gentle-AI project-local registry/cache surface. D026/D030 allow it to coexist as external uncommitted host state, but source correctness must not depend on a named Gentle-AI adapter inside `pyproject.toml`.

Required rework:

1. Remove `.atl` from Ruff configuration.
2. Remove `**/*.ipynb` because T001 does not introduce Jupyter/notebook support or policy.
3. Remove `handoffs` from the Ruff exclusion list; handoff JSON does not need a product-specific Ruff exclusion.
4. Keep the explicit committed-Markdown protection required by T001/D025.
5. `.venv` may remain explicitly excluded as repository-local generated environment state, although Ruff may also exclude it by default.
6. Do not modify/delete the pre-existing untracked `.atl/` workstation/clone state as part of this rework.
7. Add no new dependency or external-tool integration.

Expected minimal semantics:

```toml
extend-exclude = [
    "**/*.md",
    ".venv",
]
```

An equivalently minimal configuration satisfying the same boundaries is acceptable.

## Review finding R3-HANDOFF-METADATA — stale recommended-next-task dependency

The R2 handoff correctly describes implementation and verification evidence, but `recommended_next_task.depends_on` still says:

`T001 R1 acceptance by ChatGPT Orchestrator...`

R1 is superseded. Update this non-authoritative recommendation metadata so it depends on final T001 acceptance after the current review, without naming an obsolete review revision.

This is a metadata accuracy cleanup, not a new product requirement and not a blocker on the verified R2 test behavior by itself.

## Verification required after R3

Because R3 changes `pyproject.toml`, rerun the complete canonical non-mutating gate:

```text
uv sync --locked
uv run --locked ruff check .
uv run --locked ruff format --check .
uv run --locked python -m pytest
```

The configuration change must not modify `uv.lock` because no dependency is changing.

Record exact pass/fail/skip counts and Python/uv/pytest/Ruff versions.

## Handoff requirements after R3

Update `handoffs/T001-executor-handoff.json` so that it:

- uses a new `implementation_head_sha` identifying the committed R3 implementation/configuration state;
- preserves the accepted R1/R2 history and D030 clone-local RDD disposition;
- records the R3 Ruff configuration cleanup;
- corrects the stale `recommended_next_task.depends_on` wording;
- reports no unresolved issue if verification passes;
- remains committed and pushed under D029;
- uses `status: DONE` only if T001 plus R1/R2/R3 are otherwise contract-compliant.

Then return only:

`STATUS: DONE | BLOCKED | PARTIAL`

`HANDOFF: handoffs/T001-executor-handoff.json`

`BRANCH: test/governance-harness`

`HEAD: <actual-pushed-final-branch-head-sha>`

Do not open or merge a PR.
