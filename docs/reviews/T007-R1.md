# T007-R1 — Branch hygiene cleanup review

Status: REWORK_REQUIRED

Reviewed executor branch: `chore/branch-hygiene-cleanup`  
Reviewed executor HEAD: `656c71e22f60ae8b235304179dc1d8fee4ec4031`  
Reviewed base: `c30017ccfd6ab912321f10fe0baa9b5181787609`

## Accepted evidence

- The executor branch is exactly one commit ahead of the T007 base.
- The only committed repository change is `handoffs/T007-executor-handoff.json`.
- Fifty historical topic branches classified `DELETE` are absent from the canonical remote.
- The executor-controlled checkout evidence reports stale local test branches removed and remote-tracking refs pruned.
- `main` and `develop` were not moved by cleanup operations.

## R1 finding — unresolved cancelled T004 branch

`eval/d032-agent-capability` remains on the canonical remote and locally at `eb20dc0fed2674190a82ef40aa0e02436c02ced4` because no associated PR exists.

Orchestrator comparison against current `develop` shows its unique diff is only the cancelled T004 implementation surface:

- `evals/d032/adapters/opencode.py`
- `evals/d032/cases.json`
- `evals/d032/runner.py`
- `handoffs/T004-executor-handoff.json`
- `tests/test_d032_agent_eval_harness.py`

T004 is terminal `CANCELLED_BY_HUMAN` under D037 and this implementation was never authorized for integration. Its unique branch work is therefore intentionally abandoned, not an active preservation requirement.

R1 authorizes deletion of `eval/d032-agent-capability` remotely and in the executor-controlled local checkout at the exact recorded SHA above. Re-read the ref immediately before deletion and stop if its SHA has changed.

## Procedural nonconformance to preserve

During the initial T007 pass, `eval/d032-agent-capability` was deleted before its missing PR association was fully resolved, then restored at the exact original SHA. The final state was recovered safely, but this violated the required classify-before-delete sequence. Preserve this fact in the final handoff; do not rewrite it away.

## Required completion

1. Re-read `eval/d032-agent-capability` and verify it is still exactly `eb20dc0fed2674190a82ef40aa0e02436c02ced4`.
2. Delete that remote branch.
3. Delete its local branch in the executor-controlled checkout after switching away and verifying no worktree owns it.
4. Fetch/prune and verify the only non-task branches remaining remotely are `main` and `develop`.
5. Update the T007 handoff with R1 evidence and the preserved procedural nonconformance.
6. Commit and push the updated handoff on `chore/branch-hygiene-cleanup`.

No product/Core/test content may be changed.