# T063-R6 — V3 Config-Authoritative Receipt Repair and Clean Rerun Authority

Status: `STAGE5_CANDIDATE_PUBLISHED / STAGE6_AUTHORIZED_AWAITING_HUMAN_CODEX_START`  
Date: 2026-09-10  
Authority: Human Owner / ChatGPT Orchestrator  
Task: `docs/tasks/T063-adaptive-worker-routing-requalification.md`  
Prior blocked-run review: `docs/reviews/T063-R5.md`  
Receipt/version research: `docs/research/R021-T063-V3-CONFIG-AUTHORITATIVE-WORKER-RECEIPTS.md`  
Version-sensitive authority: `docs/decisions/D077-version-sensitive-upstream-revalidation.md`  
Measurement authority: `docs/decisions/D063-qualified-codex-read-only-child-measurement-surface.md`  
Materialization authority: `docs/decisions/D068-library-first-candidate-materialization-executor-verification-boundary.md`; `docs/decisions/D076-stage6-ephemeral-executable-materialization-boundary.md`  
Provider/model calls during Orchestrator v3 repair: `0`

## Verdict

T063 is authorized for one **clean complete v3 rerun** using the published Stage 5 candidate:

```text
branch: test/t063-adaptive-worker-routing-requalification-v3
HEAD:   9b8a8d96b7586c25e808e93c3cec02d1f2fa3467
base:   7772886f174ae06e0a377fc04612f1059af961b6
```

The v1/v2 blocked branches remain historical evidence and are excluded from v3 scoring.

No provider-backed call is authorized from any other candidate HEAD.

## Stage 5 candidate

ChatGPT Orchestrator materialized the complete v3 evaluation package under:

```text
evals/adaptive_worker_routing_v3/
tests/test_t063_adaptive_worker_routing_v3_harness.py
```

The branch is exactly eight commits / eight files ahead of its verified `develop` base and contains only the new evaluation package plus its deterministic harness test.

Provider-free Orchestrator verification:

```text
candidate harness tests: 23 passed
Python compile check:    PASS
provider/model calls:    0
```

Repository-native lint was not available in the Orchestrator execution environment. Stage 6 therefore has a mandatory **pre-provider** technical gate: run the repository-native deterministic test/lint/compile checks applicable to the candidate and stop before any scored parent/child call if they do not pass.

A bounded technical repair of represented candidate code is allowed under D068/D076 only when it preserves the frozen semantics below. Any semantic/design/receipt change requires Orchestrator re-entry.

## Scientific restart rule

V3 is another clean six-arm experiment. No prior scored or diagnostic result is reused.

Historical blocked evidence remains preserved:

```text
v1  3d8a9460988351383a90adfc6b76e2deff056504
v2  3ff745a8d29e031ca818c1bc618b15a54e0cbf2b
```

The v3 first-attempt order is exactly:

```text
P1 ADAPTIVE -> CONTROL
P2 CONTROL  -> ADAPTIVE
P3 ADAPTIVE -> CONTROL
```

The original T063 probe semantics, frozen source/oracle baseline and pilot-decision definitions remain controlling except where R020/R021 explicitly repair the V2 adapter/receipt strategy.

## Frozen source/oracle baseline

The probe baseline remains:

```text
69e910f329a2294c3b40df0f6ee983f9905f4677
```

The candidate branch contains newer harness files, but all P1/P2 source characterization and the P3 source seed derive from that exact frozen baseline.

## Runtime and launch profile freeze

Use exactly:

```text
Executor:        Codex
Surface:         Codex Local / native Windows
Session:         NEW
Coordinator-ID:  AG | agent-governance | T063 | root-3
Root model:      gpt-5.6-sol
Root reasoning:  medium
Codex CLI:       exactly 0.153.4
App Server:      exactly 0.153.4
Auth category:   chatgpt
```

The child matrix remains:

| Probe | Arm | Requested model | Requested reasoning |
| --- | --- | --- | --- |
| P1 | ADAPTIVE | `gpt-5.6-luna` | `medium` |
| P1 | CONTROL | `gpt-5.6-sol` | `medium` |
| P2 | CONTROL | `gpt-5.6-sol` | `medium` |
| P2 | ADAPTIVE | `gpt-5.6-terra` | `medium` |
| P3 | ADAPTIVE | `gpt-5.6-terra` | `high` |
| P3 | CONTROL | `gpt-5.6-sol` | `medium` |

No model, effort or runtime substitution is authorized after provider-backed execution starts.

If the host/account cannot resolve a frozen requested model/effort exactly, stop as `BLOCKED_PROFILE_RESOLUTION`; do not substitute another model.

## D077 version-sensitive conclusion

Immediately before this authority, R021 revalidated the T063 blocker against:

```text
0.153.4            D063-qualified reference
0.154.0            current stable release
0.155.0-alpha.2    later relevant prerelease
```

The reviewed higher versions do not supply a public exact spawned-task-message receipt that removes the v2 blocker. T063 therefore uses the D077 disposition:

```text
PIN_RETAINED
retained runtime: 0.153.4
```

The pin is intentional, not stale-version neglect. `0.154.0` is not adopted merely because it is newer, and the prerelease is evidence only.

If a newer stable Codex release appears before Human launch, stop and return to Orchestrator for D077 relevance classification before running provider-backed work.

## V3 receipt architecture

R020 remains correct that public `thread/read` does not reconstruct the substantive V2 `InterAgentCommunication` spawn task.

V3 therefore does **not** try to recover that hidden message.

Instead the published harness makes the authoritative scored child contract part of the parent thread's App Server configuration before the parent acts:

```text
features.multi_agent_v2.subagent_developer_instructions = <exact frozen child contract>
agents.default_subagent_model = <frozen arm model>
agents.default_subagent_reasoning_effort = <frozen arm effort>
features.multi_agent_v2.expose_spawn_agent_model_overrides = false
features.multi_agent_v2.hide_spawn_agent_metadata = true
```

The substantive frozen task, task digest and per-arm contract nonce live in that Stage 5-controlled child contract.

The measurement parent is transport-only and must:

```text
spawn exactly one child
use the harness-supplied task_name
send the harness-supplied non-substantive trigger as message
use fork_turns="none"
not provide agent_type/model/reasoning_effort/fork_context
not inspect or solve the probe
not use repository/file/shell/network tools
return exactly PARENT_SPAWNED
```

The trigger is a transport consistency value, not the substantive task. The worker must return the exact received trigger together with the frozen contract nonce and task digest. Any mismatch invalidates the arm.

This worker receipt is not provider-signed prompt identity and must not be described as such.

## Public child correlation

The exact child must be correlated through one public parent `subAgentActivity` item:

```text
kind = Started
agentThreadId = exact child id
agentPath final component = exact frozen task_name
```

The completed parent turn must contain exactly one matching spawn activity and no substantive parent tool usage.

Analytics `collabAgentToolCall.prompt` and internal raw response events are not passing evidence.

## D063 live measurement gate

For every scored child the harness must preserve the D063-qualified semantic receipt set, including:

```text
real parent/child correlation
parent activePermissionProfile.id == :read-only
non-contradictory legacy read-only projection
continuous loaded-parent residency before child reattachment
child parentThreadId == exact parent
child activePermissionProfile.id == :read-only
requested child model/reasoning from frozen Stage 5 config
resolved configured child model/reasoning exact match
exact non-estimated child-turn token usage
exact child-turn duration
exact-child reroute observation
no tracked/global mutation attributable to measurement
```

`backend_served_profile_verified` remains `false` unless a separately qualified stronger receipt is introduced by new Orchestrator authority. R6 introduces none.

A missing mandatory D063 receipt is `BLOCKED_MEASUREMENT_SURFACE`, not a quality result.

## Custom role fail-closed rule

The run requires the frozen config-authoritative child contract to remain the only substantive worker authority.

If local/custom agent-role configuration could introduce `agent_type`, replace the child contract, or otherwise make role selection ambiguous, preflight must block before scored provider calls. Do not compensate by editing task semantics or inventing a role mapping in Stage 6.

## P3 runtime-root rule

P3 uses the published candidate's semantic fixture generator. The Executor supplies only a fresh runtime directory location.

The runtime root must be:

- outside the repository/worktree;
- outside Windows system temp;
- a fresh unused location compatible with read-only child access;
- absent before candidate preparation creates it.

If the prepared fixture cannot be read under the qualified child sandbox, stop as execution-invalid input before accepting a score; do not relocate and rewrite a consumed first attempt.

## Stage 6 repair and D076 boundary

Codex may execute, diagnose and make bounded technical repairs to the **published v3 candidate** when the repair preserves this review's exact scientific semantics.

It may not change:

- frozen baseline `69e910f...`;
- P1/P2/P3 task meaning or oracle meaning;
- task-message digests;
- arm order;
- child model/reasoning matrix;
- substantive task/profile authority architecture;
- D063 receipt meaning;
- P3 seeded defect/oracle;
- first-attempt scoring or pilot-decision definitions;
- historical-evidence exclusion;
- D077 runtime disposition.

Any such need is Orchestrator re-entry.

D076 applies to all non-candidate file-based executable aids. Small mechanical aids may be used and must be audited as required; substantial missing controller/harness/fixture/oracle materialization requires immediate stop/re-entry.

The Executor must not create or edit committed Markdown.

## Evidence branch and terminal files

Stage 6 runs on the same candidate/evidence branch:

```text
test/t063-adaptive-worker-routing-requalification-v3
```

Normal evidence paths are:

```text
handoffs/T063-adaptive-worker-routing-telemetry-v3.json
handoffs/T063-executor-handoff-v3.json
```

Technical repairs to candidate non-Markdown files, if any, must be represented in Git before terminal return. Evidence must record the actual `candidate_head_before_execution` and any repair commits that preceded provider execution.

## Pilot decision

Compute the original T063 pilot decision only from the six fresh v3 first attempts.

No v1/v2 result contributes to:

```text
control_pass_count
adaptive_pass_count
exact token totals
exact duration totals
first-attempt failures
false positive/negative counts
pilot_decision
```

A valid `QUALIFIED` or `QUALIFIED_QUALITY_ONLY` result supports later Orchestrator convergence under D057; it does not itself change global routing policy.

R007 remains `EVALUATING` until terminal v3 evidence is reviewed and accepted.

## D060 root authority

Use a NEW failover root:

```text
AG | agent-governance | T063 | root-3
```

Root-2 executed the invalid v2 adapter and observed its diagnostic P1 result. The v3 authority changes the substantive receipt architecture and requires a fresh uncontaminated execution context. Reusing root-2 would undermine the clean-rerun boundary.

This is D060 same-work-unit failover, not a new Task Contract.

## Human-mediated transport

D071 controls. ChatGPT does not start or control Codex.

After this review and O264 are integrated into `develop`, the launch state is:

```text
AUTHORIZED_AWAITING_HUMAN_CODEX_START
```

The Human starts a NEW Codex session with the frozen launch profile and exact Coordinator-ID/title, loads the canonical authority from `develop`, checks out the exact candidate branch/HEAD, and lets the published harness own the experiment.

## Terminal return

Return only:

```text
STATUS: COMPLETED | BLOCKED
HANDOFF: handoffs/T063-executor-handoff-v3.json
BRANCH: test/t063-adaptive-worker-routing-requalification-v3
HEAD: <actual remote pushed HEAD>
```

No additional provider-backed call is authorized after a BLOCKED terminal result without new Orchestrator review.
