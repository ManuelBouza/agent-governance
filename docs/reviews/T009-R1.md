# T009-R1 — Protocol-version baseline alignment review

Status: ACCEPTED

Task: `T009`  
Task Contract: `docs/tasks/T009-protocol-version-baseline-alignment.md`  
Executor branch: `test/protocol-version-baseline-alignment`  
Reviewed executor HEAD: `e0c80c62c1c543504719616c547d4df03d1b3d21`  
Implementation anchor: `4d3b2cc440e7f8e0b3d6f720d12540231abed2c0`  
Base: `develop@d1e16fc60c1fca909a49306e78fb9ccde180d6e5`

## Decision

T009 is ACCEPTED.

Remote review confirms the implementation is limited to the authorized deterministic test baseline plus the executor handoff:

- `tests/_helpers.py` updates the single test-side `SOURCE_PROTOCOL_VERSION` verifier expectation from `1.11.0` to the authoritative Core version `1.12.0`;
- `tests/test_execution_control_contract.py` removes only the redundant literal assertion `SOURCE_PROTOCOL_VERSION == "1.11.0"`;
- `handoffs/T009-executor-handoff.json` persists the required evidence.

No Markdown, Governance Core, T008 implementation, T006/D035/D036 behavior, dependency, configuration, CI/ruleset, consumer footprint, model/provider, external service, or network-dependent verification changes are present in the reviewed diff.

## Deterministic-verification assessment

The correction does not remove protocol-version mismatch detection. The source tests continue to parse `governance-core/GOVERNANCE.md` with `protocol_version_from()` and compare that declaration against the single test-side verifier expectation in `tests/_helpers.py`. A future mismatch therefore still fails deterministically.

The executor reproduced the pre-fix failures and reported the following post-fix gates:

```text
uv run --locked pytest -q tests/test_execution_control_contract.py tests/test_canonical_layout.py
47 passed

uv run --locked pytest -q
126 passed

uv run --locked ruff check .
PASS

uv run --locked ruff format --check .
PASS
```

## L001 consequence

T009 acceptance proves the planned correction is suitable. L001 does not become `VERIFIED` yet: its selected control must first be integrated into `develop`, and T008 must subsequently rerun successfully on the corrected baseline as required by `docs/learning/L001-protocol-version-baseline-drift.md`.

## Integration authorization

The exact reviewed executor HEAD `e0c80c62c1c543504719616c547d4df03d1b3d21` is authorized to proceed through an implementation PR to `develop` after this acceptance record is integrated.
