# T063-R4 — Stage 5 Harness Repair and Clean Rerun Authority

Status: `STAGE5_CANDIDATE_PUBLISHED / STAGE6_AUTHORIZED_AWAITING_HUMAN_CODEX_START`  
Date: 2026-09-10  
Authority: Human Owner / ChatGPT Orchestrator  
Task: `docs/tasks/T063-adaptive-worker-routing-requalification.md`  
Historical launch authority: `docs/reviews/T063-R2.md`  
Blocked-run review: `docs/reviews/T063-R3.md`  
Harness re-entry research: `docs/research/R019-T063-MULTI-AGENT-V2-HARNESS-REENTRY.md`  
Measurement authority: `docs/decisions/D063-qualified-codex-read-only-child-measurement-surface.md`  
Materialization authority: `docs/decisions/D068-library-first-candidate-materialization-executor-verification-boundary.md`; `docs/decisions/D076-stage6-ephemeral-executable-materialization-boundary.md`  
Provider/model calls during Orchestrator repair: `0`

## Verdict

T063 is authorized for one **clean complete v2 rerun** using the published Stage 5 harness on:

```text
test/t063-adaptive-worker-routing-requalification-v2
```

The old blocked evidence branch remains immutable historical evidence:

```text
test/t063-adaptive-worker-routing-requalification
3d8a9460988351383a90adfc6b76e2deff056504
```

No historical scored arm may be reused in the v2 pilot calculation.

## Stage 5 candidate

ChatGPT Orchestrator has materialized the substantial execution harness under:

```text
evals/adaptive_worker_routing/
tests/test_t063_adaptive_worker_routing_harness.py
```

The published candidate implements:

- frozen P1/P2/P3 inputs/oracles from `69e910f329a2294c3b40df0f6ee983f9905f4677`;
- exact six-arm ordering and profile matrix;
- provider-free oracle preparation and fixture generation;
- exact Codex/App Server `0.153.4` preflight;
- explicit Multi-Agent V2 feature/profile overrides;
- one fresh App Server + read-only parent + fresh child per scored arm;
- exact child task-message equality verification through the child thread;
- D063 configured profile/permission/residency/usage/duration/reroute receipts;
- first-attempt immutable scoring and pilot-decision computation;
- v2-only evidence emission;
- D076 ephemeral-artifact audit fields.

Provider-free Stage 5 verification is:

```text
python -m pytest -q tests/test_t063_adaptive_worker_routing_harness.py
21 passed

python -m compileall -q evals tests
PASS
```

The exact remote branch HEAD containing this review and candidate is frozen by the Human launch card after remote publication verification. Codex must start from that exact remotely verified HEAD and must not substitute a later branch state silently.

## Scientific restart decision

The previous P1/P2 matched pairs are **not** reused for final scoring.

Reason: the corrected run changes material execution conditions — published controller ownership, exact V2 task transport, explicit feature configuration, exact child-message verification, P3 input location, and fresh App Server isolation per arm. A mixed historical/new six-arm result would not be one homogeneous matched experiment.

The v2 scored set is therefore exactly six fresh first attempts:

```text
P1 ADAPTIVE -> CONTROL
P2 CONTROL  -> ADAPTIVE
P3 ADAPTIVE -> CONTROL
```

Historical usage/quality data remain preserved only as blocked-run evidence.

## Frozen source inputs

The probe source/oracle baseline remains unchanged:

```text
69e910f329a2294c3b40df0f6ee983f9905f4677
```

This branch contains newer harness/authority files, but P1/P2 answers and the P3 seed are always derived from the exact frozen commit above. The harness must not reinterpret current branch source as the probe oracle.

## Runtime/profile freeze

Use exactly:

```text
Executor:        Codex
Surface:         Codex Local / native Windows
Session:         NEW
Coordinator-ID:  AG | agent-governance | T063 | root-2
Root model:      gpt-5.6-sol
Root reasoning:  medium
Codex CLI:       exactly 0.153.4
App Server:      exactly 0.153.4
Auth category:   chatgpt
```

`0.154.0` is now a newer stable Codex release, but it is intentionally **not** adopted for this rerun. `0.153.4` remains the D063-qualified and previously frozen experimental runtime. Running T063 v2 on another version is a stop/re-entry condition.

The child matrix is unchanged:

| Probe | Arm | Requested model | Requested reasoning |
| --- | --- | --- | --- |
| P1 | CONTROL | `gpt-5.6-sol` | `medium` |
| P1 | ADAPTIVE | `gpt-5.6-luna` | `medium` |
| P2 | CONTROL | `gpt-5.6-sol` | `medium` |
| P2 | ADAPTIVE | `gpt-5.6-terra` | `medium` |
| P3 | CONTROL | `gpt-5.6-sol` | `medium` |
| P3 | ADAPTIVE | `gpt-5.6-terra` | `high` |

No model, effort or runtime substitution is authorized after any provider-backed scored turn begins.

## D060 root authority

Use a NEW failover root:

```text
AG | agent-governance | T063 | root-2
```

Root-1 is historical and is not safe to continue for the clean rerun: it executed the blocked harness, observed historical arm results and P3 failure context, and predates the published D076-compliant Stage 5 candidate. Reusing it would contaminate the intended fresh execution context and make the old/new process boundary ambiguous.

This is a D060 same-work-unit failover, not a new Task Contract.

## Exact Multi-Agent V2 transport

The harness, not free-form Executor improvisation, owns the experiment topology.

At `0.153.4`, every scored parent must use the exact candidate behavior:

```text
features.multi_agent=true
features.multi_agent_v2.enabled=true
features.multi_agent_v2.expose_spawn_agent_model_overrides=true

spawn_agent:
  task_name: exact harness-generated arm name
  message: exact frozen child task message
  model: exact frozen arm model
  reasoning_effort: exact frozen arm effort
  fork_turns: "none"
```

Do not send `fork_context`; Multi-Agent V2 rejects it.

The parent is a read-only measurement carrier, not an oracle worker. It may perform the one authorized spawn and wait/return flow only. Parent command execution, file reads, MCP calls, file changes or other substantive probe work invalidates the arm.

The child `thread/read` user message must equal the frozen task message exactly before the arm can score. This check is task-message transport evidence only; it is not restated as provider-signed backend prompt identity.

Frozen task-message SHA-256 values:

```text
P1  9aa60aef807873669690a2ad2b564fed58e0731ff0d6e0162fbef40c621a2164
P2  031c06d7544f6146901e633d4bceb8af0e15325c0124dc586e10f545aaadfe4a
P3  9d980424d08517c72911eaacaa9cee36ddf2003d50c21ad6b4d0c25786d9554f
```

## P3 runtime-root correction

The Executor supplies only a fresh **directory location** for the already-materialized harness to use. It does not implement P3 fixture semantics.

The runtime root must be:

- outside the repository/worktree;
- outside Windows system temp;
- an unused sibling of the exclusive T063 v2 worktree;
- absent before the harness creates it.

The harness creates and validates the single fixture, checks host readability and frozen SHA, and deletes it after the matched P3 pair.

If the live read-only child still cannot read the prepared fixture, stop as `BLOCKED_EXECUTION_INVALID_INPUT`; do not relocate and retry a scored first attempt under the same run.

## D063 live preflight

Before the first scored provider-backed arm, the Executor must run the published provider-free/native preflight and fail closed unless all mandatory D063 surfaces are present, including:

```text
real parent/child correlation
parent activePermissionProfile.id == :read-only
continuous parent residency through child reattachment
child activePermissionProfile.id == :read-only
non-contradictory read-only legacy projection
requested/resolved configured model + reasoning
exact non-estimated child-turn usage
exact child-turn duration
exact-child reroute observation
no tracked/global mutation attributable to measurement
```

`backend_served_profile_verified` remains `false` unless a separately qualified stronger receipt exists; none is introduced by R019/R4.

## Stage 6 execution and repair boundary

Codex executes, diagnoses and technically repairs the **published candidate** under D068.

In-authority repair is limited to technical defects in the harness implementation that preserve this review's frozen experimental semantics. Any need to change probe meaning, arm order, source freeze, task messages/digests, profile matrix, scoring, D063 receipt meaning, P3 defect/oracle, or acceptance requires Orchestrator re-entry.

D076 applies throughout Stage 6:

- ordinary commands, schema/help captures, response payloads, inline snippets and tiny mechanical glue are allowed;
- substantial new controller/harness/fixture/oracle implementation outside the published candidate is forbidden;
- every file-based executable artifact created outside the candidate and actually used to influence verification must appear in `ephemeral_artifacts`;
- `material` or `uncertain` missing-candidate artifacts require immediate re-entry.

## Evidence branch and output

The v2 execution branch is the same published candidate branch:

```text
test/t063-adaptive-worker-routing-requalification-v2
```

The normal final evidence files are new paths so historical evidence remains untouched:

```text
handoffs/T063-adaptive-worker-routing-telemetry-v2.json
handoffs/T063-executor-handoff-v2.json
```

The Executor must not edit committed Markdown.

Technical Stage 6 repairs to the published non-Markdown harness are permitted only inside the frozen semantics above and must be represented in the terminal branch before evidence acceptance.

## Pilot decision

The original T063 pilot-decision definitions remain controlling. Compute the v2 pilot only from the six fresh first attempts.

No result from the historical blocked branch may be counted toward:

```text
control_pass_count
adaptive_pass_count
exact token totals
exact duration totals
first-attempt failures
false positive/negative counts
pilot_decision
```

R007 remains `EVALUATING` until Orchestrator convergence after the terminal v2 evidence.

## Human-mediated transport

D071 controls. ChatGPT does not start or control Codex.

After remote verification of this published branch, the launch state is:

```text
AUTHORIZED_AWAITING_HUMAN_CODEX_START
```

The Human starts a NEW Codex session with the frozen launch profile, uses the exact Coordinator-ID/title, and pastes the complete Orchestrator-supplied prompt.

## Terminal return

Return only:

```text
STATUS: COMPLETED | BLOCKED
HANDOFF: handoffs/T063-executor-handoff-v2.json
BRANCH: test/t063-adaptive-worker-routing-requalification-v2
HEAD: <actual remote pushed HEAD>
```
