# T023 Review R16 — T061 MG1-v13 Stage 6 Blocked Convergence

Status: `BLOCKED_ACCEPTED / REENTER_STAGE5`

Date: 2026-09-07

Owner: Human Owner / ChatGPT Orchestrator

Controlling Task Contract: `docs/tasks/T061-mg1-v13-positive-anchor-reference-evaluation.md`

Prior launch gate: `docs/reviews/T023-R15.md`

Executor handoff: `handoffs/T061-executor-handoff.json`

## Stage 7 disposition

ChatGPT Orchestrator accepts the Executor's terminal `BLOCKED` result for T061 Stage 6.

The blocker is independently reconstructable from canonical Git and is not accepted merely from the Executor chat response.

Verified remote Executor terminal state:

- branch: `test/t023-skill-activation-topology-evals-v13`
- terminal HEAD: `d0ebe46a68c02c66dcfbb21c3dfaee43fb15c27f`
- terminal commit parent / reviewed implementation HEAD: `f893a03d17596182db209322f5357cda75ea3781`
- terminal commit changes only `handoffs/T061-executor-handoff.json`
- handoff status: `BLOCKED`
- provider/model calls issued: `0`
- synthetic canary calls issued: `0`
- acceptance observations issued: `0`

The Executor did not modify Markdown, candidate/reference bytes, corpus, oracle, trial envelope, harness, dependency state or configuration.

## Independently verified blocker

T061-P3 and the Stage 5 presentation construction require G3 v4 Skill files to be copied byte-for-byte from their corresponding `presentations-v3/G3/` sources.

At Candidate Freeze A `a454091aff7bb932372a6057e2d9804f94e66320`, `evals/skill_activation_topology/candidate-hashes-v13.json` declares:

```text
evals/skill_activation_topology/presentations-v4/G3/source-maintainer/SKILL.md
  copy_equivalence ->
evals/skill_activation_topology/presentations-v3/G3/source-maintainer/SKILL.md
```

Canonical Git contradicts that declaration:

```text
v3 G3 source-maintainer blob = e00edee8b1a1bacbb47b01bad4492f8ddf954628
v4 G3 source-maintainer blob = 13f064d186af528557d1e389e271aaa684122eb1
```

The file contents are materially different, not merely different line endings. The v4 G3 target matches the frozen hash recorded for the v4 file, but it is not the required byte-identical copy of the v3 G3 source.

Therefore the Stage 5 candidate-integrity assertion was false at Freeze A even though later anti-contamination checks correctly preserved the already-frozen bytes.

## Root-cause classification

Classification:

`STAGE5_CANDIDATE_MATERIALIZATION_DEFECT / COPY_EQUIVALENCE_FAILURE`

This is not currently classified as a defect in the T061-P3 requirement or controlling topology semantics. The specification already requires the correct byte-for-byte G3 copy. The defect is that Stage 5 materialized the wrong bytes while recording the intended G3 provenance.

Stage 6 correctly failed closed before deterministic execution, host preflight, canary or acceptance observations. Repairing the frozen semantic asset was outside Executor authority.

## Freeze-boundary consequence

T061 states that from Freeze B onward, any change to a v13 candidate/reference byte invalidates corpus v7 and oracle v13, and re-entry must allocate a new corpus/oracle/execution identity before any acceptance call.

Accordingly:

- Freeze A `a454091a...` remains historical evidence and is not rewritten;
- Freeze B `7b990f4d...` remains historical evidence and is not rewritten;
- corpus `MG1-T023-CORPUS-v7` and oracle `MG1-T023-TOPOLOGY-ORACLE-v13` remain frozen historical v13 assets and MUST NOT be reused as acceptance authority after correcting the G3 candidate byte;
- the terminal Executor commit `d0ebe46a...` and its handoff remain preserved evidence;
- no force-push, rebase, reset or historical cleanup is authorized.

The Orchestrator must re-enter D068 Stage 5 and create a fresh prospective candidate-freeze boundary plus new corpus/oracle/execution identities before a later Stage 6 acceptance launch. Exact successor identity names are intentionally not selected by this review; they must be allocated coherently during the re-entry materialization/trace update.

## Executor continuity

The T061 Codex coordinator `AG | agent-governance | T061 | root-1` reached a valid terminal blocker for this invocation. No Executor work is authorized while Stage 5 re-entry is in progress.

If T061 later returns to Stage 6 and the existing root remains safely recoverable, D060 prefers `CONTINUE` on the same coordinator root after fresh Git synchronization and newly persisted launch authority. A new root is not created merely because of this Orchestrator barrier.

## Acceptance / non-acceptance

Accepted:

- the terminal `BLOCKED` handoff as valid Stage 6 evidence;
- the provider-free fail-closed behavior;
- the diagnosis that G3 v4 source-maintainer violates its declared v3 G3 byte-copy relation;
- re-entry to Orchestrator Stage 5.

Not accepted / not performed:

- no candidate qualifies;
- B2 was not executed;
- F2/G3 were not scheduled;
- no topology is selected;
- no release decision exists;
- T024 remains blocked;
- D066 remains unchanged;
- T058 remains frozen.

## Next controlled frontier

T061 remains the governing work unit, but `READY_FOR_STAGE6` is no longer the effective current frontier after this accepted blocker.

The controlling current state is:

`BLOCKED_ACCEPTED -> REENTER_STAGE5`

The next Orchestrator action is to materialize the correct G3 v4-equivalent successor bytes from the canonical v3 G3 source, allocate the mandatory fresh post-Freeze-B acceptance identities, rebuild the integrity boundary without modifying historical Freeze A/Freeze B, and only then determine renewed Stage 6 readiness.
