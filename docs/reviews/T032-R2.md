# T032-R2 — RCAB snapshot/live separation acceptance review

Review ID: `T032-R2`  
Task: `T032`  
Disposition: `ACCEPTED`  
Date: 2026-08-22  
Reviewed base: `develop@7141b18d1a3f62a9b8e35b1d3b5e7628dd101dcf`  
Submitted executor HEAD: `044491aa5cb87814f0b34952cd1d56462f24ddad`  
Implementation anchor: `eb7f8f53883b6ae13b2d8f8ef5623642d4626255`

## Scope and identity

The submitted branch is five commits ahead and zero behind the reviewed base. The net task diff is exactly:

- `tools/repository_context.py`;
- `tests/test_repository_context.py`;
- `baselines/repository-context-manifest-v1.json`;
- `handoffs/T032-executor-handoff.json`.

No committed Markdown, T021/T022, dependencies, Governance Core, Skill/runtime/profile, release, model/network, or D047 threshold changes are present.

D029 identity is valid: `implementation_head_sha` is `eb7f8f53883b6ae13b2d8f8ef5623642d4626255`; the final remote HEAD `044491aa5cb87814f0b34952cd1d56462f24ddad` adds only handoff finalization after that implementation anchor.

## R1 blocker resolution

The R1 blocker is resolved.

The snapshot schema is now `1.2.0` and carries a non-self-referential `snapshot_payload_digest` over the complete canonical epoch-evidence payload. Canonical JSON bytes are required. Snapshot-carried registry semantics are canonicalized and independently hashed; registered entry metadata must match that registry; registered entry path/class/routes/physical-metric/SHA types, values, uniqueness and ordering are validated; bootstrap/router state is recomputed exactly from snapshot entries plus the accepted reference.

The implementation therefore distinguishes a valid historical snapshot from a locally altered projection without consulting mutable live source.

## Acceptance criteria

### AC-T032-1 — PASS

Deterministic epoch snapshot semantics, canonical byte identity, no Git/self-reference, and complete snapshot-payload binding are present. Repeated generation is byte-identical.

### AC-T032-2 — PASS

Live status derives registry, physical metrics, content identity and ratchet state from current source rather than snapshot values.

### AC-T032-3 — PASS

The required R1 tamper classes are independently covered and rejected:

- class/routes mismatch against snapshot registry semantics;
- focused physical-metric alteration through payload binding;
- arbitrary registry identity even after refreshing the top-level payload digest;
- bootstrap files/current/accepted-reference/delta/warning/warning-reasons/ratchet-candidate alteration even after refreshing the top-level payload digest;
- invalid entry types/values/order;
- semantically equivalent but non-canonical JSON bytes.

The preserved explicit fresh/stale/tampered currentness controls remain present.

### AC-T032-4 — PASS

A legitimate registered-source advance leaves historical snapshot integrity valid, makes explicit currentness stale, and leaves live status current.

### AC-T032-5 — PASS

D047 remains exactly `2 files / 21,471 bytes / 298 lines / 5% warning / non-blocking`.

### AC-T032-6 — PASS

Source-only/package isolation remains intact. No Consumer/Core/Skill/runtime/profile/dependency/network drift is present.

## Verification evidence

Executor handoff reports:

- Ruff check: PASS;
- Ruff format check: PASS;
- focused repository-context suite: `56 passed`;
- T020 isolation: `9 passed`;
- full deterministic suite: `321 passed`;
- Python compilation: PASS;
- snapshot integrity CLI: PASS;
- live status CLI: PASS;
- manifest JSON parse: PASS;
- repeated snapshot byte identity: PASS;
- diff/scope checks: PASS.

No GitHub-hosted CI status exists for the submitted HEAD; acceptance relies on exact remote code/diff review plus the persisted deterministic local verification evidence above.

## Disposition

`T032` is `ACCEPTED` at remote HEAD `044491aa5cb87814f0b34952cd1d56462f24ddad` and may be integrated into `develop`.

T021 remains frozen until T032 integration completes and the canonical post-integration frontier is recorded. Do not continue directly from this review into T021 before that integration step.
