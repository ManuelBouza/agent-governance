# T063-R14 — V7 Stage 5 Readiness Review

Status: ACCEPTED / STAGE6_AUTHORIZED_AWAITING_HUMAN_START  
Date: 2026-09-11  
Task: `docs/tasks/T063-adaptive-worker-routing-requalification.md`  
Design authority: `docs/reviews/T063-R13.md`  
Prior convergence: `docs/reviews/T063-R12.md`

## Candidate reviewed

```text
branch: test/t063-adaptive-worker-routing-requalification-v7
HEAD:   9fb55e8f36570f6b91d0a23720ccc6a62a9d0b90
base:   a3c719ad862128e292c7313b39966fbbbe156799
```

Remote comparison verifies that the candidate is exactly one commit ahead of the post-design protected-base snapshot.

The candidate adds exactly 24 executable/test files relative to that base:

- the accepted v3 core harness package and test;
- the accepted v4 reattachment adapter and test;
- the accepted v5 model-evidence adapter and test;
- the accepted v6 live parent-surface adapter and test;
- the new v7 package (`__init__.py`, `__main__.py`, `runner.py`);
- the new v7 replicated-qualification regression test.

Historical v1-v6 terminal telemetry/handoff evidence is not promoted into the candidate.

## Preserved executable lineage

The v3-v6 packages and their four tests were promoted using their exact accepted v6-candidate blob identities. In particular, no historical measurement layer was rewritten while materializing v7.

V7 is therefore a thin successor layer over the qualified v6 substrate.

## V7 materialized behavior

The v7 adapter changes only the prospectively approved R13 surfaces:

1. replaces the six-arm order with the frozen four-block / 24-arm counterbalanced schedule;
2. records `replicate`, `sequence_index`, `within_block_index`, and deterministic `trial_id` on every fully measured child snapshot;
3. preserves valid quality FAILs and continues the remaining fixed schedule;
4. aggregates per-probe/profile attempt count, PASS count, total tokens, total duration, tokens per success, duration per success, and profile-integrity evidence;
5. computes the four per-probe pair outcomes;
6. computes global ADAPTIVE and CONTROL absolute-quality qualification independently;
7. applies the prospective 10% exact-token materiality floor only when both profiles are globally quality-qualified;
8. implements the complete R13 pilot-decision enum;
9. overrides inherited v5 run-level evidence semantics so a complete 24-child run is execution-valid regardless of valid quality PASS/FAIL composition;
10. preserves v4 reattachment, v5 child-level evidence attribution, and v6 live parent-surface measurement unchanged.

## Deterministic regression coverage

The new v7 conformance test covers at least:

- exact four-block / 24-arm schedule;
- 2/2 per-probe order counterbalancing;
- `QUALIFIED_PROFILE_AND_USAGE_EFFICIENCY`;
- `QUALIFIED_PROFILE_ROUTING_ONLY`;
- `ADAPTIVE_QUALITY_QUALIFIED_CONTROL_DEFICIENT`;
- `NOT_QUALIFIED` on adaptive quality failure;
- resource cost of valid FAILs remaining in tokens-per-success numerator;
- adaptive reroute disqualification;
- refusal to produce a pilot decision from an incomplete run;
- unique replicate/trial metadata;
- complete-run model evidence remaining `VALID` when a child has a valid quality FAIL.

## Orchestrator verification performed

Completed before publication/readiness:

```text
candidate/base ancestry:        VERIFIED REMOTELY
candidate delta boundary:       VERIFIED REMOTELY — 24 files
v3-v6 exact blob reuse:         VERIFIED BY REMOTE BLOB IDS
new v7 Python AST parse:        PASS
v7 pure-logic smoke checks:     PASS
schedule length:                24
replicate count:                4
counterbalance shape:           PASS
```

The pure-logic smoke checks exercised the four principal decision paths and the fixed 24-arm schedule without provider/model calls.

## Orchestrator execution limitation

A fresh clone of the published v7 candidate was attempted from the Orchestrator sandbox. The sandbox again could not resolve `github.com`. The locally installed environment also does not contain Ruff.

Therefore this review does **not** claim repository-native pytest, Ruff or compileall success for v7.

This limitation is fail-closed by the Task Contract. Stage 6 on native Windows MUST execute every required deterministic/provider-free test/compile/lint/oracle/preflight gate successfully before the first provider/model call. Any failure returns `BLOCKED` with zero scored provider calls.

No provider/model call was consumed during v7 Specify/Design/Plan, Stage 5 materialization or readiness review.

## D076 review

The replication controller, aggregation/taxonomy behavior and conformance tests are substantial task-specific executable material. They were correctly materialized by ChatGPT Orchestrator in Stage 5.

Stage 6 may diagnose and make only bounded represented mechanics-preserving repairs. Any missing or defective material experiment design/controller/oracle behavior requires Orchestrator re-entry rather than Executor-side replacement tooling.

## D077 readiness review

Official Codex release state was revalidated after candidate publication:

```text
qualified pin:              0.153.4
latest stable:              0.154.0
newest observed prerelease: 0.155.0-alpha.3.9
newer stable than 0.154.0:  none
```

No new stable release changes the v7 launch analysis. D063 remains qualified specifically on `0.153.4`; the experiment therefore retains exactly that runtime/App Server version.

Disposition:

```text
PIN_RETAINED
```

If a stable release newer than `0.154.0` appears before the first v7 provider call, execution must stop for D077 relevance classification.

## Scientific-readiness decision

R13 corrected the v6 baseline/taxonomy defect prospectively. The candidate implements that design without consuming historical v6 observations as v7 trials.

The fixed sample is calibration evidence, not a formal population-level non-inferiority study. Outcome-conditioned sample extension and compensating quality reruns remain forbidden.

Stage 5 is complete and the exact candidate above is authorized for Human-mediated Stage 6, subject to all pre-provider gates in the revised Task Contract.

## Launch continuity

V6 `root-6` is retired after its consumed run and material successor redesign.

V7 uses a NEW same-work-unit failover root:

```text
Executor:       Codex
Surface:        Codex Desktop / native Windows
Session:        NEW
Coordinator-ID: AG | agent-governance | T063 | root-7
Root model:     gpt-5.6-sol
Root reasoning: medium
Runtime:        exactly 0.153.4
App Server:     exactly 0.153.4
Auth:           chatgpt
```

The launch profile remains separate from the Human transport prompt.
