# T020-R2 — Self-contained artifact acceptance

Review: T020-R2  
Status: ACCEPTED  
Task: T020 — Self Contained Build Artifact And Identity  
Reviewed executor HEAD: `0aad8ce78b52a4bd2a4851663d675048215a539c`  
Reviewed implementation commit: `d1d478da36a8c05c14181126abb34aa999aa632d`  
Prior review: `docs/reviews/T020-R1.md`  
Integration PR: #127

## Review result

T020 is ACCEPTED at exact HEAD `0aad8ce78b52a4bd2a4851663d675048215a539c`.

OP054 Stage A passed independently: PR #126 contains a durable receipt with base `bc5bff41f115158a49dfce1c57c0cef646604678`, `STATUS: DONE`, retirement of `docs/chained-executor-transitions`, remaining branches `develop`, `feat/t020-self-contained-governance-artifact`, `main`, and `EXCEPTIONS: none`.

The T020-R1 correction from `a50b4bbb572c44e0715fda2b49955f36bbf043d2` consists of one implementation commit plus one metadata-only handoff commit. The implementation changes only `src/agent_governance/artifact.py` and `tests/test_governance_artifact.py`; the successor changes only `handoffs/T020-executor-handoff.json`.

No Markdown, dependency, lockfile, provider/model, host-permission, protocol-semantic, Governance Core semantic, release-promotion, or unrelated runtime change is present in the candidate.

## T020-R1 closure

### Distribution boundary

The builder now uses an explicit positive `SKILL_SOURCE_FILES` allowlist containing exactly `SKILL.md`, the seven current Consumer assets, and `scripts/governance.py`. The prior broad subtree copy is removed, so source lifecycle/status `STATUS.md` is outside the Consumer artifact.

Regression coverage requires exact equality with the intended generated Skill-side file set, explicitly proves `STATUS.md` is absent, and retains byte comparison of generated Core/runtime/assets/schema material with canonical build inputs.

Finding 1 is closed for T020.

### Artifact-only operations

The isolation test builds from disposable source, deletes that source tree, proves engine/Core/assets resolve below the artifact root, and then successfully executes representative valid `bootstrap`, `validate`, `state`, `event`, `skill`, `ecosystem`, and `archive` operations in an unrelated repository. `--help` remains only a seven-command surface assertion and is no longer substituted for execution evidence.

Finding 2 is closed for T020.

## Preserved architecture

The accepted candidate preserves:

- artifact-local shared runtime resolution;
- generated Core from canonical `governance-core/`, not a second maintained authority;
- no artifact runtime dependency on source-tree siblings;
- distinct build-schema, Skill, protocol, installed-footprint, source-commit, inventory and digest identity dimensions;
- same-input identity reproducibility and atomic build behavior;
- exact seven-command Consumer v1 surface and frozen T018/T019 behavior.

## Verification evidence

The final handoff reports:

- focused artifact packaging/isolation: 3 passed;
- T018 characterization: 2 passed;
- T018 + Consumer regression: 77 passed;
- T019 shared-engine structural: 1 passed;
- full deterministic suite: 265 passed;
- Ruff check/format, `py_compile`, identity-schema JSON parse and `git diff --check`: PASS;
- network required: false.

The handoff now accurately states that all seven commands were directly executed from the isolated artifact.

## Residual boundaries

Skill version and installed-footprint version remain explicit build inputs because the source has no authoritative release values yet. Exact source-commit provenance for exported trees remains release-pipeline evidence, while payload digests identify built bytes. These are non-blocking T020 boundaries and remain for later release gates.

## Acceptance decision

All T020 Task Contract criteria and T020-R1 conditions are satisfied at exact HEAD `0aad8ce78b52a4bd2a4851663d675048215a539c`.

Only that exact HEAD may be integrated through PR #127. Any source-branch advancement before integration invalidates this acceptance and requires re-review.

Acceptance does not promote a public release and does not make T021 READY. The post-T020 ICAE methodology gate remains mandatory before T021.
