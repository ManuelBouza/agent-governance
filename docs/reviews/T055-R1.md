# T055-R1 — Codex Child Observability Qualification Review

Status: ACCEPTED  
Qualification-Decision: `PARTIAL_OBSERVABILITY`  
Date: 2026-09-05  
Authority: ChatGPT Orchestrator  
Task: `docs/tasks/T055-codex-child-observability-qualification.md`  
Executor branch: `test/t055-codex-child-observability-qualification`  
Submitted HEAD: `71cd53d4a83a300d6f741aade627a7ee2cc99a67`  
Evidence integration PR: `#280`  
Integrated evidence commit: `55758864fa68a953637e0203d52a6d994ed8e045`

## Review conclusion

T055 is **ACCEPTED as a faithful execution of the frozen observability-qualification contract**. Acceptance of the execution does not mean the measurement substrate qualified for a successor adaptive-routing pilot.

The frozen qualification result is correctly:

```text
PARTIAL_OBSERVABILITY
```

The actual installed Codex host demonstrated useful supported child telemetry:

- one real spawned child was uniquely correlated to its instrumented parent;
- requested `gpt-5.6-terra / low` matched supported resolved child-thread state;
- exact non-estimated token usage was attributable to the child thread and turn;
- child-turn duration was attributable;
- installed Codex/App Server version provenance was captured;
- no reroute was observed and backend-served model/reasoning identity remained explicitly unverified.

However, the mandatory sandbox gate failed. The supported child receipt reported `workspaceWrite`, not an unambiguous non-write/read-only envelope. Therefore T055 cannot be upgraded to either qualified outcome.

No global child-routing policy, R007 reactivation, D055 change, persistence-policy change, quantitative savings claim, or product change is authorized.

## Evidence boundary

Remote convergence verified:

- canonical task base: `develop@9522bde72476e63e88fb898d93a56a36aacdd3f1`;
- submitted Executor HEAD: `71cd53d4a83a300d6f741aade627a7ee2cc99a67`;
- submitted HEAD is exactly one commit ahead of the task base;
- the Executor branch changed only:
  - `handoffs/T055-child-observability-telemetry.json`;
  - `handoffs/T055-executor-handoff.json`;
- no product code, Markdown, dependency file, Governance Core, D055 policy, routing policy or persistent `.codex/agents/` catalog changed;
- no GitHub commit-status checks existed on the submitted HEAD.

Evidence was integrated through PR `#280` as `55758864fa68a953637e0203d52a6d994ed8e045`.

## Contract conformance

### D042 and launch profile

D042/RB001 passed against the canonical remote baseline. The run preserved pre-existing checkout/worktree state and used an isolated current worktree.

The Human-visible Executor root remained:

```text
Executor: Codex
Session: NEW
Model: GPT-5.6 Sol
Effort: Medium
```

No root-profile change was recorded.

### Installed version and surface

The actual installed host was:

```text
Codex CLI:      0.149.0
App Server:     0.149.0
Transport:      stdio / JSON-RPC 2.0
Authentication: chatgpt category
Python SDK:     not used
```

This is older than R008's 0.153.4 reference. The Executor did not upgrade the global installation. Instead it used native 0.149.0 generated schemas/help to verify the required fields before provider-backed execution.

The run used direct App Server JSON-RPC with a temporary external controller and added no repository dependency.

### Real child topology

The instrumented parent used `gpt-5.6-sol / medium` with a supported `readOnly` parent sandbox.

Exactly one real collaboration child was requested and discovered. The child was distinct from the parent and reported `subAgent/thread_spawn`, depth `1`, with the expected task path.

Stable `sourceKinds` enumeration returned no direct-parent match. Deterministic correlation therefore required the documented experimental `thread/list parentThreadId` filter.

This satisfies the topology evidence requirement while preserving the experimental-discovery dependency.

### Requested versus resolved profile

Requested child profile:

```text
model:            gpt-5.6-terra
reasoning effort: low
fork_turns:       none
service tier:     default/omitted
```

Supported child `thread/resume` state, obtained without model/reasoning overrides, reported:

```text
model provider:   openai
model:            gpt-5.6-terra
reasoning effort: low
```

Therefore `resolved_thread_profile_verified = true` is accepted.

This does **not** establish provider-served per-turn identity. `backend_served_profile_verified` correctly remains false because no stronger public execution receipt was exposed.

### Sandbox gate

The parent receipt was:

```text
readOnly
networkAccess: false
```

The child receipt was:

```text
workspaceWrite
writableRoots: []
networkAccess: false
excludeTmpdirEnvVar: false
excludeSlashTmp: false
```

Direct per-spawn sandbox override was not supported by the exercised spawn surface.

This fails AC-T055-6. Empty `writableRoots` cannot be treated as equivalent to `readOnly`: the supported receipt still identifies `workspaceWrite`, and temporary write surfaces were not excluded. The contract requires an unambiguous non-write child envelope and forbids upgrading a partial result by inference.

### Child-attributable usage

The supported `thread/tokenUsage/updated` receipt was attributed to the exact discovered child thread and child turn:

```text
total tokens:           22577
input tokens:           22562
cached input tokens:    0
cache-write tokens:     0
output tokens:          15
reasoning output tokens: 0
model context window:   258400
estimated:              false
```

The arithmetic is internally consistent and no token value was estimated.

### Child-attributable duration

The exact discovered child turn exposed supported timing records with:

```text
duration: 3743 ms
```

Synthetic parent wall-clock timing was not substituted for child duration.

### Reroute and backend identity

The installed schema exposed `model/rerouted`. No reroute notification was observed for the child turn.

Absence of reroute was correctly **not** treated as backend-served-profile verification.

## Acceptance criteria

```text
AC-T055-1   PASS
AC-T055-2   PASS
AC-T055-3   PASS_WITH_EXPERIMENTAL_DISCOVERY
AC-T055-4   PASS
AC-T055-5   PASS
AC-T055-6   FAIL
AC-T055-7   PASS
AC-T055-8   PASS
AC-T055-9   PASS
AC-T055-10  PASS
AC-T055-11  PASS
```

The AC-T055-6 failure is an experimental result, not an execution-contract violation. It forces `PARTIAL_OBSERVABILITY` under the frozen decision rules.

## Research disposition

### R008 — Codex child observability surface

Disposition: `DEFERRED`.

T055 materially improved the evidence base: supported profile state, exact child/turn usage and duration are available on the actual host, and experimental relation discovery can correlate the real child. But the required non-write child sandbox receipt is absent on the exercised 0.149.0 host.

Therefore the measurement substrate is not yet sufficient for a corrected adaptive-routing evaluation under the frozen governance/safety boundary.

Reconsider R008 when a current installed/supported Codex surface can demonstrate an unambiguous non-write child sandbox receipt for a real spawned child without relying on product dependency changes, private persistence, or global configuration mutation. A later qualification may still use the documented experimental relationship filter if all measurement gates otherwise pass and the dependency remains explicit/version-pinned.

### R007 — adaptive subagent compute routing

Disposition remains: `DEFERRED`.

T055 does not reactivate R007. No corrected routing Task Contract is authorized while the child sandbox measurement/safety gate remains unresolved.

## Final authority boundary

T055 is closed as:

```text
Execution: ACCEPTED
Qualification decision: PARTIAL_OBSERVABILITY
R008 decision state: DEFERRED
R007 decision state: DEFERRED
D055: unchanged
Global child-routing policy: unchanged
Quantitative savings claim: none
```

Do not rerun T055 unchanged on the same installed 0.149.0 surface. A future qualification requires a materially changed supported child-sandbox surface or other persisted Orchestrator authority.