# T063-R5 — Blocked V2 Execution Convergence

Status: `BLOCKED_ACCEPTED / NO_PILOT_DECISION / ORCHESTRATOR_REENTRY_REQUIRED`  
Date: 2026-09-10  
Authority: Human Owner / ChatGPT Orchestrator  
Task: `docs/tasks/T063-adaptive-worker-routing-requalification.md`  
Launch authority: `docs/reviews/T063-R4.md`  
Receipt correction research: `docs/research/R020-T063-V2-LIVE-RECEIPT-CORRECTION.md`  
Measurement authority: `docs/decisions/D063-qualified-codex-read-only-child-measurement-surface.md`  
Stage 6 materialization authority: `docs/decisions/D076-stage6-ephemeral-executable-materialization-boundary.md`

## Terminal return verified

The Human returned:

```text
STATUS: BLOCKED
HANDOFF: handoffs/T063-executor-handoff-v2.json
BRANCH: test/t063-adaptive-worker-routing-requalification-v2
HEAD: 3ff745a8d29e031ca818c1bc618b15a54e0cbf2b
```

GitHub verification confirms that the returned HEAD exactly matches the remote branch.

The v2 terminal branch is three commits ahead of the frozen launch candidate `7bc77fdeaf77662264357bd3a2f3a04796d5ee81`. Its post-launch delta is limited to:

```text
evals/adaptive_worker_routing/measurement.py
evals/adaptive_worker_routing/runner.py
tests/test_t063_adaptive_worker_routing_harness.py
handoffs/T063-adaptive-worker-routing-telemetry-v2.json
handoffs/T063-executor-handoff-v2.json
```

No committed Markdown or product source outside the published evaluation harness changed.

## Stage 6 repairs accepted

Before any provider-backed scored turn, Codex made two represented bounded technical repairs:

```text
8472718db9f6a82b4c79b9493255f3c314ef27b2
3088501147b408503acdd93b8bc7b76e6f957bac
```

They respectively:

1. parse the actual `0.153.4` App Server version receipt from the initialized response/user agent;
2. remove an invalid provider-free preflight archive operation.

These are accepted as D068/D076 Stage 6 repairs because they modify the already-published candidate, are visible in Git, preserve the frozen scientific semantics, and occurred before the first scored provider-backed attempt.

The candidate HEAD immediately before provider execution was therefore:

```text
3088501147b408503acdd93b8bc7b76e6f957bac
```

## Provider-call accounting

The accepted run accounting is exactly:

```text
scored parent turns:       1
scored child attempts:     1
compensating attempts:     0
diagnostic child attempts: 0
valid scored children:     0
```

The only consumed scored attempt was:

```text
P1 ADAPTIVE
gpt-5.6-luna / medium
```

It was not rerun.

## P1 ADAPTIVE disposition

Post-run supported `thread/read` identified the exact child and resolved its configured profile as Luna/Medium. Its answer matched the P1 oracle.

That observation is retained as:

```text
PASS_DIAGNOSTIC_ONLY_UNSCORED
```

It is not a T063 PASS because the live App Server process ended before the mandatory D063 measurement receipts could be qualified. The evidence has no exact non-estimated token usage, no qualified child permission receipt, no continuous-parent-residency receipt, and no exact-child reroute observation.

Accordingly:

```text
control_pass_count:  0
adaptive_pass_count: 0
pilot_decision:      null
```

There is no worker-quality failure to score and no profile-resolution failure to count.

## Root cause

R020 establishes that the published Stage 5 harness and T063-R4 carried two incorrect Multi-Agent V2 receipt assumptions for Codex `0.153.4`:

1. the harness waited for a live `collabAgentToolCall` item to correlate the V2 child, while the exact spawn handler emits `subAgentActivity` to App Server and sends the collab structure to analytics;
2. R4 required the child `thread/read` history to contain a single exact `userMessage`, while the spawn task is delivered as `InterAgentCommunication` and the public thread-history reducer does not project that rollout item into the reconstructed child turn.

The blocker is therefore classified as:

```text
BLOCKED_EXECUTION_INVALID
cause: ORCHESTRATOR_STAGE5_ADAPTER_AUTHORITY_DEFECT
```

This is not attributed to Codex noncompliance.

## Raw-event route not adopted

Exact `0.153.4` source shows that `thread/start.experimentalRawEvents=true` can expose `rawResponseItem/completed`, and a raw `ResponseItem::FunctionCall` contains function name, arguments and call ID.

That could potentially attest the actual `spawn_agent` arguments, but the protocol labels this surface internal-only and D063 did not qualify it. T063-R4 did not authorize it.

R5 therefore does not approve raw response events for scoring. A later repair must either separately qualify that surface or establish another supported receipt strategy before further provider-backed attempts.

## D076 review

Terminal evidence contains no Executor-created non-candidate file-based executable artifacts:

```text
ephemeral_artifacts: []
executor_material_ephemeral_artifacts: []
```

No D076 regression is observed. The Executor used the published harness, represented its bounded repairs in Git, consumed one invalid attempt, stopped, and returned BLOCKED.

## Scientific consequence

T063 v2 does not produce a pilot decision.

The single invalid P1 ADAPTIVE attempt cannot be reused. R4's homogeneity requirement remains controlling: if T063 is repaired again, the eventual scored pilot must be another clean six-arm run under one corrected receipt method.

The following remain preserved historical evidence and excluded from future scoring:

```text
v1 blocked HEAD: 3d8a9460988351383a90adfc6b76e2deff056504
v2 blocked HEAD: 3ff745a8d29e031ca818c1bc618b15a54e0cbf2b
```

## Verdict

```text
remote terminal evidence:       VERIFIED
Stage 6 bounded repairs:        ACCEPTED
provider attempts consumed:     1 scored child / 1 parent
valid scored arms:              0 / 6
P1 ADAPTIVE answer:             diagnostic PASS only
profile-resolution failure:     NO
worker-quality failure:         NO
oracle leakage:                 none observed
D076 violation:                 NO
pilot_decision:                 NONE
T063 execution continuation:    NOT AUTHORIZED
required next state:            ORCHESTRATOR REENTRY
R007 global routing policy:     UNCHANGED / EVALUATING
```

No new Codex/provider execution is authorized by this review.