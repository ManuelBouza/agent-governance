# T023 Review R18 — T061 MG1-v14 Stage 5 Completion

Status: `READY_FOR_STAGE6 / AWAITING_SEPARATE_HUMAN_LAUNCH`

Date: 2026-09-07

Owner: ChatGPT Orchestrator

Controlling work unit: `T061`

Stage 5 re-entry authority: `docs/reviews/T023-R17.md`

Prior blocked convergence: `docs/reviews/T023-R16.md`

## Outcome

T061 D068 Stage 5 re-entry is complete on the prospective MG1/T023 v14 line.

The v13 terminal blocker accepted by R16 was a candidate-materialization defect, not a failure of the frozen T061 semantic design: historical v4 `G3/source-maintainer/SKILL.md` used the F2 source-maintainer copy instead of the declared byte-identical v3 G3 source. The corrected v14 candidate projection now uses the exact v3 G3 source blob and was frozen before any exact v14 holdout prompt was authored.

This review establishes technical readiness for a later D068 Stage 6. It does not authorize an Executor, provider/model calls, Stage 7 acceptance, topology selection, T024, D066 changes, or T058 re-entry.

## Prospective branch and ancestry

Topic branch:

`test/t023-skill-activation-topology-evals-v14`

Prospective line:

1. current canonical `develop` base at re-entry: `f0811365698667bae9b0ab77a9300c8c9aa988b7`;
2. R17 re-entry authority: `de96230a9921ce318cb816ca3d657d0936be2ac1`;
3. Candidate Freeze C: `1fc38f979d67ff29649f69ae39b8d46d2523518b`;
4. Holdout/oracle Freeze D: `f8cee7c72688f0486211474d2678d018772e2a58`;
5. v14 harness mechanics: `ba508cb451624c0d08dd1bee81f1fd5b4a1bc848`;
6. characterization alignment: `4edef37df67abf7548c57cb4eb6663ea188dfa40`.

All topic-branch ref moves in this re-entry were fast-forward updates. Historical v13 evidence was not rewritten, rebased, reset, amended, relabeled or force-pushed.

## Freeze C — corrected candidate boundary

Freeze C is:

`1fc38f979d67ff29649f69ae39b8d46d2523518b`

It freezes the prospective candidate set before the exact v14 acceptance holdout:

- capability-source epoch: `MG1-2026-09-06-v4`;
- topology revision: `MG1-T023-TOPOLOGIES-v4`;
- presentation revision: `MG1-T023-PRESENTATIONS-v5`;
- candidate hash identity: `MG1-T061-CANDIDATE-HASHES-v2`;
- candidates: `B2`, `F2`, `G3`;
- historical unscheduled candidates: `B0`, `B1`.

The accepted materialization correction is present:

- prospective `presentations-v5/G3/source-maintainer/SKILL.md` Git blob: `e00edee8b1a1bacbb47b01bad4492f8ddf954628`;
- canonical v3 G3 source-maintainer Git blob: `e00edee8b1a1bacbb47b01bad4492f8ddf954628`;
- historical erroneous v4 materialization blob remains historical evidence and is not rewritten.

`candidate-hashes-v14.json` requires v5 F2/G3/shared copy equivalence against the corresponding v3 source paths, including the corrected G3 source-maintainer path.

Freeze C was remotely published and verified before exact v14 acceptance prompts were authored.

## Freeze D — fresh holdout/oracle boundary

Freeze D is:

`f8cee7c72688f0486211474d2678d018772e2a58`

Freeze C -> Freeze D changes exactly:

- `evals/skill_activation_topology/corpus.json`;
- `evals/skill_activation_topology/oracle.json`;
- `evals/skill_activation_topology/verify_v14_holdout_integrity.py`.

No candidate/reference bytes, candidate hash manifest, topology semantics, presentation manifest, or capability-source semantics changed across the holdout boundary.

Frozen identities:

- corpus: `MG1-T023-CORPUS-v8`;
- oracle: `MG1-T023-TOPOLOGY-ORACLE-v14`;
- execution epoch: `MG1-T023-EXECUTION-v14`;
- presentation revision: `MG1-T023-PRESENTATIONS-v5`;
- trial envelope: `MG1-T023-TRIAL-ENVELOPE-v2`;
- Candidate Freeze C: `1fc38f979d67ff29649f69ae39b8d46d2523518b`.

The v14 corpus preserves the accepted T061 geometry:

- positive-consumer: 6;
- positive-source-maintainer: 6;
- positive-external-skill-trust: 6;
- negative: 10;
- near-miss: 30, six on each accepted near-miss axis;
- ambiguous: 4;
- cross-profile: 4;
- multi-intent: 4;
- total: 70;
- false-activation denominator: 40.

The v14 holdout explicitly excludes exact prompt reuse from both historical acceptance corpora:

- v13 terminal corpus at `d0ebe46a68c02c66dcfbb21c3dfaee43fb15c27f`;
- v12 terminal corpus at `3e5bec392d0b8e5804c4efaad74b795b08dc9779`.

No v13 or v12 observation may enter v14 scoring.

## Preserved oracle semantics

No acceptance threshold, scheduling rule, materiality rule or host cell was relaxed during re-entry.

v14 preserves:

- activation precision `>= 0.95`;
- activation recall `>= 0.95`;
- activation F1 `>= 0.95`;
- false activation rate `<= 0.05`;
- wrong specialist rate `<= 0.05`;
- overactivation rate `<= 0.05`;
- semantic outcome accuracy `>= 0.95`;
- zero critical cross-profile violations;
- zero critical ambiguous permission broadening;
- mandatory deterministic/profile-isolation/consumer-source-independence/source-distribution/single-install non-regression;
- B2 as the only reference-stage candidate;
- F2/G3 unscheduled unless B2 qualifies;
- paired 2+1 aggregation with pair-scoped conditional third repetition and no fourth repetition;
- critical any-occurrence disqualification;
- challenger materiality relative to B2: F1 `+0.03`, median observed context ratio `<= 0.85`, FAR delta `<= 0`, wrong-specialist delta `<= 0.01`, overactivation delta `<= 0.01`;
- the accepted tie-break order;
- native Windows / GPT-5.6 Sol / Medium;
- exact Codex CLI baseline `0.149.0`;
- unchanged synthetic Skill canary requirement `2/2 PASS` after deterministic and workspace/backend preflight gates.

## Harness mechanics after Freeze D

Post-Freeze-D harness work is technical mechanics only.

Commits:

- `ba508cb451624c0d08dd1bee81f1fd5b4a1bc848` — adapt v14 harness mechanics;
- `4edef37df67abf7548c57cb4eb6663ea188dfa40` — align characterization coverage.

Remote comparison from Freeze D through `4edef37d...` changes only harness modules, compatibility guard entrypoints and tests. It changes none of:

- `presentations-v5/**` candidate/reference bytes;
- `candidate-hashes-v14.json`;
- `topologies.json`;
- `presentations/manifest.json`;
- `docs/AGENT-GOVERNANCE-CAPABILITY-SOURCE.md`;
- `corpus.json`;
- `oracle.json`;
- `verify_v14_candidate_integrity.py`;
- `verify_v14_holdout_integrity.py`.

The mechanics conservatively reuse the previously represented v13 technical blobs where the method is unchanged and introduce v14-specific identity validation where identities changed.

`_harness/frozen_inputs.py` now fail-closes on the v14 oracle/corpus/presentation identities, Candidate Freeze C ancestry, candidate/reference hashes and Freeze C bytes, v5-to-v3 copy equivalence, 70-case/40-FAR geometry, five near-miss axes, and exact historical prompt exclusion against both v13 and v12.

The legacy compatibility filenames `verify_v13_candidate_integrity.py` and `verify_v13_holdout_integrity.py` are technical wrappers only; they delegate to the current v14 guards because the reused provenance module invokes those historical command names. They do not define independent semantic authority.

The reused deterministic evidence field names `candidate_bytes_unchanged_since_freeze_a` and `exact_v12_prompt_reuse_count` are compatibility labels inherited from the v13 mechanics. They are not the complete v14 freshness proof. v14 freshness additionally fail-closes through `frozen_inputs.py` and `verify_v14_holdout_integrity.py` against both historical v13 and v12 corpora.

## Stage 5 verification evidence

Provider/model calls during the entire v14 Stage 5 re-entry: exactly `0`.

Evidence established by the Orchestrator in this environment:

- remote branch/commit ancestry and fast-forward publication verified through GitHub;
- Freeze C -> Freeze D path boundary verified remotely;
- Freeze D -> current mechanics path boundary verified remotely;
- corrected G3/source-maintainer target blob equals the canonical v3 G3 source blob;
- current candidate-hash manifest and v14 guards remain unchanged after their freezes;
- newly authored Python modules/tests were parsed/compiled successfully with Python `py_compile` in the Orchestrator runtime;
- no GitHub Actions run exists for the v14 topic branch at this review point.

Not claimed as executed/passing during Stage 5:

- repository `ruff check`;
- repository `ruff format --check`;
- full pytest suite;
- code-health/symbol-map runtime commands;
- Stage 6 deterministic evidence bundle;
- native-Windows backend/workspace preflight;
- synthetic canary;
- B2/F2/G3 provider/model observations.

`ruff` is not installed in the current ChatGPT runtime. Stage 5 therefore does not manufacture a PASS for runtime gates that were not executed.

## Required Stage 6 gate order

A later Human-authorized Executor MUST begin provider-free and fail closed:

1. synchronize the represented v14 branch and verify Freeze C/Freeze D ancestry and current frozen identities;
2. run candidate-integrity and holdout-integrity guards;
3. run the frozen deterministic regression, scheduler simulation, repository lint/format/pytest/code-health/symbol-map gates required by the harness;
4. establish that provider/model calls issued so far equal `0`;
5. only after all deterministic gates pass, resolve the frozen native-Windows backend/workspace profile;
6. only after workspace/backend PASS, run the unchanged synthetic Skill canary and require `2/2 PASS`;
7. only then schedule B2 Stage R;
8. schedule F2/G3 only if B2 qualifies;
9. perform Executor-owned Code Review & Verify and create/push the required handoff before terminal response.

A failure before step 6 must not issue synthetic or acceptance model calls where the oracle forbids them. A failure before B2 qualification leaves F2/G3 unscheduled.

## Readiness decision

T061 MG1-v14 is technically `READY_FOR_STAGE6`.

This readiness is intentionally narrower than launch authorization:

- no Executor is active;
- no Stage 6 launch authority is created by R18;
- no provider/model call is authorized by R18;
- a new, separate Human-selected Stage 6 launch objective is required because the prior v13 launch terminated in an accepted blocker and R17 explicitly required a later launch decision after re-materialization;
- the next launch authority, if Human-selected, must name the v14 branch and current reviewed Stage 5 HEAD rather than reuse the historical v13 launch gate.

Historical v13 evidence remains immutable. T058 remains frozen. D066 remains unchanged. No release topology is selected and T024 remains blocked pending later accepted topology selection.
