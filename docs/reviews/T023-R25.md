# T023 Review R25 — T061 v14-r1 Renewed Stage 6 Launch Gate

Status: `STAGE6_AUTHORIZED / AWAITING_HUMAN_CODEX_START`

Date: 2026-09-08

Owner: ChatGPT Orchestrator

Controlling work unit: `T061`

Stage 5 readiness: `docs/reviews/T023-R24.md`

Consumed prior launch authority: `docs/reviews/T023-R19.md` — historical only

## Human selection

The Human Owner explicitly selected renewed T061 D068 Stage 6 on 2026-09-08 by replying `go` after O249 reported `READY_FOR_STAGE6_AWAITING_HUMAN_SELECTION`.

This review is the current Stage 6 launch authority for T061 v14-r1. It does not revive R19 and does not reinterpret historical v14 Stage 6 evidence at `test/t023-skill-activation-topology-evals-v14@75f52f64adddfc738ab1d5362efdfe90cf327786`.

## Authorized execution target

Represented Stage 6 topic branch:

`test/t023-skill-activation-topology-evals-v14-r1`

Ready implementation head:

`6f98f2d64babbbbae32ff7f8b6460a9123c993b9`

Terminal Stage 5 evidence head / current remote branch head at authorization:

`aea43441a424fe18003176cb05b5594b8b561a68`

Candidate Freeze C:

`1fc38f979d67ff29649f69ae39b8d46d2523518b`

Holdout/oracle Freeze D:

`f8cee7c72688f0486211474d2678d018772e2a58`

Canonical `develop` at authorization:

`10e1e4f09eee7cef39ace3a9a6d2a2905e668968`

Remote launch revalidation established:

- `develop` still resolves to `10e1e4f09eee7cef39ace3a9a6d2a2905e668968`;
- O249 remains current and requires a new launch gate after separate Human selection;
- R24 remains `READY_FOR_STAGE6 / AWAITING_SEPARATE_HUMAN_SELECTION`;
- `test/t023-skill-activation-topology-evals-v14-r1` still resolves exactly to `aea43441a424fe18003176cb05b5594b8b561a68`;
- that evidence head is a direct child of implementation head `6f98f2d64babbbbae32ff7f8b6460a9123c993b9`;
- Stage 5 provider-free readiness is green, including Ruff lint/format, code-health, deterministic subsets, scheduler and full locked pytest `477 passed`;
- T061 harness provider/model calls remain exactly `0` through Stage 5;
- exact Codex CLI `0.149.0` is realizable on the selected native-Windows host through isolated official `@openai/codex@0.149.0` invocation without replacing the default desktop-bundled `0.153.4` installation;
- Freeze C / Freeze D and all frozen scientific assets remain unchanged.

Later orchestration Markdown on `develop` is not permission to merge/rebase/cherry-pick `develop` into the scientific branch merely to consume authority. Read controlling Markdown from `origin/develop` while preserving the represented scientific branch history.

## Executor launch profile

Concrete Executor: `Codex`

Surface: `Codex Local / native Windows / isolated official @openai/codex@0.149.0 realization`

Session: `NEW`

Coordinator-ID: `AG | agent-governance | T061 | root-4`

Model: `GPT-5.6 Sol`

Reasoning effort: `Medium`

Codex CLI: exactly `0.149.0`

Rationale:

- T061 freezes the live scientific cell to Codex / native Windows / GPT-5.6 Sol / Medium / exact CLI `0.149.0`;
- Stage 5 proved an isolated exact `0.149.0` realization path without changing repository dependencies/configuration or replacing the Human's default `0.153.4` installation;
- prior coordinator `AG | agent-governance | T061 | root-3` was deliberately scoped to Stage 5 provider-free verification/formatter repair and did not operate as the required provider-backed Stage 6 cell;
- Stage 6 therefore uses D060 failover to a fresh root rather than broadening the prior coordinator's authority/host assumptions;
- no newer CLI/model is accepted merely because it exists.

The Human SHALL start/select a NEW Codex Local session using the exact `0.149.0` realization. If exact `0.149.0`, native Windows, GPT-5.6 Sol or Medium cannot be realized for the Stage 6 executor, fail closed before any provider-backed T061 evaluation and return `BLOCKED`.

## Stage 6 authority boundary

R25 authorizes D068 Stage 6 Execute / Diagnose / Repair / Verify for T061 v14-r1 under the existing frozen scientific semantics.

It authorizes technical execution and bounded technical repair only where the repair preserves approved semantics and Design.

It does not authorize:

- D068 Stage 7 convergence/acceptance/integration;
- release topology selection;
- T024;
- D066 gap changes;
- T058 re-entry;
- semantic changes to B2/F2/G3 presentations;
- changes to Candidate Freeze C or Holdout/oracle Freeze D;
- changes to corpus v8, oracle v14, capability-source semantics, topology/presentation semantics, trial-envelope semantics, thresholds, scheduling, materiality or aggregation rules;
- history rewriting of historical Stage 6 evidence or Stage 5 evidence heads;
- treating default Codex CLI `0.153.4` as equivalent to `0.149.0`.

A suspected material requirement/specification/Design/Plan/oracle defect is a stop-and-return condition for ChatGPT Orchestrator re-entry, not Executor authority to tune the experiment.

T061 remains `ASSURED`. D065 remains applicable before substantial Stage 6 work and again before final Code Review & Verify.

## Mandatory execution order

The Executor MUST fail closed in this order:

1. synchronize with GitHub and establish a safe current local baseline without discarding unrepresented local work;
2. verify `origin/develop`, O250/R25 authority, represented `v14-r1` branch/head, implementation head and Freeze C/Freeze D ancestry;
3. verify exact native-Windows Codex CLI `0.149.0` realization for the Stage 6 execution cell before any provider-backed T061 call; default `0.153.4` is not equivalent;
4. run candidate-integrity and holdout-integrity guards;
5. run all required provider-free regression, Ruff lint/format, code-health, deterministic subsets, scheduler and full locked pytest gates;
6. prove T061 harness provider/model calls issued so far = exactly `0`;
7. resolve/verify the frozen backend/workspace profile under the exact live cell;
8. only after all prior gates PASS, run the unchanged synthetic Skill canary and require `2/2 PASS`;
9. only after canary PASS, run B2 Stage R under frozen corpus v8 / oracle v14 / execution v14;
10. if B2 does not qualify or reaches valid qualification futility, terminate `BLOCKED / NO QUALIFYING SINGLE-FAMILY REFERENCE` and leave F2/G3 unscheduled;
11. if B2 qualifies, establish B2 as the same-epoch reference and schedule F2/G3 only as permitted by the frozen scheduler/materiality rules;
12. perform Executor-owned Code Review & Verify, including the second D065 delegation evaluation;
13. create and push `handoffs/T061-executor-handoff-v14-r1.json` on the represented task branch before terminal response.

No local-only/chat-only result is terminal evidence. The terminal handoff and represented branch HEAD must be pushed to GitHub.

## Handoff requirements

The terminal handoff SHALL identify at least:

- `task_id: T061`;
- status `COMPLETED` or `BLOCKED`;
- branch `test/t023-skill-activation-topology-evals-v14-r1`;
- ready implementation head `6f98f2d64babbbbae32ff7f8b6460a9123c993b9`;
- Stage 5 evidence head `aea43441a424fe18003176cb05b5594b8b561a68`;
- Candidate Freeze C and Holdout/oracle Freeze D;
- exact Codex CLI realization evidence showing `codex-cli 0.149.0`;
- deterministic/provider-free gate outcomes;
- synthetic canary outcome and call counts if reached;
- B2 observations/qualification if reached;
- F2/G3 scheduling and observations only if B2 qualified;
- all provider/model call counts;
- code-review/delegation evidence;
- unresolved issues and terminal reason.

## Required terminal response

Return only:

```text
STATUS: <COMPLETED|BLOCKED>
HANDOFF: handoffs/T061-executor-handoff-v14-r1.json
BRANCH: test/t023-skill-activation-topology-evals-v14-r1
HEAD: <remote pushed HEAD sha>
```

## Human-mediated transport

D071 controls transport. ChatGPT does not start or control Codex. The Human starts the NEW coordinator and pastes the complete prompt below.

D072/D073 control title behavior. The launch card and prompt use the exact same Coordinator-ID.

## Transport prompt

```text
Operate as the Agente de IA Ejecutor for ManuelBouza/agent-governance.

Coordinator-ID: AG | agent-governance | T061 | root-4

Set the visible Codex chat title to exactly:
AG | agent-governance | T061 | root-4

If the current Codex surface does not expose a supported way for you to set or rename the chat title from within this session, do not claim success. State exactly:
CHAT_TITLE_ACTION_REQUIRED: AG | agent-governance | T061 | root-4
so the Human can apply the title in the UI, then continue under the same Coordinator-ID.

This is a NEW T061 MG1/T023 v14-r1 D068 Stage 6 execution.

Synchronize safely with GitHub first. Establish a safe current local baseline without discarding, cleaning, resetting, overwriting or otherwise destroying unrepresented local work.

Canonical repository:
ManuelBouza/agent-governance

Current canonical develop at launch authority:
10e1e4f09eee7cef39ace3a9a6d2a2905e668968

Read current repository instructions and execution authority from origin/develop, including:
- AGENTS.md
- docs/orchestrator/CHECKPOINT.md
- docs/tasks/T061-mg1-v13-positive-anchor-reference-evaluation.md
- docs/reviews/T023-R17.md
- docs/reviews/T023-R24.md
- docs/reviews/T023-R25.md

Do not merge/rebase/cherry-pick develop into the scientific task branch merely to read these Markdown authorities.

Use the represented task branch:
test/t023-skill-activation-topology-evals-v14-r1

Before substantive execution, origin/test/t023-skill-activation-topology-evals-v14-r1 MUST resolve to the Stage 5 terminal evidence head expected by R25:
aea43441a424fe18003176cb05b5594b8b561a68

The ready implementation head is:
6f98f2d64babbbbae32ff7f8b6460a9123c993b9

Preserve:
Candidate Freeze C 1fc38f979d67ff29649f69ae39b8d46d2523518b
Holdout/oracle Freeze D f8cee7c72688f0486211474d2678d018772e2a58

Historical evidence heads 75f52f64adddfc738ab1d5362efdfe90cf327786, 6ef1478122379578adc535a05d759658ea1d0847 and aea43441a424fe18003176cb05b5594b8b561a68 are durable and must not be rewritten.

Use exactly:
- native Windows
- GPT-5.6 Sol
- Medium reasoning effort
- Codex CLI exactly 0.149.0

The desktop-bundled/default Codex CLI 0.153.4 is NOT equivalent and MUST NOT be silently substituted.

Use the isolated official @openai/codex@0.149.0 realization path established by Stage 5 evidence. Verify the active Stage 6 execution cell reports exactly:
codex-cli 0.149.0
before any provider-backed T061 evaluation call. If that exact cell cannot be realized, fail closed and return BLOCKED.

T061 is ASSURED. Evaluate bounded delegation before substantial Stage 6 work and again before final Code Review & Verify. Preserve exclusive branch/worktree ownership, evidence traceability and semantic authority.

Fail closed in this order:
1. safe Git synchronization and represented branch/head/ancestry verification;
2. exact Codex CLI 0.149.0/native-Windows/GPT-5.6 Sol/Medium cell verification;
3. Candidate Freeze C integrity guard;
4. Holdout/oracle Freeze D integrity guard;
5. Ruff lint and Ruff format check;
6. code-health check/map and required deterministic regression subsets;
7. v14 provider-free scheduler simulation;
8. full locked pytest;
9. prove T061 harness provider/model calls so far = 0;
10. frozen backend/workspace preflight;
11. unchanged synthetic Skill canary, requiring 2/2 PASS;
12. B2 Stage R only after canary PASS;
13. F2/G3 only if B2 qualifies under the frozen oracle/scheduler/materiality rules.

The accepted Stage 5 baseline had candidate/holdout integrity PASS, Ruff PASS, format PASS, code-health PASS, deterministic subsets PASS, scheduler 3 passed with provider calls 0, and full pytest 477 passed. Re-run the required gates; do not merely trust the prior evidence.

Do not run synthetic canary, B2, F2 or G3 until all preceding gates authorize them.

Do not change any frozen scientific semantic asset. Do not tune candidate presentations, corpus, oracle, topology, thresholds, scheduling, materiality or aggregation semantics. A material requirement/Design/Plan/oracle defect requires stop and ChatGPT Orchestrator re-entry rather than semantic repair.

Technical repairs during Stage 6 are allowed only when they preserve the approved semantics/Design and remain within R25/D054/D068 authority. Do not create or edit committed Markdown.

If B2 fails to qualify or valid qualification futility is reached, terminate BLOCKED and leave F2/G3 unscheduled.

If B2 qualifies, use it as the same-epoch reference and schedule F2/G3 only under the frozen v14 rules.

Before terminal response, create and push:
handoffs/T061-executor-handoff-v14-r1.json

Push all required non-Markdown Stage 6 evidence to:
test/t023-skill-activation-topology-evals-v14-r1

Verify the remote branch HEAD equals the pushed terminal HEAD.

Return only:
STATUS: COMPLETED | BLOCKED
HANDOFF: handoffs/T061-executor-handoff-v14-r1.json
BRANCH: test/t023-skill-activation-topology-evals-v14-r1
HEAD: <actual remote pushed HEAD>
```

## Current launch state

`AUTHORIZED_AWAITING_HUMAN_CODEX_START`

No Stage 6 Executor evidence, synthetic canary, B2/F2/G3 observation or provider-backed T061 call is claimed by this launch review itself.
