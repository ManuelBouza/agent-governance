# T023-R34 — T062 v15 Stage 6 resumption revalidation

Status: STAGE6_CONTINUATION_REAUTHORIZED / AWAITING_HUMAN_CODEX_CONTINUE  
Date: 2026-09-12  
Authority: Human Owner / ChatGPT Orchestrator  
Task: `T062 / T023 v15 RIQ-NBC`  
Supersedes operational hold: `T023-R33`  
Preserves execution authority: `T023-R31`, `T023-R32`

## Human resumption selection

The Human Owner explicitly selected T062/T023 v15 for resumption on 2026-09-12 and stated that this selection removes the line from `HUMAN_HOLD_R32_UNCONSUMED`, while explicitly forbidding automatic consumption of any prior execution without fresh canonical revalidation.

This review is the canonical persistence of that selection after revalidating Git authority, scientific branch identity, frozen inputs, provider-call accounting, and version-sensitive upstream state.

## Canonical revalidation

Revalidation base on `develop`:

```text
8746e81c0d57476a4b9bc5f4e2d04333bcc07da1
```

Current T062 Task Contract remains:

```text
docs/tasks/T062-t023-riq-nbc-v15-reference-independent-evaluation.md
Status: READY
Stage-Readiness: READY_FOR_STAGE6
```

Current scientific branch remains exactly:

```text
branch: test/t023-skill-activation-topology-evals-v15
HEAD:   b9034e450f04fbc9736543425e531159d4b79d49
```

That HEAD remains one commit above the terminal Stage 5 candidate:

```text
Stage 5 candidate: 3e0d0b71cf382db502e186622f40a23bcd915390
```

The only delta is:

```text
handoffs/T062-executor-handoff.json
```

No scientific candidate, harness, frozen input, test, oracle, trial-envelope, or Markdown experiment specification changed on the scientific branch after the blocked attempt.

The frozen inputs remain ancestors of the scientific HEAD:

```text
Freeze E: 5b025087bc7b6996f683a34fdd1ce441d3d6dd82
Freeze F: 5b8ac55980ecdbb6a2bf3784812b933647f2f13d
```

`T023-R31`, `T023-R32`, and `T023-R33` remain present on current `develop`; no T062 Task Contract or T023-R31/R32/R33 mutation occurred after R33.

## Provider-call accounting

The persisted blocked handoff still records:

```text
provider/model calls issued: 0
canary attempts:              0
acceptance attempts:          0
total Stage 6 attempts:       0
```

The scientific branch has not advanced after that handoff. Therefore no prior provider/model execution is consumed or adopted by this resumption gate.

R32's Human-approved destination/payload/cost envelope remains unconsumed and is reactivated only prospectively by the Human selection represented here.

## D077 upstream revalidation

T062's frozen live cell requires:

```text
Host:       Codex
Runtime:    native Windows
Model:      GPT-5.6 Sol
Reasoning:  Medium
Codex CLI:  exactly 0.149.0
```

Official OpenAI Codex release state checked on 2026-09-12:

```text
pinned/reference:       0.149.0
current stable:         0.154.0
higher prerelease seen: 0.155.0-alpha.3.9
```

Official upstream sources:

- https://github.com/openai/codex/releases/tag/rust-v0.154.0
- https://github.com/openai/codex/releases

Disposition:

```text
PIN_RETAINED
```

Rationale:

- `0.149.0` is part of the frozen scientific live-cell identity, not a workaround chosen to evade a known CLI defect;
- the prior blocker occurred before Codex process creation at the managed external-transmission/cost-approval boundary, so it is not evidence of a `0.149.0` runtime defect;
- the newer stable/runtime surface is materially different and changing to it would introduce a new experimental variable and require requalification rather than a transparent continuation;
- no prior provider call was issued, so retaining the frozen version preserves the original prospective experiment rather than mixing observations across runtime versions.

Immediately before any provider-backed call, the Executor must fail closed if exact native-Windows Codex CLI `0.149.0` cannot be selected and verified. It must also stop for Orchestrator re-entry if the official stable release has advanced beyond `0.154.0` before launch, so D077 relevance can be reclassified before consequential execution.

## D076 prospective constraint

D076 applies prospectively to this resumed Stage 6.

The published Stage 5 candidate already contains the material evaluation harness. The Executor may execute, diagnose, and perform bounded technical repair within approved semantics, plus small subordinate execution mechanics.

If continuation would require first-pass creation of a substantial new controller, harness, semantic fixture generator, oracle, workflow state machine, or other material executable artifact, the Executor MUST stop before creating it and request Orchestrator Stage 5 re-entry.

Any file-based ephemeral executable aid actually used during resumed Stage 6 must be classified in the persisted handoff as required by D076.

## Continuity decision

D060/D058 continuity remains the default:

```text
Session:        CONTINUE
Coordinator-ID: AG | agent-governance | T062 | root-1
```

This is valid only if the Human can identify the same recoverable T062 root, represented scientific branch/workspace, and exclusive task worktree, and that root can reload current persisted authority from `origin/develop`.

If those continuity conditions cannot be established, do not guess and do not create a replacement root silently. Stop for Orchestrator failover classification; any replacement coordinator must be separately authorized as `root-2` or later.

## Resumed Stage 6 order

After Human-mediated transport, the Executor shall first revalidate remote and local launch identity before any provider/model call:

1. fetch `origin` and load current `origin/develop`, `AGENTS.md`, the T062 Task Contract, R31/R32/R33/R34, D076 and D077;
2. verify the scientific branch still equals `b9034e450f04fbc9736543425e531159d4b79d49` before new evidence;
3. verify its parent/delta relation to `3e0d0b71cf382db502e186622f40a23bcd915390` and Freeze E/F ancestry;
4. rerun the candidate/holdout integrity guards and required provider-free verification;
5. verify persisted provider/model attempts remain exactly zero;
6. verify native Windows, GPT-5.6 Sol, Medium, and exact Codex CLI `0.149.0`;
7. perform the previously blocked backend/workspace/model behavioral preflight;
8. only if every gate passes, execute the frozen 2/2 canary and then the frozen acceptance schedule within the existing R32/T062 ceilings;
9. persist terminal evidence/handoff and push the scientific branch for Orchestrator review.

Any mismatch is fail-closed and must not be repaired by silently changing scientific semantics, model, reasoning, runtime, CLI version, frozen inputs, attempt ceilings, or candidate set.

## Explicit exclusions

T063 remains closed under O278/T063-R20 as frozen mapping `NOT QUALIFIED`. No T063 observation is imported, rescored, replayed, or used as T062 evidence.

T058 remains frozen and is not resumed, integrated, cleaned, copied, or otherwise consumed by this authority.

No Stage 7 topology selection, T024 transition, or other successor work is authorized by this review.

## Resulting authority

```text
T062 Stage 6:                    REAUTHORIZED
R32 destination/payload/cost:   VALID / UNCONSUMED UNTIL HUMAN TRANSPORT
Scientific HEAD:                b9034e450f04fbc9736543425e531159d4b79d49
Provider/model calls consumed:  0
D077 disposition:               PIN_RETAINED at Codex CLI 0.149.0
Coordinator:                    CONTINUE T062 root-1 when safely recoverable
Next action:                    Human-mediated Codex CONTINUE under D055/D071
```

The launch gate is ready only after this review is merged into `develop`. The Human-facing launch card and thin transport prompt may then be rendered by the Orchestrator.