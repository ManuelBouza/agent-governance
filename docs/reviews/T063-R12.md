# T063-R12 — V6 Stage 7 Convergence and Baseline-Validity Re-entry

Status: ACCEPTED / V6 EVIDENCE VALID / PILOT INCONCLUSIVE_BASELINE / SPEC-DESIGN RE-ENTRY REQUIRED  
Date: 2026-09-11  
Task: `docs/tasks/T063-adaptive-worker-routing-requalification.md`  
Prior review: `docs/reviews/T063-R11.md`

## Reviewed evidence

Authorized v6 candidate:

```text
branch: test/t063-adaptive-worker-routing-requalification-v6
HEAD:   af2de380569285b63e33de3f53628cadcf4052b6
base:   d5ff447d3ad162ddfc8afd8baadeac67154d7a6a
```

Executor terminal evidence:

```text
STATUS: BLOCKED
HANDOFF: handoffs/T063-executor-handoff-v6.json
BRANCH: test/t063-adaptive-worker-routing-requalification-v6
HEAD: 276fa94cde6904c003482c8b527de7e8cda416d4
```

Remote comparison verifies that the terminal HEAD is exactly one commit ahead of the authorized candidate and adds only:

```text
handoffs/T063-adaptive-worker-routing-telemetry-v6.json
handoffs/T063-executor-handoff-v6.json
```

No candidate implementation, test, configuration or Markdown repair was made during Stage 6.

## Pre-provider and measurement verification

Accepted Executor evidence:

```text
candidate/base ancestry:           PASS
Codex CLI / App Server:            0.153.4 / 0.153.4
auth category:                     chatgpt
stable-release gate:               PASS; 0.154.0 remained latest stable
deterministic tests:               45 passed
compileall:                        PASS
Ruff:                              PASS
oracle preparation:                PASS
public parent-surface validation:  6/6 PASS
scored parent turns:               6
scored child attempts:             6
reattachment RPC retries:          6
compensating attempts:             0
diagnostic child attempts:         0
```

All six child snapshots satisfy the published D063/v6 execution-validity surface. All six are `execution_validity=VALID`; five are quality-preserving efficiency eligible and all six are model-quality eligible.

The v6 measurement correction therefore succeeded. The v3-v5 measurement-adapter blockers are not present in this run.

## First-attempt quality evidence

Observed frozen-arm result:

```text
P1 ADAPTIVE  gpt-5.6-luna / medium   PASS
P1 CONTROL   gpt-5.6-sol  / medium   PASS
P2 CONTROL   gpt-5.6-sol  / medium   PASS
P2 ADAPTIVE  gpt-5.6-terra/ medium   PASS
P3 ADAPTIVE  gpt-5.6-terra/ high     PASS
P3 CONTROL   gpt-5.6-sol  / medium   FAIL
```

The P3 CONTROL failure is a valid first-attempt `WORKER_QUALITY` result. It produced one material false negative and one material false positive against the frozen P3 oracle. It is not an execution, profile-resolution, permission, measurement, transport or adapter failure.

The valid P3 CONTROL failure MUST NOT be rerun away or replaced. The first-attempt evidence is consumed.

## Aggregate observations

```text
CONTROL quality:  2/3
ADAPTIVE quality: 3/3
CONTROL tokens:   84,985
ADAPTIVE tokens:  84,547
CONTROL duration: 104.161 s
ADAPTIVE duration:139.296 s
```

The raw six-arm totals show only 438 fewer adaptive tokens (about 0.5%) and materially higher aggregate adaptive duration. These totals are descriptive only: the failed P3 CONTROL arm is not quality-preserving efficiency evidence, so they do not support a valid accepted-quality savings claim.

The stronger result is qualitative: the prospectively frozen ADAPTIVE P3 profile passed while the root-equivalent CONTROL P3 profile failed. That is valid model-quality evidence, but it does not satisfy the predeclared baseline-validity condition for the T063 qualification decision.

## Contract/harness conflict discovered at convergence

The Task Contract and inherited T054 design require CONTROL 3/3 for a qualified comparison. The published harness enforces that gate by raising:

```text
ExecutionInvalid("CONTROL did not pass 3/3 first attempts; baseline invalid")
```

However, the same Task Contract also defines:

- a valid first-attempt quality FAIL as model-quality evidence;
- `execution_validity=VALID` for fully measured scored children, including quality FAILs;
- `pilot_decision = null` for blocked/incomplete runs;
- no explicit pilot outcome for a complete six-arm run in which CONTROL has a valid quality failure while ADAPTIVE passes.

V6 therefore exposed a prospective specification/decision-taxonomy gap inherited from T054. The run itself is technically complete and its six child executions are valid; classifying the whole run as `EXECUTION_INVALID` conflates baseline suitability with execution validity.

## Stage 7 interpretation

Accepted interpretation:

```text
six-arm execution completeness:         COMPLETE
all child execution validity:           VALID
model-quality evidence count:           6
quality-preserving efficiency count:    5
CONTROL baseline suitability:           INVALID_FOR_QUALIFICATION
matched accepted-quality cost claim:    NOT_AVAILABLE
pilot_decision under frozen enum:        null
R007 global routing decision:            NOT_ADOPTED
```

The Executor-emitted terminal label `BLOCKED_EXECUTION_INVALID` is retained as historical harness output, but it is not adopted as the semantic Stage 7 diagnosis of the six measured children.

The canonical Stage 7 diagnosis is:

```text
INCONCLUSIVE_BASELINE / SPECIFICATION_DESIGN_GAP
```

This diagnosis does not retroactively add a new v6 pilot decision. It explains why the frozen enum cannot validly classify the observed complete run.

## No post-hoc rerun

No v6 rerun, replacement P3 CONTROL attempt, compensating scored call, profile substitution or threshold change is authorized.

Repeating the same single-trial design until CONTROL happens to pass would condition continuation on the observed outcome and would violate the first-attempt/frozen-design protections that make the evidence interpretable.

The v6 launch authority is consumed.

## Required successor re-entry

Before any v7/provider-backed successor, T063 must re-enter **Specify / Design / Plan & Trace** prospectively.

At minimum the successor design must define, before execution:

1. the semantic outcome when a fully measured CONTROL arm has a valid quality failure;
2. whether the experiment is an absolute accepted-quality gate, a paired comparative/non-inferiority evaluation, or both;
3. how stochastic model variance is handled without outcome-conditioned reruns;
4. the minimum repeated-trial/sample structure, if repetition is used;
5. the decision taxonomy for `CONTROL_FAIL / ADAPTIVE_PASS`, `CONTROL_PASS / ADAPTIVE_FAIL`, both-pass and both-fail cases;
6. which observations are eligible for accepted-quality efficiency claims;
7. whether P3 CONTROL remains root-equivalent Sol/Medium or whether a separate accepted-quality reference is required without contaminating the routing variable.

The successor must preserve v6 as historical evidence and must not reuse any of its attempts as a new prospectively scored sample unless a future design explicitly treats historical data only as non-scored planning evidence.

## T063/R007 disposition

T063 v6 produced the first complete technically valid six-arm evidence set, but it does not authorize a global adaptive worker-routing policy.

R007 remains `EVALUATING`.

The evidence is sufficient to reject the assumption that the frozen root-equivalent Sol/Medium CONTROL is guaranteed to be a valid accepted-quality baseline for every probe. It is not sufficient to establish a general adaptive-routing efficiency policy from one trial per arm.

## Next action

Do not launch another T063 Executor run.

ChatGPT Orchestrator must perform the prospective outcome-taxonomy/statistical-design correction, then materialize a successor only after the revised specification, Design and Plan/Trace are frozen. Any future Task Contract must be materially revised against the Maintainer Skill v4 template before Stage 6 authorization.
